# FINDINGS — what the competitive loop needs from the layers it may not edit

The loop never edits the harness runner, its thresholds or corpora, the
standard, the CI gates, the inbound policy, or product source (the brief's
Phase 3 rules). A need in any of those is written here for the human who
owns that layer. `CF-n` numbering; a row is closed by naming the commit
that met it.

**Scope of every measurement row below.** The numbers are the loop's own
runs of the tools named, at their documented default install, on the
corpora and tasks the row names (so far one 277-file corpus and ten
tasks). Each is three runs in the same container, counted by the same
tokenizer. They exist to find work for jCodeMunch and to keep the loop
honest about its own methodology; they are not a product comparison and
they are not a recommendation. A row's caveats (a task the tool cannot
answer, an axis that is not like-for-like, an unstable triple) are part of
the row, and quoting a number without them misreads it.

| # | Finding | Where | Status |
|---|---|---|---|
| CF-1 | The tier is invoked as `python benchmarks/competitive/run.py`, not `python -m harness compete` as DESIGN D7 wanted: registering a tier means editing `harness/__main__.py` and `harness/tiers.json`, which the build rules leave to the harness owner. Until then the runner writes its own result file and the harness's `latest.json` does not carry a `compete` entry. | `harness/__main__.py` (a `tier_compete` beside `tier_bench`), `harness/tiers.json` (a `compete` list with one step) | OPEN, human |
| CF-2 | `docs/competitive/` was under `docs/*` in `.gitignore`; PR 1 adds the `!docs/competitive/` exception beside the other four. Recorded because a rotation that writes into `docs/` proves nothing by creating the file (CLAUDE.md Practice 5). | `.gitignore` | CLOSED by PR 1 |
| CF-3 | The jcodemunch adapter runs in a fresh subprocess on the runner, not in the D2 container. The container base, its flags and the digest bookkeeping arrive with the first competitor adapter (PR 2a), at which point jcodemunch moves into the same image shape so the sandbox cost is symmetric. Until then the jcm row's `index_cold_seconds` and `latency_call_ms` are not comparable to a containerised competitor's and the summary must say so. CLOSED 2026-09-05: `sandbox/jcodemunch.Dockerfile` + `sandbox/jcm_worker.py`; the jcm row runs under the same flags as cymbal when `--sandbox docker`, and a `none` run is labelled in the header. | `benchmarks/competitive/adapters/jcodemunch.py`, `sandbox/` | CLOSED by PR 2a |
| CF-4 | The proof task set (`tasks/self.json`) scores P1 at FILE level with a 100,000-line tolerance because the self corpus moves every commit and a pinned line would rot. Under the first (many-to-many) F1 the row read grep 0.6538 vs jcm 0.6349; under one-to-one matching (review finding 2) the re-measured result at `cdc6542` reads grep 0.2669 vs jcm 0.3333 (delta -0.0664, band 0.0167, meaningful). Neither number measures retrieval: with ONE expected line per task, precision is 1 over the citation count, so the axis is counting how many lines a tool cites (jcm five symbols, grep every matching line) and the verdict flips with the matching rule. Line-level ground truth with several expected lines per task on pinned corpora (Phase 3 item 3) is the measurement; until then f1_P1 on the self set is reported and must not be read as a lead for either side. | `benchmarks/competitive/tasks/self.json` | OPEN until item 3 |
| CF-5 | The shared file set was, in PR 1, what OUR discovery admits (`index_folder`'s skip patterns and size cap). PR 2a's first result showed why that flatters the home team: this tree's `server.py` and `parser/extractor.py` exceed the shipped `max_file_size` (the repo's own `.jcodemunch.jsonc` raises it, the scratch copy has no config), so both were withheld from every row, including the P4 ground truth for a task cymbal answered correctly (its 277 files against our 275, CF-15). The shared set is now the corpus's tracked text files (`git ls-files`, binaries skipped): what an agent with no tool could open. Each tool indexes what it wants and reports `files_indexed`; the null rows read everything. Consequence stated: the null token figures here no longer match `run_benchmark.py`'s (R34), which reads the index's file list; the tier's nulls are the larger, honest ones. | `benchmarks/competitive/run.py::build_corpus` | CLOSED by PR 2a round 2 |
| CF-6 | The `tools_list_tokens` axis for jcodemunch is read from `benchmarks/schema_baseline.json` (`full_full`, the shipped default), not measured live over stdio, because the live capture is the bench tier's `schema_baseline_live` step and a second capture would be a second copy of that measurement (R46/R50). A competitor's value WILL be measured live over stdio (DESIGN s2), so the two are the same quantity by different instruments; the first competitor adapter PR adds a live capture for jcodemunch too and this row closes. CLOSED, with the transport named: the worker serialises `server._build_tools_list()` IN-PROCESS (name, description, inputSchema, the zhang-liz shape) and the adapter counts it with cl100k; it is not an MCP stdio `tools/list` round trip, and the `counter` variant row of DESIGN s1.4 is not produced yet. | `adapters/jcodemunch.py::tools_list_tokens` | CLOSED by PR 2a |
| CF-7 | `pytest.mark.timeout` is not registered in this repo's pytest configuration (the `--timeout` plugin is absent, CLAUDE.md "Tests"), so the end-to-end test bounds its own subprocess with `timeout=580` instead. If the full tier ever gains a per-test timeout, this test wants one. | `tests/test_competitive_tier.py` | RECORDED |
| CF-8 | The stability rule (spread within 10% of the row's own median) marks every sub-millisecond latency row unstable: grep's per-call median is 0.34 ms with a 0.11 ms spread, read-all's 35.96 ms with 5.37 ms, both on the re-measured result at `cdc6542`. The rule is doing what DESIGN s5.1 says, so a fast tool's latency can never be called a meaningful gap on a noisy box; whether an absolute floor on the spread (in ms) belongs beside the relative one is a Phase 4 question, answered from three CI runs, never from one box. | `benchmarks/competitive/score.py::_stable` | OPEN, Phase 4 |
| CF-9 | A result file names the product commit (`header.jcm_commit`, the tree at run time) but the scorer that wrote it may be uncommitted: `results/2026-09-05-cdc6542.json` was scored by `score.py` as committed at 445bd73, so a reader who checks out cdc6542 and re-runs gets the many-to-many F1 (0.6538 / 0.6349), not the file's 0.2669 / 0.3333. Review round 2, finding 1. The next runner change adds `tree_dirty` and the scorer's file sha256 to the header, and `--record` keeps refusing a dirty tree. CLOSED: the header carries `tree_dirty` and `scorer_sha256` over run.py, score.py, adapter.py, every adapter and the sandbox directory. Reading a result file: `jcm_commit` is HEAD at run time and `tree_dirty: true` means the scorer and the image came from the working tree, which is the NEXT commit; the file committed with that commit has a `scorer_sha256` equal to `run._scorer_sha256()` at that commit, which is the reconciliation (verified for `2026-09-05-b70b439c.json` against e7d50a5 by the reviewer). | `benchmarks/competitive/run.py` header | CLOSED by PR 2a |
| CF-10 | A corpus must be a git repository. cymbal keys its index by git root and answers "not inside a git repository, results may be empty" otherwise: every query returned nothing on a plain directory copy of `src/` and every query answered after `git init` plus one commit (measured 2026-09-05). The runner now makes the self corpus a single-commit repository; the pinned corpora are checkouts already. A tool that needs history (churn, blame) would see one commit on the self corpus and its full history on the pinned ones, which is a fairness note for that tool, not a corpus change. | `benchmarks/competitive/run.py::_git_init`, `fairness/cymbal.md` | RECORDED, by design |
| CF-11 | The inbound ruleset (22311729) restricts ref CREATION to `inbound/**`, `inbound-ledger` and `main`; pushing `competitive/tier-and-nulls` was bypassed by the maintainer's role ("Bypassed rule violations"). A human pushes these branches, so nothing is blocked today; if Phase 3 item 6 ever has the App create a `competitive/**` ref, the ruleset's exclusion list is a RUNBOOK step (the App can only push where the ruleset lets it). | repository ruleset 22311729 | OPEN, human, only if item 6 needs it |
| CF-12 | Under the container a cymbal query costs about 750 ms (probe, self corpus, 277 files) against its README's "roughly 10-40 ms"; a process start, a read-only rootfs and a per-query auto-refresh walk are the candidate causes, none measured apart. The per-call axis is defined as the wait per call for this reason (DESIGN s5.1) and the fairness note names it. Separating process start from query time would need a resident mode the tool does not document; recorded so nobody reads the row as the tool being 20x slower than it says. | `fairness/cymbal.md` s"Where the harness may disadvantage it" | RECORDED |
| CF-13 | **The first competitor row reproduces sverklo-bench's P2 zero on our own tree.** On the self corpus at `b70b439c`, task `self-P2-cache_put` (expected: the one in-tree caller, `result_cache_put` in `token_tracker.py:1622`): `find_references(repo, identifier="cache_put")` at its shipped default returned no references (68 tokens, 0 citations, F1 0.0); cymbal `refs cache_put` returned exactly that line (F1 1.0); grep-top-3 0.1818. The CHANGELOG (v1.80.7 era) called the sverklo P2 result "partly real and partly a docs problem"; this is the real part, measured with our own methodology: the default `find_references` tracks imports and dbt refs, and a same-file call site is neither. `include_call_chain=true` is the documented variant DESIGN s1.4 reserves as a LABELLED row, never a silent substitution; it is not run yet. This is the shape of a `competitive-gap` draft (DESIGN s7.1, first hypothesis `tool_not_called` is wrong here, `coverage` is right) and it waits for Phase 3 item 5; no product source is touched by this layer. | `benchmarks/competitive/results/2026-09-05-b70b439c.json` row f1_P2; `src/jcodemunch_mcp/tools/find_references.py` (product, not this layer's to edit) | OPEN, competitive-gap candidate |
| CF-14 | **Inside the container jcodemunch's cold index of the self corpus costs 18.25 s (median of three, spread 3.26, unstable under the 10% rule) against 3.11 s on the host from the same tree** (`results/2026-09-05-cdc6542.json`, host, sandbox `none`), while cymbal indexes the same mount in 1.7 s. Both read `/corpus` over the same Windows bind mount; jcodemunch also WRITES its SQLite store to the `/out` bind mount (WAL, fsync), which cymbal does too but with a far smaller store. The candidate cause is bind-mount I/O under Docker Desktop on Windows, not the tool, and it is not separable on this box. The row is what the design says it is (the same sandbox cost on every row, DESIGN D2) and it is still a 5.9x move on OUR axis from the sandbox alone; Phase 4's three CI runs on a Linux runner (overlay, no gRPC-FUSE mount) decide whether the number belongs to the tool or to the box. Until then `index_cold_seconds` from a Windows box is reported and not read as a finding against either side. | `benchmarks/competitive/sandbox.py` (mounts), `results/2026-09-05-e7d50a53.json` row index_cold_seconds | OPEN, Phase 4 |
| CF-15 | cymbal indexes 277 files where our discovery admits 275 on the same tree: the two are `server.py` (5,000+ lines) and `parser/extractor.py`, withheld by our shipped `max_file_size` in a scratch copy with no `.jcodemunch.jsonc`. Measured consequence: `find_importers` misses `server.py` among the importers of `token_tracker.py` because it is not in our index at shipped defaults (8 of the 9 textual importers found, `results/2026-09-05-e7d50a53.json`; on the widened CF-19 truth the row reads jcodemunch 0.4324, cymbal 0.4737, codebase_memory 0.9825, `results/2026-09-05-d6e3d78e.json`). An agent asking about `server.py` gets nothing from our index at shipped defaults, on our own tree. A `competitive-gap` candidate on criterion 1 with hypothesis `index_missing_files` (DESIGN s7.1), recorded until item 5 drafts issues; the size cap is product (`security.py`, `max_file_size`), not this layer's to edit. | `results/` rows f1_P4 and files_indexed; `src/jcodemunch_mcp/security.py` `max_file_size` (product) | OPEN, competitive-gap candidate |
| CF-16 | tokens_per_task on the self set: cymbal 880.8 vs jcodemunch 1,656 (ratio 0.532, band 82.8, meaningful, spread 0.1 / 0). On the five T tasks the shapes are the same (search, then read 3); on P1 cymbal's single `investigate` call (278 to 832 tokens) is cheaper than our search plus three sources (1,877 to 3,258). Re-measured at `e7d50a53` after the file-set correction (CF-5): cymbal 880.9 vs 1,656, ratio 0.532, unchanged, because neither tool's payload depends on the null rows' file set. The null rows moved: grep-top-3 now costs 118,000 tokens per task on this corpus (it opens `server.py` whole), read-all 1,161,000. This is a competitor ahead on criterion 2 by more than the band on the one corpus measured, i.e. the `competitive-gap` condition of DESIGN s7.1 with hypothesis `payload_shape`; it is recorded here until item 5 drafts issues, and the pinned corpora must show it before it is raised, because the self corpus is one Python tree. The self corpus on this box is CRLF (cymbal's `import` strings carry `\r\n` in the fixtures), which moves token counts (harness F-13); the Linux runner's runs are the ones to raise from. | `results/2026-09-05-b70b439c.json` row tokens_per_task | OPEN, competitive-gap candidate |
| CF-17 | The fast tier's 90 s Floor is contended by Docker Desktop's WSL VM on this box: with the VM up the 90-file fast list took 93, 99, 103 and 105 s (the last on a clean `origin/main` worktree, so not this change); with `wsl --shutdown` it took 72 s; the documented baseline is 48 to 54 s (harness VERIFICATION). The Floor stays (F-19: rerun, never loosen). Until Phase 4 moves container runs to the Linux runner, the rule on this box is: run the competitive tier, stop Docker Desktop, then commit. Docker Desktop relaunched itself once after a stop, so it may be set to start at login. | `harness/thresholds.json` suite.fast_seconds (unchanged), this box | RECORDED, human habit |
| CF-18 | The first P4 task listed four importers of `storage/token_tracker.py`; the source has nine, and the tool that returned all nine (cymbal) was graded DOWN on precision against the incomplete truth (0.6154 vs our 0.6667, "meaningful", in the first PR 2a result). Review round 1, finding 1. The list is corrected from the source, and with the corrected truth the row inverts: cymbal 1.0, jcodemunch 0.9412 (CF-15); the rule it broke is DESIGN s4.2 (an adapter's author does not write ground truth for that adapter's weakest category), and the proof-of-tier set is the only set where that will be tolerated, because CF-4 already says none of its F1 rows is a lead for anyone. | `benchmarks/competitive/tasks/self.json` | CLOSED by PR 2a round 2; the rule stands for item 3 |
| CF-19 | **The P4 ground truth counts resolved dependencies, not textual imports.** codebase-memory-mcp returned 28 importers of `token_tracker.py` where the source has 9 textual ones, because it resolves `from ..storage import result_cache_put` through `storage/__init__.py`'s re-export to the module that defines the name. Grading that down on precision would be CF-18 again with a subtler truth. The truth is now computed from source by AST (scratchpad `p4_truth.py`, method in the task's `source` field): 9 direct importers plus 20 files that import from the `storage` package a name its `__init__` re-exports from `token_tracker`, 29 in all. On it: codebase_memory 0.9825 (28 of 29), cymbal 0.4737, jcodemunch 0.4324 (`find_importers` reads textual imports only, 8 of 29). The definition is the field's (sverklo's P4 is "file dependencies"); item 3 inherits it for the pinned corpora. | `benchmarks/competitive/tasks/self.json`, `tests/test_competitive_codebase_memory.py` | CLOSED by PR 2b; the definition stands for item 3 |
| CF-20 | **The closest peer leads on every comparable axis on the self corpus** (`results/2026-09-05-73fbd7cf.json`, three runs, container, Windows box; every quality and token delta meaningful): tokens per task codebase_memory 980.6 vs jcodemunch 1,656 (ratio 0.592, spread 0; cymbal 880.9); `tools/list` weight 4,791 vs 23,652 tokens (ratio 0.203: fifteen tools against our ninety at the shipped `full` surface; our `counter` variant is not yet a row, CF-6); f1_P1 0.6667 vs 0.3333; f1_P2 1.0 vs 0 (CF-13); f1_P4 0.9825 vs 0.4324 (CF-19). The cold-index rows of all three tools are unstable under the 10% rule in this run (jcodemunch median 18.9 s, spread 24.1 s; codebase_memory 13.1 s, spread 7.3 s; cymbal 1.7 s, spread 3.5 s: the first run after a Docker Desktop start pays the image and mount warm-up, CF-14), so no index claim is made from it. Four `competitive-gap` candidates against us on criteria 1, 2 and 4, hypotheses `coverage` (P2), `index_missing_files` and `coverage` (P4: textual imports only), `payload_shape` (tokens), and on the schema weight the shipped-default surface itself. Recorded here until item 5 drafts issues; the pinned corpora must show them; nothing here touches product source. | `results/2026-09-05-73fbd7cf.json`; `docs/competitive/fairness/codebase_memory.md` | OPEN, competitive-gap candidates (four) |
| CF-21 | codebase-memory-mcp refuses a cache whose parent directory it does not own ("the directory CONTAINING 'cbm-cache' is not a usable private-directory parent"), which the `/out` bind mount is under Docker Desktop on Windows. The sandbox gained `private_home=True`: one more tmpfs at `/private`, uid-owned, mode 0700, and `HOME` there; nothing else changes and the flag is pinned in the sandbox test. Its contents die with the container, so the adapter re-indexes (uncharged) in each of its three sessions (index, queries, snippets). The cost of that design is three cold starts per run, each the tool's own daemon start (container time only, no result row measures it), none charged to a task. | `benchmarks/competitive/sandbox.py::PRIVATE_TMPFS`, `adapters/codebase_memory.py` | RECORDED |
| CF-22 | codebase-memory-mcp refuses the corpus mount root as a project ("/corpus: path is too broad to index as one root; name a project directory below it") and accepts `/corpus/src`. The adapter does what the message says: it indexes each top-level directory that holds files as its own project and prefixes citations with the directory. On the self corpus that is one project, so nothing is doubled; on a pinned repository with several top-level directories every question would be asked once per project and every answer charged, which the fairness note names as the harness possibly disadvantaging it. Whether the refusal fires on a real repository root is item 3's first measurement for this tool. | `adapters/codebase_memory.py::_projects`, `fairness/codebase_memory.md` | RECORDED, re-check at item 3 |
| CF-23 | **code-review-graph's `importers_of` answers zero for every file of the self corpus at its base install**, so its f1_P4 is 0 against our 0.4324 (`results/2026-09-05-95eb4a00.json`, three runs, meaningful, delta -0.4324: a measured jcm lead on a quality axis, the second after f1_P1 against cymbal, 0.3333 vs 0.2606, which CF-4 says is not a lead for anyone on single-line P1 tasks). Its build reports `imports_resolved: 98` of `imports_updated: 2046`, and the `IMPORTS_FROM` edges it keeps target module strings rather than file nodes (fairness note, probe). The documented route to Python import resolution is the optional `[enrichment]` extra (Jedi), not the default; a row with it would be a second configuration, not a correction of this one. Recorded with the same care as a loss. | `results/2026-09-05-95eb4a00.json` f1_P4; `docs/competitive/fairness/code_review_graph.md` | RECORDED (a jcm lead; no issue) |
| CF-24 | **code-review-graph's token row is not like-for-like and favours the tool.** tokens_per_task 324.8 vs our 1,656 (ratio 0.196, meaningful), but the tool has no read-source tool: a P1/T answer is three search hits with name, path, lines and signature, and an agent reads the body with its own file tool, which is not the tool's output and is not charged (fairness note, disadvantage 1). In the first probe (one run, fairness note), four of the five T queries returned nothing (`search_mode: "none"`, "search covers names, paths and signatures, not source text"); a result file has no per-task rows, so the count is the probe's. Charged as returned. Item 4's report must carry the caveat on this row, and any `competitive-gap` drafted from it must name it; a `payload_shape` hypothesis without the caveat would be the loop overstating a competitor. | `results/2026-09-05-95eb4a00.json` tokens_per_task; `fairness/code_review_graph.md` | OPEN, caveat for item 4 |
| CF-25 | **The third peer matches the second on P1 and P2**: code-review-graph f1_P1 0.6667 and f1_P2 1.0 (`callers_of` on the bare name, then the disambiguation re-run it asks for, both charged) against our 0.3333 and 0 (CF-13). Two independent graphs now find the same-file caller our `find_references` misses; the P2 `coverage` candidate of CF-20 gains a second witness. Its `tools/list` is 7,694 tokens (30 tools, FastMCP's schema) against our 23,652 (ratio 0.325). Cold index 66.2 s median, spread 7.6 s, unstable under the 10% rule and under the CF-14 mount caveat; its README's "~10 seconds for a 500-file project" is a claim and is not compared. | `results/2026-09-05-95eb4a00.json` | OPEN, strengthens CF-20's P2 candidate |
| CF-26 | code-review-graph's MCP `serverInfo.version` reads `3.4.7`, FastMCP's own version, not the tool's 2.3.8: the same defect our server carried until v1.108.292 (CLAUDE.md `server.py`, `Server(..., version=__version__)`). The pin is the wheel hash and the adapter's `version()` reports the pin; `serverInfo` is never read as a version by any adapter. A fix list is a free defect probe in both directions (Standing lesson 08-22): worth a courteous upstream note from a human, never from this loop (principle 6, tool-surface discipline). | `tests/fixtures/competitive/code_review_graph_mcp.json` `server_info` | RECORDED, human may report upstream |
| CF-27 | **The MCP driver read a server's stderr only at exit**, so a server that logs every tool result to stderr (Serena, at INFO) filled the pipe on its first large result and every later call read as a hang; the second Serena probe timed out at the 20-minute session ceiling. `mcp_driver.py` reads both pipes on threads now and keeps a bounded stderr tail; `tests/test_competitive_mcp_driver.py` fails against the pre-change driver (a stub server writes 4 MB to stderr before answering; without the drain the call times out). A harness defect that could only have produced a `not_runnable` row or a timeout, never a number; the two earlier MCP rows were re-measured with the new driver in `results/2026-09-05-76e75398.json` (medians tokens / f1_P1 / f1_P2 / f1_P4: codebase_memory 980.6 / 0.6667 / 1 / 0.9825; code_review_graph 324.8 / 0.6667 / 1 / 0; every quality and token row's three runs agree) and their pinned image digests moved with it. | `benchmarks/competitive/sandbox/mcp_driver.py`; `tests/test_competitive_mcp_driver.py`; `fairness/serena.md` | CLOSED by PR 2d |
| CF-28 | **The LSP-backed tool answers every P1 and P2 task of the self set exactly**: serena f1_P1 1 (one `find_symbol` match per name, the definition) and f1_P2 1 (the same-file caller with its line) against our 0.3333 and 0 (`results/2026-09-05-76e75398.json`, three runs, both meaningful). Three independent tools now find the caller our `find_references` misses (CF-13, CF-20, CF-25); the P2 `coverage` candidate has its third witness and is the strongest item-5 candidate on the table. Its `tools/list` is 6,476 tokens (29 tools) against our 23,652 (ratio 0.274). P4 is NOT COMPARABLE for it (no documented importers route), not a zero. | `results/2026-09-05-76e75398.json`; `fairness/serena.md` | OPEN, strengthens CF-20's P2 candidate |
| CF-29 | **Two of the LSP-backed tool's rows carry caveats that cut the other way and must travel with them.** Tokens per task 11,783.6 vs our 1,655.9 (ratio 7.1, meaningful): the T tasks dominate it, because `search_for_pattern` over the alternation of a query's words returns every match, which is the tool's answer to that pattern and a harness choice of pattern (fairness note, disadvantage 3); per-task tokens in run 1 are T router 23,776, middleware 1,896, error 33,200, request 21,619, context 23,520 against P1 cache_put 234, validate_path 281, ProgressReporter 1,368. Per-call latency 8,693 ms median vs our 56 ms (ratio 156, unstable, not meaningful): the tool syncs file-system state before a symbolic call over the Windows bind mount (CF-14), and the first symbolic call of a session costs more than the later ones (fairness note, disadvantage 7: the uncharged configuration call does not absorb it, and the second session's uncharged call removed the server restart from the first body read, which the file shows); the Linux runner's rows decide how much is the mount. Item 4 must quote both rows with these caveats, and neither is a `competitive-gap` in our favour until the pinned corpora on the Linux runner say so. | `results/2026-09-05-76e75398.json`; `fairness/serena.md` | OPEN, caveats for item 4 |
| CF-30 | The LSP-backed tool's MCP `serverInfo.version` reads `1.28.1`, the MCP Python SDK's version, not the tool's 1.7.0: the second instance of CF-26's shape (a framework's version reported as the tool's) in two consecutive adapters, and the defect our own server carried until v1.108.292. No adapter reads `serverInfo` as a version; the pin is the wheel hash. Worth one human note upstream per tool (principle 6: never from the loop). | `tests/fixtures/competitive/serena_mcp.json` `server_info` | RECORDED, human may report upstream |
| CF-31 | **The knowledge-graph tool's quality rows on the self set**: codegraph f1_P1 0.5 (stable, meaningful) against our 0.3333; f1_P2 1 (stable, meaningful) against our 0, with 1 citation, the same-file caller (CF-32 on what the probe found); f1_P4 0.0541 (stable, meaningful) against our 0.4324, with 8 citations for a 29-file gold (CF-33). Tokens per task 3,119 vs our 1,655.9 (ratio 1.88, stable, meaningful; T tasks in run 1: router 5,469, middleware 5,310, error 5,058, request 5,042, context 5,870); calls per task 1.30 vs 3.40; per-call latency 4.5 ms vs our 49.8 ms (unstable, not meaningful), with its source reads on tmpfs where ours cross the bind mount (fairness note, advantages); index 1.82 s vs our 17.04 s (unstable, not meaningful, its count of files is its own); `tools/list` 1,293 tokens under the README's example allowlist against our 23,652 (ratio 0.055). Four tools now find the caller our `find_references` misses (CF-13, CF-20, CF-25, CF-28). | `results/2026-09-05-5fc783e9.json`; `fairness/codegraph.md` | OPEN, rows for item 4; strengthens CF-20's P2 candidate |
| CF-32 | **A probe over the wrong corpus produced a finding the pinned corpus does not support, and it reached a CHANGELOG draft before the recorded run corrected it.** The probe indexed the full working tree (1,022 files by the tool's count, tests included) and its `codegraph_callers(cache_put)` named a second caller, `_fill` in `tests/test_cache_hit_rate_basis.py`, which does call it; the draft note and CHANGELOG entry called the self P2 gold incomplete. The pinned corpus is the 277-file snapshot with no tests, the recorded run cites the same-file caller alone, and the gold is complete for the corpus it describes. Rule for the next adapter: a probe runs on the pinned corpus (`Corpus` from `tasks/self.json`), never on the checkout, before anything it finds is written down as a finding about the task set. | `fairness/codegraph.md` (probe section); `tasks/self.json` | CLOSED by PR 2e (the draft was corrected before commit of the rows) |
| CF-33 | **Two of the tool's rows are shaped by its output, and the shape travels with them.** Its file-dependents answer prints eight file names and a `+K more` count (a probe over the full 1,022-file checkout printed 83 for the P4 file; the pinned corpus's count is in a payload the result file does not keep), so P4 recall is capped at eight citations whatever its graph holds (f1_P4 0.0541 on a 29-file gold); the citations are the eight, never the count. Its primary tool elides lines it sent earlier in a session ("Already sent earlier in this conversation"), so a T task's token count depends on the tasks before it in a shared session; the adapter runs each T task in its own container, and any per-session token benchmark of this tool that does not is measuring an order. Its source reads run on the sandbox's tmpfs (the tool keeps its index inside the project root, which is read-only here), a faster path than the bind mount every other adapter reads through; the latency row carries that and the Linux runner's rows remove it. | `fairness/codegraph.md`; `adapters/codegraph.py` | OPEN, caveats for item 4 |
| CF-34 | **A one-tool default surface**: the tool lists a single MCP tool by default and keeps seven more callable but unlisted, re-enabled by an environment allowlist. The row's `tools/list` is the README's example allowlist (four tools; 1,293 tokens on the pinned corpus in the result file, 1523 in the fixture captured over the full tree); the default surface is 392 tokens for one tool (probe over the full tree, not a row) against our 23,652 on `full` and the Counter's byte-pinned front door. The same design decision the Counter made (one entry point, the catalog behind it), reached independently; the tool's README argues it from measured agent behaviour ("one strong tool steers agents better than a menu of narrower ones"), which is the argument our codex_surface benchmark could not resolve (CLAUDE.md, the NEGATIVE result). The schema weight moves with the corpus: the row's allowlist is 1,293 tokens on the 277-file corpus and the fixture's 1523 on the 1,022-file tree, because the primary tool's description is scaled to the number of indexed files (its shipped code says so); a schema row for this tool is a per-corpus figure. A second surface row per configuration is item 4's call. | `tests/fixtures/competitive/codegraph_mcp.json`; `fairness/codegraph.md` | RECORDED, item 4 |
| CF-35 | **The pre-written-cards tool's rows on the self set, deterministic path**: graft f1_P1 0.3333 (stable, not meaningful) against our 0.3333, with 5 citations (its ranked tool's default limit; the method itself is not among its five lexical hits, fairness note probe section, and the gold is file-level, so a sibling in the same file is the one hit); f1_P2 1 (stable, meaningful) against our 0, with 1 citation; f1_P4 0.4286 (stable, not meaningful) against our 0.4324, with 13 citations over 2 calls (CF-36). Tokens per task 1,481.5 vs our 1,655.9 (ratio 0.89, stable, meaningful; T tasks in run 1: router 2,245, middleware 994, error 755, request 1,527, context 1,687); calls per task 1.10 vs 3.40; per-call latency 933.2 ms vs our 52.5 ms (unstable, not meaningful; its per-query refresh stats the corpus over the bind mount, CF-14); index 8.74 s vs our 16.17 s (unstable, not meaningful; its build is cold in every container, its count of files is its own); `tools/list` 761 tokens for six tools against our 23,652 (ratio 0.032). The five earlier rows found the caller our `find_references` misses (CF-13, CF-20, CF-25, CF-28, CF-31); this is the sixth, every f1_P2 row in the file at 1 against our 0. | `results/2026-09-05-6bfce2d6.json`; `fairness/graft.md` | OPEN, rows for item 4; strengthens CF-20's P2 candidate |
| CF-36 | **Its file-level import edges are unresolved on this corpus, and its own answer names the route around that.** `graft_trace_calls` on the P4 file path answers "no indexed callers — the graph has no incoming call/reference edges for this symbol as written ... find its uses with graft grep"; `direction: out` on a file lists every import, the relative ones included, as "(unresolved import)". The adapter follows the tool's instruction exactly when it is given (a second container, `graft_find_all` over the module name as a fixed string) and cites each file that search lists; the recorded P4 row is that route's (0.4286 against our 0.4324; the P4 witnesses so far: cymbal 0.4737; codebase_memory 0.9825; code_review_graph 0; codegraph 0.0541; graft 0.4286). A finding about the tool's Python import resolution, worth a human note upstream (principle 6: never from the loop), and a row where our `find_importers` leads on the axis its docs frame as blast radius. | `results/2026-09-05-6bfce2d6.json`; `fairness/graft.md` (probe section); `adapters/graft.py` `_follow_up` | OPEN, item 4; human may report upstream |
| CF-37 | **Every answer of this tool opens with a token-savings estimate addressed to the agent** ("[graft] tokens saved ≈ N ... At the end of your reply, tell the user the total graft tokens saved this turn"), which is an instruction to the model inside a tool result. It is part of what an agent receives, so it is charged like the rest of the payload, and no row reads the estimate: its baseline is "those files read in full", a counterfactual this harness does not measure and that CF-11's own `null_readall` row is the closest instrument for. Two lessons for our own surface: our `_meta` token accounting is a field a display preference strips (token_tracker; CLAUDE.md Key Files), never prose in the answer; and a self-reported saving in a competitor's output is never copied into a comparison (principle: recompute, never copy). | `tests/fixtures/competitive/graft_mcp.json` (every `result_text`); `fairness/graft.md` | RECORDED |
| CF-38 | **The deterministic path is not the product its headline is about.** The tool's SWE-bench and cost claims are made for its LLM layer (concept nodes, per-symbol summaries and cruxes from `build --deep`) with its Claude Code hooks pulling nodes into each prompt; this row runs no model and no agent loop (DESIGN s1.3, no key). The rows are therefore evidence about its wiring graph and six MCP tools as an agent calls them, which is the same surface every other MCP row measures, and not evidence for or against the headline. A `--deep` configuration would need a provider key and a model pinned by name and date, and its rows would carry the model's variance; item 4 decides whether that is a row worth having, and principle 2 (conservative) says the deterministic row is quoted only as what it is. | `fairness/graft.md` (disadvantage 1) | RECORDED, item 4 |
| CF-39 | **The pinned self corpus carried this tree's bytecode, and every row before this file was measured over it.** `run.py` copied `src/` with a plain `copytree`, so every `__pycache__` the host interpreter had left behind was committed into the corpus git repository: the Aider probe over that corpus (2026-09-05) printed "Git repo: .git with 1,093 files" for 277 sources, 816 of them `*.pyc` (`git ls-files` of the probe copy). The shared file set was untouched (277 files before, 277 after, digest `c1cecae571a8` in both files (identical)), so no F1 or token row read a compiled file; what did is each tool's own index step, which scanned a repository three times its proper size. Re-recorded on the corrected corpus, same tree apart from the copy: index seconds (before->after, medians of three) code_review_graph 58.64->58.52; codebase_memory 5.69->6.10; codegraph 1.59->1.48; cymbal 1.99->1.75; graft 8.74->8.39; null_grep n/a->n/a; null_readall n/a->n/a; serena 8.11->3.57; ours 16.17->16.74; 7 of 7 index rows in the after file are unstable (a spread over 10% of its median), so a per-tool delta here is what two recordings show and not a settled cost; tokens per task code_review_graph 324.80->324.80; codebase_memory 980.60->980.60; codegraph 3,119->3,119; cymbal 880.90->881; graft 1,481.50->1,481.50; null_grep 118,038.40->118,038.40; null_readall 1,161,455->1,161,455; serena 11,783.56->11,783.56. A harness defect found by a competitor's banner (a free probe, the CLAUDE.md lesson), fixed by name and suffix and asserted over `git ls-files`; the rows before this file are not compared with the rows after on the index axis. | `results/2026-09-05-6bfce2d6.json` (before); `results/2026-09-05-cd9ad926.json` (after); `run.py` `copy_source_tree`; `tests/test_competitive_tier.py` | RECORDED, fixed |
| CF-40 | **The repo-map tool's row on the self set, the token axis only**: aider tokens per task 8,601.8 vs our 1,655.9 (ratio 5.19, stable, meaningful); per-task in run 1: router 8,575, middleware 8,579, error 8,574, request 8,570, context 8,711; every T answer in the three runs is between 8,570 and 8,711 tokens, the same command over the same corpus. That is the cost of the map the tool sends with every change request at its default budget (the banner line "Repo-map: using 4096 tokens, auto refresh", fixture), and it buys orientation no axis here scores (fairness note, disadvantage 1). Against the other token rows (cymbal 881; codebase_memory 980.6; code_review_graph 324.8; serena 11,783.6; codegraph 3,119; graft 1,481.5) it is the second-largest non-null competitor payload per task, and it is one text: calls per task 1 by construction; per-call latency 4,467.0 ms (stable, meaningful) is a process start plus the map build, not a query; index 5.87 s (unstable, not meaningful) is the first cold invocation; no F1 row (4 axes NOT COMPARABLE, by design). | `results/2026-09-05-6215a70c.json`; `fairness/aider.md`; `tests/fixtures/competitive/aider_map.json` | OPEN, row for item 4; the whitepaper's "complementary" claim is still untested (a cost is not a benefit) |
| CF-41 | **Its default map budget is not the number its docs give.** The docs say `--map-tokens` "defaults to 1k tokens"; `models.py`'s `get_repo_map_tokens` is `max_input_tokens / 8` clamped to 1024..4096, so for the model named here the tool's own banner reads "using 4096 tokens", and with no files in the chat the no-files multiplier (2) sits on top; the map measured 8,570..8,711 harness tokens. A user reading the docs would budget a quarter of what the default sends. Not a defect of the tool; a documented figure and a computed one that disagree, the same shape as our own tool-count literals (CLAUDE.md: never copy a figure) and one more reason every number here is read from a file. | `fairness/aider.md` ("Budget", probe section); the tagged source | RECORDED |
| CF-42 | **A CLI's stdout is not all payload, and the boundary has to be the tool's own.** `--show-repo-map` prints a human-facing announce block (version, repo size, budget line; here also two lines from a price-table fetch the sandbox blocks) before the text it sends the LLM, which starts at its repo-map preface. The adapter charges the map after that preface and keeps the block aside in a side file; the block is 257 harness tokens on the cold run (it carries two git-identity hints the warm runs do not) and 215 on the warm runs, from the fixture. Charging it would have added the sandbox's own failure lines to a competitor's cost. Rule for the remaining CLI rows: the payload boundary is the tool's documented output, and anything the sandbox adds is recorded, not charged. | `adapters/aider.py` `split_map`; `fairness/aider.md` ("Payload") | RECORDED, rule |
| CF-43 | **The embedding representative's rows on the self set**: cocoindex f1_P1 0 (stable, meaningful) against our 0.3333 (per task in run 1: cache_put 0 (5 cited), validate_path 0 (5 cited), ProgressReporter 0 (5 cited); the other P1 rows: cymbal 0.2606; codebase_memory 0.6667; code_review_graph 0.6667; serena 1; codegraph 0.5; graft 0.3333); tokens per task 1,295.5 vs our 1,655.9 (ratio 0.78, stable, meaningful; T tasks in run 1: router 1,157, middleware 1,061, error 1,448, request 1,267, context 1,194); calls per task 1 vs 3.40; per-call latency 432.2 ms vs our 49.1 ms (unstable, not meaningful; CF-45); index 191.39 s vs our 18.65 s (unstable, not meaningful; the three runs 187.5, 213.5, 191.4 s; its own file count 274 of 277 listed), the daemon's first start and the model load inside it by the tool's design; `tools/list` 397 tokens for one tool against our 23,652 (ratio 0.017); f1_P2, f1_P4, f1_P5 NOT COMPARABLE by scope (no documented references or dependents tool). | `results/2026-09-06-825d2cc9.json`; `fairness/cocoindex.md` | OPEN, rows for item 4; the set's eighth and last adapter |
| CF-44 | **A semantic search asked an identifier ranks the places that MENTION it above the place that DEFINES it.** `search("cache_put")` at the defaults returns five chunks of tool-name lists (in `config.py`, `briefing.py`), not `_State.cache_put`; the same for `validate_path` and `ProgressReporter` (fixture). The P1 row (0) is that behaviour scored against a file-level gold, and it is the harness's choice of task put to a tool built for a different question (fairness note, disadvantage 4); the T rows are the fairer test of what it does and carry no F1 by design. For our own surface the lesson is the one the whitepaper states: an embedding channel is a complement to a symbol index, not a substitute for it, and `search_symbols` runs the lexical channel first for exactly this shape of query. | `tests/fixtures/competitive/cocoindex_mcp.json` (calls `p1_*`); `fairness/cocoindex.md` (probe section) | RECORDED |
| CF-45 | **Its default `refresh_index=true` is most of every call.** In the probe, the same query cost 434 ms with the default refresh and 15 ms with `refresh_index=false` (fixture calls `p1_again`, `p1_norefresh`): the incremental index check before each answer, not the vector search, is the latency an agent waits for. Charged as the default an agent gets (the recorded per-call row is 432.2 ms); a tool that is asked to stay fresh on every call pays for it on every call, which is the trade our watcher makes once per file change instead (CLAUDE.md, `watcher.py`). | `tests/fixtures/competitive/cocoindex_mcp.json`; `fairness/cocoindex.md` ("Commands", disadvantage 3) | RECORDED |
| CF-46 | **The pinned three plus the self corpus fail the corpus check on (a) and (e), not on the four DESIGN s3.4 predicted.** `docs/competitive/evidence/corpus_check_old_set.json` (the CLI's verdict over self + express + fastapi + gin): 3 language(s) at or above 2% of the set's code files (go, javascript, python); the policy needs 4; python is 85% of the set's code files; the policy caps a language at 60%. It PASSES (b) (self@101aa9e1's `src/jcodemunch_mcp/parser/extractor.py`, 12,158 lines), (c) (fastapi/fastapi@a64dfbb, 3,127 files) and (d) (2 domains). DESIGN's arithmetic left out the self corpus and fastapi's size. With the additional set the nine pass, per the recorded header of `2026-09-06-0e3a1706.json`: languages counted java, javascript, python, typescript (kotlin 2%, under the floor); shares java 45%, typescript 40%, python 7%, javascript 6%; longest code file `.github/actions/deploy-docs-site/main.js` of angular/angular@468b65b at 65,433 lines (a bundled action, counted because it is a tracked `.js` file); largest corpus spring-projects/spring-framework@82a6b40 at 11,315 files (the shared file set: text files of `git ls-files`). The set has two over-10k corpora because with the Java one alone (e) failed in the session's exploratory checks (not recorded; the recorded verdicts are the two files named here). Deviation from DESIGN s3.3 recorded: criterion (d) reads each corpus's `domain` in `corpora.json` (the self corpus's is set by run.py), not `tasks.json`'s `description`, which the additional corpora do not have. | `docs/competitive/evidence/corpus_check_old_set.json`; `benchmarks/competitive/corpora.json`; `corpus_policy.json`; the verdict in every result header (`corpus_check`) | RECORDED; the self corpus and the pinned three stay for continuity (s3.4) |
| CF-47 | **DESIGN s4.2's independence rule ("nobody who wrote an adapter writes tasks for the category that adapter is weakest on") is not met, and cannot be by one agent.** Every adapter and every task file in this tier has one author. The mitigation is the one the tier can give: for lodash, requests and express the tasks are a third party's rules (sverklo-bench a0c3017c, CC-BY-4.0) reproduced by `tasks/from_sverklo.py`, with each hand-verified definition line re-verified against a definition regex at our SHA (a moved line refuses the generator); for nest, spring and angular the author chose WHICH public entry points are asked (`tasks/authored.py`, choices dated 2026-09-06, symbols any user of the framework looks up first) and the expected sets are computed by the same rules, never typed. The reviewer is told on every PR carrying a task file. A second author for the authored set is the human's to supply. | `benchmarks/competitive/tasks/authored.py` (header); `from_sverklo.py` (header) | OPEN, needs a human: a second author for tasks/nest.json, spring.json, angular.json |
| CF-48 | **Three of sverklo-bench's tasks do not exist at our SHAs and are dropped with the count, not guessed.** express `merge` (their P1 10) matches none of the six definition patterns under `lib/` or `index.js` at 1faf228 (it is a dependency import there); express `lib/router/index.js` (their P4 3) has no `require.*index` importer at 1faf228 (the router moved to the `router` package); lodash `fp/_convertBrowser.js` (their P4 2) has no `require.*_convertBrowser` importer at 4.17.21 (a build entry point). Each file's `dropped` list carries the reason. Express P1 lines are resolved by the definition regex at OUR SHA (theirs is 4.21.1), so the nine that remain are ours to defend, not theirs. | `tasks/express.json`, `tasks/lodash.json` (`dropped`) | RECORDED |
| CF-49 | **A sandbox timeout killed the docker CLIENT and left the container running, and the first full-set run died of it.** `sandbox.run` enforced its timeout with `subprocess.run(timeout=)`, which kills the `docker run` process on the host; the container it started kept running to completion. On 2026-09-06, run 1 of 3 over the nine corpora reached the last adapter (the embedding representative, 20-minute timeout, 8 GB cap): its spring container "timed out" and lived on (27 minutes when found), the runner started its angular container beside it (7 minutes, 2.6 GB), and the host's low-memory guard killed the runner process at roughly the 45-minute mark of the first run, discarding every row the ten adapters before it had produced. Two fixes in the harness, neither in a threshold: containers are named and a timeout runs `docker kill` on the name (`tests/test_competitive_checks.py` proves a timed-out container is gone, and that the pre-fix code leaks one); `run.py` writes a checkpoint of finished runs after each run (`results/checkpoint-<commit>.json`, removed when all runs finish; `--record` never reads it). The budget question DESIGN s9.2 asks (240 minutes for a full run) is still open: the first run had not finished at 45 minutes. | `benchmarks/competitive/sandbox.py` (`kill_container`); `run.py` | FIXED in this PR (harness only); the run-time budget is measured by the re-run |
| CF-50 | **The first recorded run over two of the additional corpora (the utility library and the HTTP client library; the self corpus beside them), eleven adapters, three runs, 2026-09-06-0e3a1706.json.** **lodash/lodash**: jcm f1_P1 0.34, f1_P2 0, f1_P4 0.8341, tokens per task 1,236.7; f1_P1 by tool: null_readall 0 (meaningful, stable), null_grep 0.0081 (meaningful, stable), cymbal 0.1032 (meaningful, stable), codebase_memory 0 (meaningful, stable), code_review_graph 0.4667 (meaningful, stable), serena n/a (not comparable), codegraph 0.45 (meaningful, stable), graft 0.29 (meaningful, stable), aider n/a (not comparable), cocoindex 0 (meaningful, stable); f1_P4 by tool: null_readall 0 (meaningful, stable), null_grep 0 (meaningful, stable), cymbal 0 (NOT COMPARABLE: tools_not_called), codebase_memory 0 (NOT COMPARABLE: tools_not_called), code_review_graph 0 (NOT COMPARABLE: tools_not_called), serena n/a (not comparable), codegraph 0.0556 (meaningful, stable), graft 0.6004 (meaningful, stable), aider n/a (not comparable), cocoindex n/a (not comparable); tokens per task by tool: null_readall 1,238,880 (meaningful, stable), null_grep 392,357.9 (meaningful, stable), cymbal 467.9 (meaningful, stable), codebase_memory 334.4 (meaningful, stable), code_review_graph 771.7 (meaningful, stable), serena 13,396 (meaningful, stable), codegraph 723.1 (meaningful, stable), graft 647.7 (meaningful, stable), aider 7,463 (meaningful, stable), cocoindex 1,692.5 (meaningful, stable) **psf/requests**: jcm f1_P1 0.3566, f1_P2 0, f1_P4 0.7532, tokens per task 1,386.6; f1_P1 by tool: null_readall 0.0001 (meaningful, stable), null_grep 0.0152 (meaningful, stable), cymbal 0.3492 (not meaningful, stable), codebase_memory 0 (meaningful, stable), code_review_graph 0.4667 (meaningful, stable), serena 0.6136 (meaningful, stable), codegraph 0.49 (meaningful, stable), graft 0.2966 (meaningful, stable), aider n/a (not comparable), cocoindex 0 (meaningful, stable); f1_P4 by tool: null_readall 0.0002 (meaningful, stable), null_grep 0 (meaningful, stable), cymbal 0.7253 (not meaningful, stable), codebase_memory 0 (NOT COMPARABLE: tools_not_called), code_review_graph 0 (NOT COMPARABLE: tools_not_called), serena n/a (not comparable), codegraph 0.4312 (meaningful, stable), graft 0.5048 (meaningful, stable), aider n/a (not comparable), cocoindex n/a (not comparable); tokens per task by tool: null_readall 2,396,539 (meaningful, stable), null_grep 31,701 (meaningful, stable), cymbal 449.1 (meaningful, stable), codebase_memory 343 (meaningful, stable), code_review_graph 908.8 (meaningful, stable), serena 2,398.2 (meaningful, stable), codegraph 847.8 (meaningful, stable), graft 874.7 (meaningful, stable), aider 8,261 (meaningful, stable), cocoindex 1,707.6 (meaningful, stable). P1 losses, meaningful and not flagged silent: code_review_graph on lodash/lodash (0.4667 vs 0.34); codegraph on lodash/lodash (0.45 vs 0.34); code_review_graph on psf/requests (0.4667 vs 0.3566); serena on psf/requests (0.6136 vs 0.3566); codegraph on psf/requests (0.49 vs 0.3566). P4: jcm ahead of every tool whose P4 row is comparable (null_readall on lodash/lodash, null_grep on lodash/lodash, codegraph on lodash/lodash, graft on lodash/lodash, null_readall on psf/requests, null_grep on psf/requests, codegraph on psf/requests, graft on psf/requests); the tools whose P4 (or other) rows the same file flags `tools_not_called` are NOT COMPARABLE there and are not counted as wins, the rule CF-52 applies to the symbol server. P2: jcm 0 on every corpus (CF-51); the non-zero P2 rows: null_readall 0.0005 on lodash/lodash, null_grep 0.1024 on lodash/lodash, cymbal 0.1109 on lodash/lodash, code_review_graph 0.0125 on lodash/lodash, codegraph 0.0158 on lodash/lodash, graft 0.0067 on lodash/lodash, null_readall 0.0019 on psf/requests, null_grep 0.028 on psf/requests, cymbal 0.0806 on psf/requests, serena 0.0346 on psf/requests, codegraph 0.0187 on psf/requests, graft 0.0282 on psf/requests; above the grep null's P2 on the same corpus: cymbal on lodash/lodash, cymbal on psf/requests, serena on psf/requests, graft on psf/requests. `tools_not_called` names 9 (tool, corpus, category) rows: jcodemunch P2 on lodash/lodash@f299b52; jcodemunch P2 on self@0e3a1706; cymbal P4 on lodash/lodash@f299b52; codebase_memory P2 on lodash/lodash@f299b52; codebase_memory P4 on lodash/lodash@f299b52; codebase_memory P4 on psf/requests@0e322af; code_review_graph P4 on lodash/lodash@f299b52; code_review_graph P4 on psf/requests@0e322af; code_review_graph P4 on self@0e3a1706; 2 of them ours. The file records no wall time for the run (a header field to add), so none is quoted. | `2026-09-06-0e3a1706.json`; `results/latest.md` | RECORDED; the rows are item 4's input; each `tools_not_called` row is a hypothesis for its adapter's next PR |
| CF-51 | **Our P2 row is 0 on every corpus because the adapter asks the wrong tool, and the tool says so in its own reply.** The jcodemunch worker answers P2 (reference finding: where is this name USED) with `find_references`, whose reply on lodash `map` is `reference_count 0` plus the tip the tool ships: "Tip: use identifiers=[...] to query multiple identifiers in one call. For usage-site matching beyond imports, also try search_text or check_references." `find_references` is the IMPORT-graph tool (who imports this identifier), and a single-file library has no importers of `map`; the third party's P2 gold is usage lines by word-grep, which `check_references`/`search_text` answer. So the 0 is a harness mapping defect and a real loss at once: any user who reaches for `find_references` for this question gets the same answer. Not fixed in this PR (an adapter change, one per PR; the mapping is our own side and must be argued in its fairness note, never tuned to the gold). The tools_not_called row for jcodemunch P2 is this. | `benchmarks/competitive/sandbox/jcm_worker.py` (P2 branch, `cite_references`); the P2 payloads in `2026-09-06-0e3a1706.json` run 1; `docs/competitive/fairness/jcodemunch.md` | FIXED 2026-09-06 (the mapping: `check_references` at its shipped defaults, argued in the note; the 20-file cap stays); MEASURED on this tree, self corpus, host run (`sandbox none`, 3 runs, result file dated 2026-09-07T00:26:04Z, jcm `4e56fd59` with `tree_dirty` True since the fix was in the working tree; in the session scratch, not recorded; the two files' `scorer_sha256` differ because the hash covers the adapters and the worker, and the worker is what changed; `score.py` is byte-identical between the two commits (git diff)): jcodemunch f1_P2 0.125 (spread 0) against 0 on the recorded run's self row; null_grep f1_P2 0.1818 on the same tasks (0.1818 recorded), so the grep null is still ahead of us on P2 here; tokens_per_task 1705.7 (recorded 1655.9) and calls_per_task 3.4 (recorded 3.4) on our row moved with the mapping. The next recorded run carries it on every corpus. The product-doc half (what steers a user to `find_references` for this question) is CF-63 |
| CF-52 | **The symbol-server adapter's image cannot answer a JavaScript corpus: its TypeScript language server needs `node`, and the image has none.** Every P task on lodash errored with "Error executing tool find_symbol: Exception: The language server manager is not initialized, indicating a problem during project initialisation." (`typescript: node` not found); on the Python corpus the same image answered (P1 0.6136, meaningful, ahead of ours). The rows are NOT COMPARABLE, not a loss for it, and a competitor at less than its documented recommended configuration is the fairness rule broken on our side. Fix in the next serena adapter PR: install node in the image and re-record. | `2026-09-06-0e3a1706.json` run 1, serena/lodash task errors; `sandbox/serena.Dockerfile` | OPEN: serena image needs node for TS/JS corpora |
| CF-53 | **The full set does not fit DESIGN s9.2's 240-minute budget on this workstation; the size of the miss is unmeasured.** The run over all nine corpora (11 adapters, 3 runs) was started three times on 2026-09-06 and finished none: twice the session's own background-task memory guard killed the runner while the host still had free memory (the WSL VM's footprint, not the harness; an observation of this session, no recorded log), and the third, detached from the session, was stopped by hand with the FIRST of three runs unfinished after roughly two hours (session observation). No result or checkpoint file exists for the full set, so no figure is quoted; what is recorded is that a run's wall time is not in the result header yet. Item 6's schedule is a monthly job on a runner, not a nightly; the recorded run of this PR (CF-50) is the self corpus plus the two third-party corpora. The two over-10k corpora have no recorded row. | this file; DESIGN s9.2 | OPEN: item 6 sizes the schedule from a measured full run on a runner; record wall time in the header |
| CF-54 | **The summary's variant rows (DESIGN s5.3: jcm's `counter` surface and `include_call_chain` reported as labelled variants under the default) have no producer yet.** The jcodemunch adapter runs one configuration, the default (D3). The reporting side is in place: an adapter that declares `variant_of = "jcodemunch"` is labelled `(variant of jcodemunch)` in every table and its rows are scored against the default like any other tool's. FIXED 2026-09-06 for the `counter` surface: `jcodemunch_counter` (adapters/jcodemunch.py `make_counter`, registered) is the same adapter with JCODEMUNCH_TOOL_SURFACE=counter set for the worker in both sandbox modes, so its `tools_list_tokens` row is the front door's weight and every other row of it is the default's repeatability; `findings.py::ours` keeps every variant out of the gap, watch and proposal drafts (our configuration is not a finding about the field). The `include_call_chain` variant is retired: the P2 call is `check_references` (CF-51), which has no such switch. MEASURED on this tree, self corpus, host run (`sandbox none`, 3 runs, result file dated 2026-09-07T00:41:11Z, jcm `c8126dff`, `tree_dirty` True; in the session scratch, not recorded): the variant's pin carries `variant_of: jcodemunch` and the summary labels its row `jcodemunch_counter (variant of jcodemunch)` (present in latest.md); tools_list_tokens 945 for the variant against 23652 for the default (delta 0.04, band 1182.6, meaningful); the other rows are the default's repeatability: tokens_per_task 1705.7 vs 1705.7 (equal), calls_per_task 3.4 vs 3.4 (equal), f1_P1 0.3333 vs 0.3333 (equal), f1_P2 0.125 vs 0.125 (equal), f1_P4 0.4324 vs 0.4324 (equal). No draft names the variant (findings.py::ours) | `benchmarks/competitive/run.py` (`render_md`, `pins[].variant_of`); `tests/test_competitive_trend.py` | OPEN: the jcodemunch adapter's next PR adds the `counter` variant |
| CF-55 | **Movement can be judged on the self corpus only from this PR on, and for the pinned corpora the earlier lines carry no band.** `history.jsonl`'s eleven earlier lines record medians only; a movement needs the CURRENT run's band, which `trend.line_from_result` writes from this PR on, so every line before it can serve as `previous`/`first` (medians give the deltas and the gap) but a run recorded from an old line as CURRENT would say `no band recorded` rather than invent one. The self corpus id carries the running commit; keys are normalised to `self` when read, or every self row is a `first run` forever (found on the first render of the section). The recorded summary's Movement section at 0e3a1706 shows every self row `unchanged` against the previous run and every lodash/requests row `first run`. | `benchmarks/competitive/trend.py`; `results/latest.md` (Movement) | RECORDED |
| CF-56 | **One recorded run over three corpora yields 88 `competitive-gap` drafts, and 7 of them are a NULL baseline ahead of us.** `findings.py` over `results/2026-09-06-0e3a1706.json` with an empty issue list (draft mode, nothing posted): by label {'competitive-gap': 88}; by axis calls_per_task 21, f1_P2 19, tools_list_tokens 18, tokens_per_task 15, f1_P1 9, latency_call_ms 4, f1_P4 2; by tool code_review_graph 14, codegraph 14, codebase_memory 12, graft 12, cymbal 10, serena 9, cocoindex 7, null_grep 4, null_readall 3, aider 3. The null rows are the ones to read first: null_readall on latency_call_ms over lodash/lodash@f299b52; null_readall on f1_P2 over lodash/lodash@f299b52; null_readall on f1_P2 over psf/requests@0e322af; null_grep on f1_P2 over self@0e3a1706; null_grep on f1_P2 over lodash/lodash@f299b52; null_grep on latency_call_ms over psf/requests@0e322af; null_grep on f1_P2 over psf/requests@0e322af. A grep null ahead of us on reference finding is CF-51's defect scored (our adapter asks the import-graph tool); the read-all null ahead on `calls_per_task` is that null's one call per task against our three, which is a shape of the axis (a tool that reads everything makes one call) and says nothing a user should act on. The volume is the rule's: s7.1 drafts every meaningful row on any corpus, so the count scales with corpora x tools x axes, and item 6's monthly job writes them to the ledger branch where a human approves or not. Two rules narrowed the set: a CLI's zero schema cost is not a gap (FIELD.md, `interface: cli`); a null is drafted like a competitor because a null ahead is the strongest signal the loop has. `competitive-idea` waits for item 6's feed; `competitive-watch` and `standard-proposal` need two consecutive recorded runs and produced nothing on this one. The `index_missing_files` hypothesis (our `files_indexed` under the corpus's code-file count) cannot fire on this file: the header records `code_files` only from PR 3c's commit on, and the first draft of the rule read a field the producer never writes (review round 1; a fixture had supplied the contract). | `benchmarks/competitive/findings.py`; the drafts in this session's scratch (not committed: drafts live on the ledger branch, s7.3) | RECORDED; the null rows are item 4/CF-51's input |
| CF-57 | **The scheduled jobs exist and none of them can act until a human flips two variables.** `competitive-run.yml` (weekly cron kept to the first Sunday by its first step, because cron cannot say "first"; `workflow_dispatch` with `reason` and `tool`), `competitive-feed.yml` (Sundays 04:00 UTC) and `competitive-post.yml` (daily 07:00 UTC) follow the inbound pattern byte for byte where it applies: the kill switch read with the App token before the gate and again before the first write, the budget row by workflow stem, the ledger branch the only push target, the audit record in the inbound vocabulary, artifacts by run id. `INBOUND_ENABLED` gates all three; `COMPETITIVE_POST_ENABLED` gates the post job as well and is absent, so nothing can post. The four `competitive-*`/`standard-proposal` labels do not exist; creating them is the human's (DESIGN s7.3, RUNBOOK section 10 when Phase 5 writes it). The App's ruleset confines it to `inbound-ledger`; the competitive files live under `competitive/` there and the sweep job's own paths are untouched. | `.github/workflows/competitive-*.yml`; `tests/test_competitive_workflows.py` | OPEN, needs a human: the labels, `COMPETITIVE_POST_ENABLED`, and a first dispatched run on a runner (CF-53's measurement) |
| CF-58 | **The feed reads registries, and for a registry pin the GitHub repository comes from the registry's own metadata, never from a guess.** A `github-release` pin names owner/repo in its package; a `pypi` or `npm` pin is read from the package registry's JSON (latest version, declared source URL), and only a GitHub URL declared there leads to a releases read. A registry with no declared source gets a version comparison and no title, so neither release rule can fire on it; that is recorded per tool in `feed.json` (`release_title: null`) rather than invented. The only competitor text kept is the release title, inside a fenced `data` block under POLICY 4.2's preamble read from the file; the body is matched for the axis words and discarded. 5 of the 8 set members are registry pins (counted from adapter.REGISTRY's pins by the round-1 generator; the first draft typed four). | `benchmarks/competitive/feed.py` (`source_repo`, `evaluate_tool`) | RECORDED |
| CF-59 | **`/competitive-compare` ran end to end against a ref, and its page, this row's figures included, come from `compare_ref.py`, not from anyone's typing.** Working tree at `4137064b` against `59d2e405`, adapters `cymbal`, `null_grep`, `null_readall` plus jcodemunch, 3 runs each side in the container (Filter: tool cymbal (plus the nulls and jcodemunch); --set none (self corpus only; corpus check recorded, not enforced).); wall 240 s current, 241 s ref; DIFFERENT scorer code on the two sides (the page says so before its first number); interpreters 3.12.4 (current) and 3.12.6 (ref), disclosed on the page. Rows 36: 9 jcm rows, 8 with both sides, 6 differing by exactly 0 and 0 moved past the current band; the other 27 rows classified as n/a 8, unchanged 19. Page: `docs/competitive/evidence/compare_dryrun_item7.md`. | `.claude/commands/competitive-compare.md`; `benchmarks/competitive/compare_ref.py --findings-row` | RECORDED; the full-set form is CF-53's measurement |
| CF-60 | **`compare_ref.py --findings-row` with no `--ref` renders UNKNOWN provenance as a mismatch.** `summary()` sets `same_scorer`/`same_python` to `None` when the ref side is absent (the command's documented "ref has no `run.py`" path); `provenance_lines` handles that case, `findings_row()` reads the `None` as false and prints `against \`None\``, `DIFFERENT scorer code` and `interpreters X (current) and None (ref)`: an UNKNOWN rendered as a mismatch, the STANDARD DoD 8 shape. Not reached by the dry run (it had a ref); no test covers the no-ref row. Review round 2 of PR 3e named it and placed it in a follow-up beside a test for the no-ref row. | `benchmarks/competitive/compare_ref.py` (`findings_row`) | OPEN: the next compare_ref change carries the tri-state and the no-ref test |
| CF-61 | **Two axes DESIGN s2 calls COMPARABLE are not measured, and a competitor's first argument on reading the summary would be that they are the ones they win.** `score.py`'s axes are tokens_per_task, calls_per_task, latency_call_ms, index_cold_seconds, tools_list_tokens and the F1 rows; no adapter implements a `reindex_one` step (the interface has `index` only), so 3(b) one-file reindex cost has no row, and image build seconds and size are in the adapter PRs' build logs, not in any result file, so criterion 6 has no row either. Written up by the Phase 4 skeptical-competitor pass (VERIFICATION.md s8). Measuring 3(b) needs an adapter method that edits one file of the corpus and re-runs the tool's incremental path where its docs have one (`full_reindex` label where they do not), charged the same way `index` is; measuring 6 needs `sandbox.build` to record build wall seconds and the image size into the pin record, which the D2 build already has in hand. DESIGN s2 marks both rows "designed, not measured" until then. | `benchmarks/competitive/score.py` (axes), `adapter.py` (interface), `docs/competitive/DESIGN.md` s2 | OPEN: two adapter-interface changes, each a PR of its own |
| CF-62 | **The fairness note is a file the reviewer reads, not a field a run records, so a result file cannot say which note it ran under.** DESIGN s10 named "a fairness-note field the reviewer diffs against the Dockerfile and the adapter's config dict"; `Pin` and `Adapter` carry no such member and no result header names the note or its digest. Today the note is `docs/competitive/fairness/<tool>.md`, named in each competitor adapter's module header (eight files; the nulls and the jcodemunch adapter have none), and the check is the reviewer's diff on the adapter's PR, which is what happened on every adapter PR. The gap: a later edit to a note leaves every earlier result file indistinguishable from one run under the new note. Measuring it means a `fairness_sha256` per pin in the result header, hashed from the note file at run time, the way `scorer_sha256` already hashes the scorer. Found by the Phase 4 pass (VERIFICATION.md s2); DESIGN s10 now names the file and the diff. | `benchmarks/competitive/adapter.py` (`Pin`), `run.py` (header), `docs/competitive/fairness/` | OPEN: one header field, the next run.py PR |
| CF-63 | **The tool description that steers a user to `find_references` for a usage-site question is the product's, and this tier does not edit product source.** `find_references`' description says it answers "where is this imported / re-exported?" and "does NOT exhaustively enumerate every call site"; `check_references`' says "is this identifier referenced anywhere: imports + file content". The names invert the reader's expectation (a user asking "where is X referenced" reaches for the tool named after references), which is how our own adapter got CF-51's 0. The global exploration policy shipped to users (the `CLAUDE.md` block `jcodemunch-mcp init` writes) maps "where is this name used" to `find_references` as well. Recorded for the product; the tier's own row now asks the tool whose description matches the question. | `src/jcodemunch_mcp/server.py` (the two descriptions), `cli/policy.py` (the policy block) | OPEN, product: a description or policy edit is a `/feature` on its own, with the core_compact ceiling in mind |
