# FINDINGS — what the workflow layer needed and did not have (2026-09-04)

Each entry names what a workflow needs from the harness, a script or Claude
Code, how the workflow degrades until it exists, and its status. Rule from
the brief: workflows do not modify the harness, CI or product source; they
log the gap here and degrade gracefully.

| ID | Finding | Where | Degradation | Status |
|---|---|---|---|---|
| W-1 | `scripts/surface_diff.py` diffs tool NAMES only. A description, argument or tier change is a surface change (STANDARD criterion 4; DoD 4) and is invisible to it, so `surface_guard` cannot see the change class that grew `core_compact` to within two tokens of its ceiling. | `scripts/surface_diff.py`, H3 | `/feature` step 7 dumps `_build_tools_list()` name→description JSON from both trees and diffs it into `evidence/surface_descriptions.md`; `dod_checklist.py` marks DoD 4 with "description diff not captured (W-1)" when the dump is absent; the reviewer counts an absent dump as unmet. | OPEN; a `--descriptions` flag on the script would close it |
| W-2 | `scripts/pr_bench_comment.py` renders base-vs-PR rows for the latency and token ids only; fidelity, replay, goldset, route and schema Floors have no base value outside `harness/results/latest.json`. | `scripts/pr_bench_comment.py` | `/benchmark-compare` step 4 renders the remaining rows from the two `latest.json` files; a value absent on either side prints `n/a`, never 0. | OPEN |
| W-3 | The harness has no stamp-file output for "this tier passed on exactly this tree". `--write-results` writes the TRACKED `harness/results/latest.json`, which a branch must not dirty, and records `env.commit`, not the working-tree identity. | `harness/__main__.py` | `.claude/hooks/run_full.py` wraps the full tier, writes `.claude/state/full-tier.json` with the tree id (HEAD tree + digest of the uncommitted diff) before and after the run, and invalidates the stamp if the tree moved during it. | OPEN; a `--stamp-file PATH` would remove the wrapper |
| W-4 | No machine-readable list of LOAD-BEARING test files; H2 parses `docs/harness/ARCHAEOLOGY.md`'s table rows by filename. | `docs/harness/ARCHAEOLOGY.md` | Acceptable: `tests/test_retirement_ledger.py` parses the same table, so the format is already load-bearing. | ACCEPTED |
| W-5 | Claude Code 2.1.260 has no commit or PR hook EVENT and a `PreToolUse` hook cannot see the commit message. "The commit message states the lesson" (DoD 11) is therefore the reviewer's check over `git log`, not H2's. | Claude Code | Documented substitution (DESIGN D4); H1/H4/H5 match on the Bash/PowerShell command text. A `git commit` typed by the human outside the agent is not hooked, and never was. | ACCEPTED |
| W-6 | Reviewer determinism is not controllable from a prompt. The fixed output format makes drift visible line by line; it cannot make it impossible. | `.claude/agents/reviewer.md` | Phase 4 runs the same diff twice in fresh contexts and diffs the verdicts; the record is in `VERIFICATION.md`. | MEASURED in Phase 4 |
| W-7 | The hook JSON `additionalContext` channel used for WARNINGS (exit 0) is shown to the model as context, not to the human in the transcript; a warning the model ignores is invisible to jjg. Blocks (exit 2) are shown to both. | `.claude/hooks/_common.py` | Every WARNING also names itself in the first line so it is greppable in the session log; a budget overrun is additionally written to `.claude/state/evidence/hook_warnings.log` (added 2026-09-04). | OPEN; a Claude Code option to surface `additionalContext` to the user would close it |
| W-8 | The project `settings.json` hooks apply to EVERY session in this directory, including the one building them: the first `gh pr create` smoke test blocked the very Bash call that carried the test payload, because H4 matched the command text. | `.claude/settings.json` | Tests drive the hooks through a Python runner with the payload built in code (`tests/test_workflow_hooks.py`), never through a shell line that contains the trigger verb. Documented in `VERIFICATION.md` §1. | ACCEPTED (it is the hook working) |
| W-9 | `dod_checklist.py` DoD 7 (background-behaviour disclosure) and DoD 8 (`*_basis` beside a rate) are regex heuristics over the diff (`threading.Thread`, `socket.socket`, `httpx.`, keys ending `_pct/_rate/_share`, `confidence`). A new spelling of either is invisible to them; the reviewer's prompt carries the property. | `.claude/hooks/dod_checklist.py` | The checklist row says which regex fired or did not; the reviewer re-verifies each row (prompt step 1). | ACCEPTED; `tests/test_standard_invariants.py` is the durable half for DoD 8 |
| W-10 | The `release` skill is now TRACKED here and COPIED at `C:\MCPs\.claude\skills\release\` for jdoc/jdata (DESIGN D2). Two copies drift; the suite copy has no test. | suite `.claude/` | The tracked copy carries a REVISED banner naming the suite copy; a suite-level rule ("edit the jcm copy, sync outward") is the next layer's. | OPEN |
| W-11 | H1 reads the INDEX to decide whether a commit touches code, and `git add -A && git commit` in one Bash line runs the hook BEFORE the add, so the index is empty and the first commit of this layer (095a80e, tests/ and .claude/ staged in the same line) passed in 0.2 s without the fast tier. | `.claude/hooks/pre_commit.py` | When the command text also contains `git add` or `commit -a`, everything modified or untracked counts as staged. Recorded because it is the vacuous-pass shape the layer exists to refuse; found by the commit taking 0.2 s. | FIXED 2026-09-04 |
| W-12 | Claude Code loads `.claude/agents/*.md` at session start: `reviewer` was not spawnable (`Agent type 'reviewer' not found`) in the session that created it, so the Phase 4 reviewer probes ran a `general-purpose` agent told to read and follow `reviewer.md`. Same isolation (fresh context, no implementer conversation), same prompt; the only difference is that the agent's tool list is the general one instead of the four the definition names. | Claude Code | Documented substitution for this session; every later session gets the real agent type. `/feature`, `/fix-issue` and `/review` name `reviewer`; if it is absent, the command says so and uses the substitution rather than skipping the review. Superseded within the hour: the `reviewer` type appeared in the Agent list after a few minutes without a restart. | CLOSED |
| W-13 | The first stamped full-tier run ended `ok=false, tree changed during the run`: pytest-cov writes `.coverage.<host>.<pid>` files at the repo ROOT (20 of them, not gitignored) and `tree_id()` hashed `git status --untracked-files=all`, so the harness's own footprint moved the identity it was measured against and NO run could ever stamp `ok`. The guard-sampled-after-the-work lesson (Standing lessons 08-24), reproduced inside the guard written to apply it. | `.claude/hooks/_common.py` | `tree_id()` ignores `.coverage*` and `.claude/state/`; the strays themselves are a harness footprint worth a `.gitignore` line (not this layer's edit). Editing a TRACKED doc while the tier runs still invalidates the stamp, and that is correct. | FIXED 2026-09-04 |
| W-14 | The reviewer probes found a HARNESS blind side: `harness/thresholds.py` announces a loosening only when `history` is non-empty, so the first-ever loosening of any Floor (every entry has `history: []` today) loads silently, and `guard_patterns`/`set_at.reason` keep naming the old value. Reported to `docs/harness/FINDINGS.md` F-20; the reviewer (BLOCK rule 3) is the only gate that saw it. | `harness/thresholds.py` | Not this layer's edit. The reviewer's DoD-12 check reads the diff, not the loader, so it does not depend on the fix. | REPORTED |
| W-15 | Second H1 slip: `printf > scripts/x.py && git add … && git commit` in one line. The W-11 fix read `git status` at hook time, and the file did not exist yet. The hook cannot see the future of the line it is asked about. | `.claude/hooks/pre_commit.py` | The rule is inverted: the tier is SKIPPED only when the hook can PROVE the line is docs-only (every segment is `cd`/`git add|commit|status|diff|rm|mv` and nothing under a code root is staged or, when adding, present); any other segment (a `printf`, a heredoc, `python -`) means unknown, and unknown runs the tier. Choosing the one that cannot skip a step over the faster one, per the brief. | FIXED 2026-09-04 |
| W-16 | The `/release` dry-run's recompute step found README's per-repo benchmark averages and ratios (express, fastapi, gin) disagreeing with the CI-captured `benchmarks/jcm_reference.json` (they match `benchmarks/rag_baseline_results.json`, a different run committed in the same `9b2123d`); the grand total agrees. `tests/test_provenance.py` is green, so the mirror ratchet does not cover those README rows. Not this layer's edit; the command refuses on it, which is what it is for. | `README.md`, `benchmarks/jcm_reference.json`, `tests/test_provenance.py` | `/release` step 4 refuses until the README rows are regenerated from the reference or the ratchet is widened to name which artifact each README figure mirrors. | REPORTED |
| W-17 | `git tag --list` (the read form) was refused by the deny guard and by `Bash(git tag*)` in settings, so `/release` step 2 and the `version-scheme` skill could not run as written. | `.claude/hooks/deny_guard.py`, `.claude/settings.json` | Deny only the creating forms (`v*`, `-a`, `-s`, `-f`, `-d`, `--force`, `--delete`, `--annotate`); the command and skill use `git for-each-ref refs/tags` and never the `tag` verb. | FIXED 2026-09-04 |
| W-18 | H1 CANNOT be exercised on a line that creates the file it commits: the probe `printf > scripts/x.py && git add … && git commit` ran the tier and the format check BEFORE the printf, both passed on a tree without the file, and the unformatted script was committed (second slip after W-15's fix, which only made the tier RUN). And the probe's cleanup (`git reset --hard HEAD~1` on the probe branch) discarded the session's uncommitted edits to five files, which had to be re-applied. | `.claude/hooks/pre_commit.py`; process | The hook now REFUSES any commit line whose segments are not all `cd`/`git add|commit|status|diff|rm|mv`/`echo`: file changes go in their own tool call, the commit in its own line, so what the hook checks is what the commit carries. Probes run on a branch with a CLEAN tree (commit first); never `reset --hard` over uncommitted work. | FIXED 2026-09-04 |
| W-19 | H1 and H4 matched their verb ANYWHERE in the Bash text, including prose inside a `python - <<'EOF'` heredoc: appending the W-18 row to FINDINGS.md was refused because the row's text names the commit line. The deny guard has the same property by design (an evasion through quoting must not work), so its false positives on prose stay; the two workflow hooks strip heredoc BODIES before matching. | `.claude/hooks/_common.py` (`strip_heredocs`) | Prose that must carry a denied verb goes through the Write tool, never a Bash heredoc (already the rule in `/triage-issue` step 6). | FIXED 2026-09-04 |
| W-20 | `python -m harness … --summary FILE` APPENDS. `run_full.py` wrote `evidence/full.md` four times; one run was a 1656 s outlier (`suite.full_seconds` FAIL, cause unknown, the .261 47-minute shape) and its FAIL row stayed in the file, so `dod_checklist.py` read the next, passing run as unmet on DoD 2. A summary that accumulates reads every past failure as present. | `harness/__main__.py` (`--summary` semantics), `.claude/hooks/run_full.py`, `pre_commit.py` | The wrappers unlink the summary before each run: one run, one file. A `--summary-truncate` on the harness would be cleaner; not this layer's edit. | FIXED 2026-09-04 |
| W-21 | The D5 stamp keyed on HEAD's tree plus the uncommitted diff, so COMMITTING the same content, or adding the CHANGELOG line, invalidated it: the `/feature` run had to run the full tier twice (211.8 s, then 220.0 s) for one change. | `.claude/hooks/_common.py` `tree_id()` | The identity now covers what the verdict depends on (`src`, `tests`, `harness`, `scripts`, `benchmarks`, `pyproject.toml`, `uv.lock`, `.github`): tracked blobs, their diff, their untracked files. A docs-only commit keeps the stamp; a doc edit that flips a doc-reading test is the PR gate's to catch. `/feature` commits BEFORE the full tier and records in a second commit. | FIXED 2026-09-04 |
| W-22 | A feature branch cut from `origin/main` before the layer merges has no `.claude/commands/`, hooks or `docs/workflows/` (main still ignores `.claude/`), so the command deleted its own machinery on step 1; the agent restored the files as ignored working-tree copies to continue. | branch state until PR #591 merges | Step 1 refuses when the branch has no `.claude/commands/`. Moot after #591. | FIXED (refusal); RESOLVES on merge |
| W-23 | `/feature` step 8 reviewed `git diff origin/main...HEAD` before step 10 committed, so the reviewer's diff was empty; `dod_checklist.py` likewise could not see an untracked new test file in its `changed` list. | `.claude/commands/feature.md` | Commit at step 5; review the committed diff plus any uncommitted remainder; the checklist also lists untracked files under the code roots. | FIXED 2026-09-04 |
| W-24 | Step 7 required `bench_table.md` even when no bench ran, so the table read base == PR, delta 0, by construction: a column that can only say "unchanged" is not evidence. `python scripts/surface_diff.py` without `uv run` cannot import the server. | `.claude/commands/feature.md` | The table is written only when the bench tier ran; otherwise one line says why not. Every script call is `uv run python`. | FIXED 2026-09-04 |
| W-25 | Three readings of DoD 3: STANDARD.md item 3 reads unconditional; `scripts/dod_changelog.py` requires an entry only for a `src/` change; the `no-changelog` label text names a `src/` change that deliberately has none. The `/feature` run wrote an entry for a `scripts/`-only change on the reviewer's first pass and could not honour the "label it" instruction without misusing the label. | `docs/standard/STANDARD.md`, `scripts/dod_changelog.py`, label | The command states the script's rule (entry required for `src/`; optional otherwise; the label is for the `src/`-without-entry case). Reconciling STANDARD.md's wording is a standard edit, not this layer's. | REPORTED |
| W-26 | A new test file has no `docs/harness/ARCHAEOLOGY.md` row and the command did not say whether one is owed. The survey is dated (2026-09-03) and `tests/test_retirement_ledger.py` guards DELETIONS against it, so a new file needs no row; the reviewer prompt says so now. | `.claude/agents/reviewer.md`, `feature.md` step 8 | Stated in both. A living roster would be the harness's (F-series), not this layer's. | ACCEPTED |
| W-27 | The `/feature` run's first review returned REQUEST CHANGES for a copied figure in SPEC.md ("two tokens" from CLAUDE.md), two dead wrappers, and an unformatted test file; all four were fixed before the second pass returned APPROVE. Recorded because it is the layer working as designed: the copied-number scan fired on prose the implementer wrote from memory. | — | — | EVIDENCE |
| W-28 | `scripts/pr_bench_comment.py --base-ref` takes its base values from the artifact COMMITTED on the ref (`git show <ref>:harness/results/latest.json`, dated 2026-09-03), not from a run of the ref; `/benchmark-compare` step 3 spent 51 s producing a fresh ref run the script never read, and the two ref columns disagreed in every row (`search_symbols` delta +0.6 committed vs -2.5 fresh). Correct for the PR gate (the committed artifact IS main's record), wrong for a same-box comparison. | `scripts/pr_bench_comment.py`, `benchmark-compare.md` step 4 | The command renders one table from the two fresh JSONs through `harness.thresholds.load()`; the script is not used there. A `--base-results FILE` on the script would let it render both. | FIXED in the command; script flag OPEN |
| W-29 | The bench tier's `--write-results` rewrites two tracked files (`latest.json` AND `self_latency.json`); the command restored one, leaving the tree dirty, and its on-main recording branch could never be satisfied. The worktree's `uv sync` chose 3.13 against the working tree's 3.12, so the two sides ran on different interpreters. `harness threshold <id>` prints the bare value, not the comparator. `--summary` appends. | `harness/__main__.py`, `benchmark-compare.md` | Command names both files, pins `--python` to the working tree's version, reads comparator and criterion through `harness.thresholds.load()`, and deletes its five evidence files by name. | FIXED 2026-09-04 |
| W-30 | Claude Code runs a project hook with `$CLAUDE_PROJECT_DIR` fixed to the session's project, so `_common.REPO` pointed at the main checkout while the `/fix-issue` probe worked in a `git worktree`: pre_commit inspected the main checkout's (empty) index and let the commit that REINTRODUCED #572 through silently; test_edit_guard and surface_guard saw worktree paths as outside the repo. The explicit scripts (`run_full.py`, `dod_checklist.py`) resolved from their own file and were right. | `.claude/hooks/_common.py`, `settings.json` | `read_hook_input()` rebinds REPO/STATE/EVIDENCE to `git rev-parse --show-toplevel` of the payload's `cwd` when that is a checkout carrying `.claude/hooks/`, and patches the hook module's imported copies. `tests/test_workflow_hooks.py::test_hooks_follow_the_session_cwd_into_a_worktree` is the red arm. | FIXED 2026-09-04 |
| W-31 | `dod_checklist.py` row 3 read `met` on the `/fix-issue` probe with `src/` MODIFIED and no CHANGELOG entry: `scripts/dod_changelog.py` diffs `base...HEAD`, so an uncommitted `src/` edit is invisible to it, and the command ran the checklist before committing. | `.claude/hooks/dod_checklist.py`, `fix-issue.md` order | Row 3 is `unmet` ("commit first") while `src/` has uncommitted changes; `/fix-issue` writes the CHANGELOG entry and commits BEFORE the tiers and the checklist, like `/feature` after W-21. | FIXED 2026-09-04 |
| W-32 | A LOAD-BEARING guard that imports a PRIVATE helper by name (`tests/test_result_cache_isolation.py` imports `_isolate`) fails as ONE collection ERROR when the defect removes the helper, and a plain `pytest` run aborts the session (`Interrupted: 1 error during collection`) before any other test, including the new reproduction, runs. The row's promised N failures collapse to an import error. | harness (ARCHAEOLOGY row 315), `fix-issue.md` step 3 | Commands pass `--continue-on-collection-errors` on the red run and treat a collection ERROR as evidence. Reported to `docs/harness/FINDINGS.md` F-21: a guard should exercise the BEHAVIOUR through the public entry points, not import the fix's helper. | FIXED (commands); REPORTED (harness) |
| W-33 | `/fix-issue`'s reviewer inputs 6 and 7 (CHANGELOG diff, checklist) were produced by step 8, after the review in step 7; `evidence/surface.md` was produced by nothing the command named. | `fix-issue.md` | Reordered: CHANGELOG at step 5, checklist inside step 6, review at 7 with every input existing. | FIXED 2026-09-04 |
| W-34 | Five `run_init(yes=True, no_backup=True)` tests in `tests/test_init.py` left `_settings_json_path` unredirected, so the FULL TIER, run inside a git worktree for the `/fix-issue` probe, ran `install_enforcement_hooks` against the developer's real `~/.claude/settings.json`; `_converge_rule` rewrote all six jcm hook commands to `shutil.which("jcodemunch-mcp")`, i.e. the worktree's `.venv`, which was deleted at the end of the run, and the next Claude Code session start failed with `No such file` on every product hook. ⚠ This row first blamed `uv sync`; nothing in the install path runs on a sync (`install_enforcement_hooks` has one caller, `run_init`) and the June-dated `settings.json.bak` beside a September `settings.json` is the `no_backup=True` fingerprint. Practice 8 (#437) in a new costume: that guard is written against `load_config` by name. | `tests/test_init.py`, `tests/conftest.py`, the two worktree commands | `tests/conftest.py::_isolate_claude_home` (autouse) redirects the four home-derived `cli.init` path helpers per test and fails any test that changes the real `settings.json` or `CLAUDE.md`; `tests/test_init_home_isolation.py` reruns the offenders in a subprocess whose home it owns. The hook-path check stays in the two worktree commands as belt and braces. | FIXED 2026-09-04 (hand-repaired the same day) |
| W-35 | `dod_checklist.py` keys DoD rows 1 and 3 on a change under `src/`, so a test-only fix with a red test in `evidence/red.txt` and a CHANGELOG entry reads `n.a. | no change under src/` on both rows; the reviewer re-derived them as `met`. Found by the W-34 review. | `.claude/hooks/dod_checklist.py` rows 1, 3 | Row 1 should read `met` when `red.txt` ends `EXIT=1` and `green.txt` `EXIT=0` regardless of `src/`; row 3 should read the CHANGELOG diff whenever `tests/` or `src/` changed. | OPEN |
| W-36 | `tests/conftest.py::_isolate_claude_home` keys its per-test directory on `request.node.name[:60]`, not `nodeid`; two same-named tests in different files on one xdist worker would share it (`exist_ok=True`). No such pair exists among the `run_init` callers today. | `tests/conftest.py` | Use `request.node.nodeid`; one-token change, deferred so the W-34 PR did not re-run its tier a third time. | OPEN |
| W-37 | `dod_checklist.py --base-ref origin/main` is what every command runs, so on a STACKED branch the checklist's diff rows describe `main..HEAD`, the whole stack, not this PR's diff against its base; the reviewer of competitive PR 3b re-verified the `src/` rows by hand against the stacked base (2026-09-06). The flag already takes any ref; the commands pass `origin/main` unconditionally. | pass the PR's base ref when the branch is stacked (`--base-ref <base branch>`), and have the commands derive it from the PR's base when one exists | OPEN, workflows layer |
| W-38 | `dod_checklist.py` row 1 reads `n.a.: no change under src/, harness/ or scripts/` for a PR that adds a module under `benchmarks/competitive/` with its test and red/green evidence (competitive PR 3c, 2026-09-06); the reviewer graded row 1 from `red.txt`/`green.txt` directly. W-35's shape one directory over: the n.a. rule under-scopes a change whose code root is `benchmarks/`. | `.claude/hooks/dod_checklist.py` row 1 | row 1 reads `met` when red.txt ends `EXIT=1`/`EXIT=2` and green.txt `EXIT=0`, whatever the path; `benchmarks/` and `tests/` count as code roots for rows 1 and 3 | OPEN, workflows layer |
| W-39 | `pre_commit.py` skipped the fast tier on a docs-only commit ("docs commits are free"), so competitive Phase 5, which edited CLAUDE.md and nothing under a code root, reached the reviewer with `fast.md` from a tree two commits older, while the one Floor the diff moved, `claude_md.max_chars`, went unmeasured on HEAD (reviewer round 1, 2026-09-06; the tier run by hand passed at 139,039 of 140,000). CLAUDE.md is a docs file whose CONTENT a Floor reads, and it is not the only one. ⚠ A by-hand `harness fast --summary` APPENDS to `fast.md` (the hook unlinks first, W-20), so the reviewer found two PASS blocks with two different sizes in one file. | `.claude/hooks/pre_commit.py` (`tier_needed`, `FLOOR_INPUTS`) | the property, not the spelling (review round 1: the first draft named CLAUDE.md alone, and the frozen `benchmarks/` artifacts the fidelity, schema and goldset Floors read were the same class one directory over): a commit staging any file a Floor's Method reads runs the tier; `tests/test_workflow_hooks.py` reads `harness/__main__.py`'s path literals and fails when one is not covered. A by-hand tier run before a docs-only PR must unlink `fast.md` first (the harness `--summary` appends). | FIXED 2026-09-06; the unlink is a habit until `--summary` gains a truncate mode |

