# ARCHAEOLOGY — why every test and benchmark in this tree exists

Read-only survey, 2026-09-03, branch `harness/source-of-truth` at `f68a728`
(v1.108.316 plus the standard docs). Box: Windows 11, Python 3.12.4, 24 CPUs.
Method per file: module docstring, issue/version references inside the file,
`git log --follow` first three subjects. Nothing was modified, moved or retired.

## 0. Totals

| Fact | Value | Source |
|---|---|---|
| Test files | 491 (487 in `tests/`, 4 in `tests/encoding/`) | `find tests -name 'test_*.py'` |
| Tests collected | 9,174 | `pytest --collect-only -q` (34.93 s) |
| Full suite, this box, `-n auto --dist loadfile` | **9,155 passed, 19 skipped, 0 failed, 188.43 s**, EXIT=0 | `scratchpad/suite_before.log`, 2026-09-03 |
| Documented serial runtime | 599 s (2026-08-16, 7,859 tests); CI `-n 4` with coverage 258 s locally | `pyproject.toml:178-179`, commit 9f6c8d3 |
| CI wall clock, last 10 runs | 9m44s to 15m59s | `gh run list --workflow test.yml` |
| Skips | 19 here vs 13 recorded in CLAUDE.md for `PYTHONPATH=src python -m pytest`; the 6 extra are UNKNOWN (uv-run env vs bare python; to be reconciled in Phase 4) | this run |
| Skip mechanisms in `tests/` | `skipif` 39, `importorskip` 31, `pytest.skip` 80, `xfail` 0, no flaky/rerun plugin | grep |
| Network | no test reaches the network (`httpx` uses an in-process ASGI transport); `test_stdio_guard.py`, `test_surface_cli.py` spawn subprocesses | grep, slice notes |
| Machine-specific literals | `test_watcher_serve.py` mocks a `C:\Windows\System32` path; `test_v1_108_84.py` uses a reporter's `/Users/...` path as INPUT; `test_render_diagram_integration.py` skips without `MMD_VIEWER_PATH` | slice notes |
| Benchmark suite runtime | no-network subset ~28 s + 18 s pin tests; token benchmark 39 s on CI; oracles/comparators UNKNOWN (see `docs/standard/DISCOVERY.md` §10) | measured 2026-09-03 |

### Classification of the 491 test files

| Class | Files |
|---|---|
| LOAD-BEARING | 485 |
| STRUCTURAL | 3 (`tests/test_context_providers.py`, `tests/test_deadcode_eval_harness.py`, `tests/test_render_diagram_integration.py`) |
| REDUNDANT | 0 (candidates were found but none is FULLY covered by a named file; see notes on `test_docs_config_parity.py`/`test_configuration_md_defaults.py`, `test_v1_108_105.py`/`test_v1_108_108.py`, `test_v1_108_230.py`/`test_v1_108_231.py`, `test_v1_108_56.py`/`test_result_cache_isolation.py`) |
| STALE | 0 |
| UNCLEAR | 3 (`tests/test_agent_selector.py`, `tests/test_config_watcher_keys.py`, `tests/test_groq_voice.py`) — **stay exactly as they are; listed for human review in §5** |

⚠ The near-total LOAD-BEARING share is not a lazy default. 202 of the 491
files are `test_v1_108_N.py` release pins or carry an issue number in their
docstring, and the project's own CLAUDE.md records the practice (9) that
turned an old red test into evidence rather than a fix target. When a test's
reason could not be found it was marked UNCLEAR, not LOAD-BEARING.

### Scars worth naming (odd inputs that are wounds)

- `tests/test_server.py` pins `len(tools) == 90` as a literal while the live `full` profile serves 91; its docstring says `test_summarizer` is disabled by default. A literal count is a second copy of the surface; flagged for the coverage map, not changed.
- `tests/test_suggest_queries.py` empty-index case is conditional and can pass vacuously.
- `tests/test_nesting_depth_channels.py` scans for a stray 0x08 BACKSPACE byte because one once replaced a backslash in a regex and compiled.
- `tests/test_v1_108_84.py` carries a real reporter's macOS path as the INPUT; the path is the defect's shape (#357), not an environment leak.
- `tests/test_watcher_serve.py` uses a literal Windows system path as a MOCKED fixture because #447 wrote a real file into a real system directory once.
- `tests/test_schema_baseline_transcription.py` fails if any literal from `schema_baseline.json` appears in `tests/` or `benchmarks/` — a guard against the number being hand-typed.
- `tests/test_optional_dep_skips_are_visible.py` forbids module-scope `importorskip` so optional-dependency tests still COLLECT and show as skips, after 105 tests vanished silently at exit 0 (2026-08-28).
- `tests/test_ci_env_reproduce_command.py` binds a command in CLAUDE.md to the workflow's install line, because the gitignored release skill drifted from CI.

## 1. Test files (491 rows)

Columns: path | asserts | why (commit / issue) | STANDARD criterion | class | notes.
Criterion codes: 1 correctness, 2 token reduction, 3 freshness/caches/absence, 4 tool surface, 5 latency, 6 install/config/clients, 7 release stability, 8 security, 9 observability honesty, 10 language breadth, N1-N7 non-functional.

| path | asserts | why | crit | class | notes |
|---|---|---|---|---|---|
| tests/encoding/test_dispatcher.py | encode_response auto/compact/json gating, generic round-trip via decoder, and that an error body survives a schema-encoded tool. | 2f6979c "release: v1.108.30 — schema encoders no longer swallow error responses"; 044e54a (#256); in-file comment "regression guard for the bug where the error text vanished into a blank 0 results envelope" | 1 | LOAD-BEARING | Error-swallowing guard is the load-bearing part; rest is MUNCH format contract. |
| tests/encoding/test_format.py | MUNCH primitives (header/scalars/table/legends) round-trip, incl. literal `@digit` in a legend and blank lines inside a table cell. | d0a3e14 "release: v1.108.266 - a blank line inside a table cell no longer truncates it"; 04f8ceb v1.108.98 "MUNCH legend @digit corruption" | 1 | LOAD-BEARING | Two named past corruption bugs pinned (@digit legend, blank-line cell). |
| tests/encoding/test_generic_hardened.py | Generic gen1 fallback encoder preserves key names, column types, nested list flattening, `_meta` passthrough, and fails open on odd shapes. | b296d62 "fix(encoding): escape newlines in quoted scalars + schema separators (F1, F11, F5)"; 1e983be "Harden MUNCH generic fallback encoder (phase 3, v1.56.0)" | 1 | LOAD-BEARING | Round-trip on synthetic shapes mirroring tools without custom encoders. |
| tests/encoding/test_tier1_roundtrip.py | Every tier-1 custom encoder round-trips a representative real-shaped response, including batch `find_importers`/`get_file_outline` and a `search_ast` fixture built from the tool's actual output. | 4fa46a1 "fix: search_ast encoded to an empty table for every language and preset (#553)"; ab127d9 (#319); docstring "fixture was FABRICATED IN THE SCHEMA'S IMAGE until #553" | 1 | LOAD-BEARING | #319 batch data loss + #553 empty-table; fixture-authored-from-schema lesson lives here. |
| tests/test_absence_wiring_guard.py | Every `build_verdict` call site passes `index_changed` and every index-aware wrapper can actually reach `absent` (AST scan of source). | 23346c8 "release: v1.108.170 — the file and symbol tools can see a rebuild too"; docstring cites v1.108.168/.169 misses | 3 | LOAD-BEARING | AST ratchet over `src/`; explicit non-vacuity test; KNOWN_GAP set now empty. |
| tests/test_adaptive_languages.py | `languages_adaptive` config parsing, detected-subset application, and that the local config file is not rewritten when unchanged. | bcf508b "feat: adaptive language config + review-feedback fixes" | 6 | LOAD-BEARING | Feature tests; no issue number; writes tmp_path only. |
| tests/test_agent_selector.py | ComplexityScorer/ModelRouter scoring, clamping, threshold boundaries, and tier-to-model resolution. | 57b7455 "feat: add Agent Selector — complexity-based model routing (v1)" | NONE | UNCLEAR | Feature unit tests for `agent_selector`; no issue or downstream failure named; 39 tests. |
| tests/test_al.py | AL (Business Central) fixture extracts tables/codeunits/enums/procedures with correct kinds and ids. | 68e73ab "Add Microsoft Business Center AL parsing (#110)" | 10 | LOAD-BEARING | Fixture-driven (`fixtures/al/sample.al`), 61 tests. |
| tests/test_analyze_perf_totals.py | `_diff_baseline` refuses a latency delta when the baseline never measured latency, `_rank_by_total` reports where wall-clock went, and `_percentile` is deleted not wrapped. | a79fd0c "release: v1.108.309 — a mean hides the tail, and a default invents a comparison" | 9 | LOAD-BEARING | Reads every baseline OFF DISK under `benchmarks/token_baselines`; source-text ratchet for `latency_bucket` / no `percentile` name. |
| tests/test_anthropic_optional.py | Summarizer falls back cleanly when no API keys are set and when `anthropic` cannot be imported. | 4e40d24 "feat: make anthropic a truly optional dependency (#13)" | 6 | LOAD-BEARING | Patches `sys.modules["anthropic"]=None` and clears env; no network. |
| tests/test_architecture_metrics.py | Gini math, fan-in hub as top concentrator, Lakos depth, cycle-based modularity, and byte mass counted once per file. | 542e12c "release: v1.108.291 — counting each byte of source once"; 4d8a809 v1.108.113 | 1 | LOAD-BEARING | v1.108.291 Gini double-count (competitor-probe finding) pinned here. |
| tests/test_architecture_tools.py | `get_dependency_cycles`/`get_coupling_metrics`/`get_layer_violations` on linear, cyclic and layered synthetic repos. | 79cfd06 "feat: v1.15 — Architecture Intelligence (dependency cycles, coupling metrics, layer violations)" | 1 | LOAD-BEARING | Feature tests, 36 cases; indexes tmp repos. |
| tests/test_arrow_functions.py | JS/TS `const x = () =>` and function expressions index as functions with stable ids; non-function assignments do not. | b6c5490 "feat: add arrow function variable support for JS/TS" | 10 | LOAD-BEARING | Inline snippets only. |
| tests/test_asm_language.py | WLA-DX/NASM/GAS/CA65 labels, macros, sections, structs and constants are extracted; local labels excluded. | 6f7750e "feat: add assembly language support (WLA-DX, NASM, GAS, CA65)" | 10 | LOAD-BEARING | 27 tests over inline assembly sources. |
| tests/test_assemble_task_context.py | `_classify_intent` keyword routing and the orchestrator's assembled context shape over a tmp repo. | fd00980 "release: v1.105.0 — assemble_task_context (F-15 task-aware orchestrator)"; 2ff57a8 (#291) | 1 | LOAD-BEARING | #291 unloadable-index truthfulness touched this file. |
| tests/test_astro.py | Astro registry presence, BOM/CRLF frontmatter line offsets, comment-masked template ids, multi-script TS inference, and synthetic template-component imports. | d1aa0e5 "feat(parser): enhance Astro support with frontmatter parsing, component extraction, and regression tests" | 10 | LOAD-BEARING | Fixture dir `fixtures/astro`; self-described regression tests. |
| tests/test_audit_agent_config.py | Token estimate, symbol/file ref extraction, bloat/dead-path/redundancy/scope-leak/stale-symbol checks, and file discovery for agent config audits. | 7699868 "feat: add audit_agent_config tool -- token waste audit for agent config files"; b524f7e revert of `.claude/rules` discovery | 4 | LOAD-BEARING | Uses `~/.claude/CLAUDE.md` as a literal path STRING in mocked fixtures (not read); 34 tests. |
| tests/test_benchmark_grep_baseline.py | The grep-then-read baseline is measured live (not a constant), shares one reader with read-all, uses `rg -l` cost, reads at most N files, ranks deterministically by match count. | d7f8d22 "release: v1.108.233 - a baseline an agent would actually recognise"; 9661e0b "test: ten skipped modules that hid 209 tests between them" | 2 | LOAD-BEARING | `skipif` on tiktoken via `find_spec` (deliberately NOT importorskip); source-text ratchet over `run_benchmark.py`; N7-relevant skip. |
| tests/test_benchmark_reference.py | `jcm_reference.json` exists, is pinned to upstream commits and a complete corpus, never provisional; harnesses carry no `JCODEMUNCH_*` constants and the size-proportional estimator is absent BY NAME. | c01dd24 "release: v1.108.222 - the benchmark can now be re-run by someone who is not us"; ddc9504 "the determinism gate compares retrieval, not the clock" | N4 | LOAD-BEARING | AST ratchet over `benchmarks/harness/*.py`; guards Practice 4 (four-month stale constant). Also maps to 2. |
| tests/test_blade.py | Blade fixture extracts @extends/@section/components and is not mistaken for plain PHP. | cb57224 "Add Laravel Blade template support (.blade.php) — closes #47" | 10 | LOAD-BEARING | Fixture-driven; #47. |
| tests/test_blast_radius.py | `_bfs_importers` depth bucketing/cycle safety, risk fields always present, and `include_source` snippet extraction. | b7f28be "feat: add include_source flag to get_blast_radius for fix-ready context (#221)"; 62f6164 v1.11.13-14 | 1 | LOAD-BEARING | #221. |
| tests/test_blast_radius_package_granular_verdict.py | Go package-granular imports yield a withheld (not zero) risk score with a verdict naming the unresolved edge; Python resolvable imports keep a real score. | ed28e18 "release: v1.108.245 - a blast radius of zero now says whether it was measured (#415)" | 9 | LOAD-BEARING | #415; controls included so the fix cannot refuse everything. Also 3 (absence claim). |
| tests/test_bm25_cache_single_flight.py | The BM25 corpus cache publishes its readiness sentinel last, concurrent cold searches both succeed, single-flight build is preserved, and no module writes the sentinel outside the helper. | 378c485 "fix(search): publish the BM25 cache readiness key last (#490)" | 3 | LOAD-BEARING | Threads + `time.sleep(0.5)` to pin interleaving (standing lesson); source-text ratchet over a module list; #370 single-flight also asserted. |
| tests/test_bm25_correctness.py | `_dl` is unique-token length (no avgdl inflation), canonical corpus tokenization, and identity channel on the raw_query path. | 9213983 "fix: content hash consistency, budget packing efficiency, tokenizer quality"; 3878e04 "restore exact search identity" | 1 | LOAD-BEARING | T10/T11 internal task ids; relies on `_FIELD_REPS` literal 3x repetition. |
| tests/test_branch_indexing.py | Branch delta save/load/compose/delete, cache key includes branch, stale-delta detection, non-git graceful degradation. | d5c1aa9 "feat: branch-aware delta-layered indexing (v1.50.0)"; 7890b88 INDEX_VERSION 14 bump | 3 | LOAD-BEARING | Runs `git` via subprocess in tmp_path; 22 tests. |
| tests/test_build.py | pyproject excludes `.claude/`, `.gitignore` excludes it, and any built sdist in `dist/` contains no sensitive paths. | 913332b "Add structural guardrails to permanently prevent credential exposure in sdist" | 8 | LOAD-BEARING | The v0.2.6 credential-leak guard; sdist scan returns silently when `dist/` is empty (vacuous locally, real in CI). |
| tests/test_build_tree_spellings.py | `build`/`.build`/`_build` are all skipped through both derived exports and the exclusion stays overridable per project. | 5bdebfd "fix(index): skip _build, the third spelling of a build tree" | 8 | LOAD-BEARING | Property-style, not list pin; cites v1.108.234 duplicate-source defect and Practice 8 / #411 isolation. |
| tests/test_byte_offset_symbol_names.py | A non-ASCII comment earlier in a file does not shift later symbol names (differential parse over 16 extractors), and `_byte_sliced_source` trims split characters. | 1110312 "release: v1.108.244 - byte offsets sliced a decoded str in 16 extractors (#414)"; 01bd357 Racket added | 1 | LOAD-BEARING | #414 (@MotoMato85); paired ASCII/U+00E4 fixtures because pure ASCII cannot catch it. |
| tests/test_cache_hit_rate_basis.py | `hit_rate` is stamped raw key-presence, unvalidated hits go to a third UNKNOWN bucket, buckets sum to hits, and `analyze_perf` cannot emit the raw rate alone. | fe1cf31 "fix(perf): stop publishing a cache hit-rate that counts key-presence as validity" | 9 | LOAD-BEARING | Cites arXiv:2608.20280; asserts on the emitted payload. |
| tests/test_cache_mode.py | `cache_mode="metadata_only"` makes `_write_cached_text` a no-op, default is full, and the key survives `config --upgrade`. | aa4cb54 "release: v1.108.22 — keyring creds, metadata-only cache, init --minimal, docstring opt-out, git-SHA verify, sigstore signing" | 6 | LOAD-BEARING | Patches config globals; 4 tests. |
| tests/test_call_extraction.py | Python/JS call_references extracted per enclosing symbol, self-recursion excluded, nested functions attributed correctly. | 32eb018 "feat: AST call graph, decorator awareness, negative evidence, framework providers + perf optimizations (#218)" | 1 | LOAD-BEARING | #218 Task 2. |
| tests/test_call_graph_ast.py | Freshly indexed repos carry call_references, `get_call_hierarchy` uses AST methodology for Python and JS, and a v7-style index without the field does not crash. | 32eb018 (#218) Task 4 | 1 | LOAD-BEARING | E2E over tmp repos; the v7 backward-compat case is a replay guard (7). |
| tests/test_call_hierarchy.py | `get_call_hierarchy`/`get_impact_preview` on a 4-file chain: word-match helpers, callers/callees, depth, and shared file reads across BFS. | 32eb018 (#218); ac1ee7e "Phase 1 competitive upgrade — landmarks, resolution tiers, identity channel"; fb3b4ea "v1.108.32 — share file reads across call-graph BFS" | 1 | LOAD-BEARING | 54 tests; overlaps test_call_graph_ast.py on hierarchy but adds impact_preview and helper units. |
| tests/test_call_outcome_contract.py | Every in-frame error exit of `_call_tool_impl` clears the ok flag, the telemetry row derives from the returned result, and front-door error bodies set `isError` (AST predicate with non-vacuity check). | 923c3d6 "fix: four import-graph and call-outcome defects (#550, #551, #552)" | 9 | LOAD-BEARING | AST ratchet over `server.py`/counter handlers; #551/#552 (@rknighton). |
| tests/test_call_references_model.py | `Symbol.call_references` defaults/round-trip, `callers_by_name` derivation, SQLite persistence, and `INDEX_VERSION == 17`. | 51dca43 "release: v1.108.96 — import-scip: compile-time evidence via SCIP index ingest"; a72b47c v1.98.0 | 7 | LOAD-BEARING | Oddity: test named `test_index_version_is_9` asserts 17 (name kept for blame per its docstring); literal must be bumped with every INDEX_VERSION change. |
| tests/test_capability_certificate.py | The corpus capability certificate counts languages not extensions (74 not 157), records grammar-pack version, keeps absent vs unknown distinct, and is content-addressed/deterministic. | 569525b "release: v1.108.221 - what this install could establish, not just what it saw" | 9 | LOAD-BEARING | Cites #382 (grammar bundling) and the v1.108.215 false-absence shape; also 3. |
| tests/test_catalog_moratorium.py | The visible catalog has not grown past `CATALOG_CEILING = 91`, withheld actions exist but are not exposed, and the exit bar is measured on the held-out corpus with a leakage ceiling. | 3075ffa "release: v1.108.219 - the catalog moratorium, with a test rather than a promise"; 2a1cc2a v1.108.220; f750718 reuse_audit withheld | 4 | LOAD-BEARING | Literal 91 pin; reads `benchmarks` holdout.json; #397/#398 cited. |
| tests/test_channel_accuracy.py | Re-runs `benchmarks/goldset/measure.py` and asserts equality with `provenance/channel_accuracy.json`, its corpus hash, and the MEASURED provenance registry entries. | c2e801a "release: v1.108.140 — first gold corpus: find_implementations channel accuracy, measured"; 67fa70b v1.108.141 | N4 | LOAD-BEARING | Loads a benchmark script by path; validates against `schemas/confidence-provenance.schema.json`; also 1. |
| tests/test_check_delete_safe.py | Orphan is safe, used is not, route decorator is entry_point, test-only importer downgrades to `test_coverage_only`, signals dict populated. | 5775faa "fix: v1.104.1 — check_delete_safe test-importer classification"; 87d98a3 v1.108.6 honest-hint | 1 | LOAD-BEARING | Predates the #566 corpus-adequacy verdict (covered elsewhere); v1.104.1 regression noted in docstring. |
| tests/test_check_edit_safe.py | Orphan is safe to edit, imported symbol flags signature impact, high complexity and test coverage are reported, tool is read-only. | b8cfe16 "release: v1.108.24 — check_edit_safe edit-safety preflight" | 1 | LOAD-BEARING | Asserts source file unchanged after call (read-only contract). |
| tests/test_check_references.py | `check_references` finds import and content refs, honours `search_content=False`, batches identifiers, errors on unindexed repo. | 7d7797a "feat: add check_references composite tool for dead-code detection"; 890ba47 "fix: has_importers race, batch dedup, redundant lowercasing, and missing edge-case tests" | 1 | LOAD-BEARING | Race/batch-dedup fixes referenced in commits. |
| tests/test_check_rename_safe.py | Safe rename returns true, collisions detected, missing symbol/repo error, result carries symbol info and timing. | 9a7b421 "release: v1.16.0 — Refactoring Safety Net" | 1 | LOAD-BEARING | 6 tests. |
| tests/test_churn_rate.py | `get_churn_rate` errors cleanly without git/source_root and returns fields/assessment for file and symbol targets against a real git repo. | 072a49b "release: v1.17.0 — Code Metrics & Health (get_symbol_complexity, get_churn_rate, get_hotspots, get_repo_health)" | 1 | LOAD-BEARING | Runs `git init`/`commit` in tmp_path; predates the `_git_history.py` shallow-coverage work. |
| tests/test_ci_env_reproduce_command.py | CLAUDE.md's documented CI reproduce command syncs with `--group dev --extra watch` matching `test.yml`, and the broken bare form appears nowhere. | 54d8c5c "release: v1.108.305 — only the reader was never fixed" | N7 | LOAD-BEARING | Text ratchet over CLAUDE.md + `.github/workflows/test.yml`; guards the 105-silent-skips incident. |
| tests/test_class_hierarchy.py | `_parse_bases` across Python/Java/TS/C++ syntaxes and `get_class_hierarchy` ancestors/descendants over an indexed repo. | 94f9ed2 "release: v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)" | 1 | LOAD-BEARING | T13 internal id. |
| tests/test_claude_md_policy.py | `_CLAUDE_MD_POLICY` contains required routing terms, cursor/windsurf rules inherit it, and `_filter_policy_for_tools` respects tool_profile/disabled_tools. | e086e9a "fix: claude-md respects tool_profile and disabled_tools (#242)"; 9cf763e cycles refactor | 6 | LOAD-BEARING | #242; term-presence list is mechanism-shaped (Practice 9 tell) but the filter half is outcome-based. |
| tests/test_claude_md_rotation.py | CLAUDE.md Current State names exactly the three newest CHANGELOG releases in order and the Older-releases boundary is the fourth; CHANGELOG headings are line-anchored and unique. | 3d634e6 "release: v1.108.213 — the Counter stopped ranking on the word me"; 642db54 "test(changelog): fail on a heading that is not line-anchored" (PR #451 rebase) | N6 | LOAD-BEARING | Text ratchet over CLAUDE.md/CHANGELOG.md/pyproject; the two-edit rotation trap named in CLAUDE.md. |
| tests/test_claude_md_size.py | CLAUDE.md is under the 140,000-char budget, ISSUE-HISTORY.md exists/is git-tracked/is pointed at, and no dated entry survives outside the archive. | 87ee49d first commit; da23d93 "chore: raise the CLAUDE.md budget to 140,000 chars" | N6 | LOAD-BEARING | Budget literal moved 130k to 140k with recorded rationale; shells out to `git ls-files`. |
| tests/test_cleanup.py | `resolve_repo` parses owner/name, looks up by name/display name, raises on unknown/ambiguous; `search_symbols` clamps `max_results`. | debc193 "fix: deduplicate repo parsing, add input validation, CI coverage (#12)" | 1 | LOAD-BEARING | #12; early-era file writes hand-built JSON index fixtures (pre-SQLite shape) — worth checking the lookup path still exercises the live store. |
| tests/test_cli.py | `config --init` creates a JSONC template and refuses overwrite; `config --upgrade` adds missing keys and preserves user values. | 8c5a7ca "test+feat: Phase 4 - config --init CLI command"; 55cf5d3 "feat: add config --upgrade for additive config migration (closes #191)" | 6 | LOAD-BEARING | #191; monkeypatches `CODE_INDEX_PATH`. |
| tests/test_cli_env_split.py | CLI/Env rows live in exactly one of CLAUDE.md or CLI-AND-ENV.md, rationale rows stay in CLAUDE.md, and every documented row still resolves in `src/`. | f9e1019 "docs: the CLI and env tables leave the always-loaded budget" | N6 | LOAD-BEARING | Text ratchet; `CLI_RATIONALE`/`ENV_RATIONALE` name lists are a budget-gaming hazard by design; "in neither" direction deliberately one-sided. |
| tests/test_cli_output_encoding.py | CLI output (`receipt --explain`, `render_diagram`) is valid UTF-8 when piped, and `PYTHONIOENCODING` is honoured as an opt-out. | 1bb4cca "release: v1.108.262 - CLI output is UTF-8 even when piped" | 6 | LOAD-BEARING | Spawns the CLI in a subprocess with a pipe (the only reproducing configuration); Windows cp1252 specific. |
| tests/test_code_index_path_is_honoured.py | `CODE_INDEX_PATH` moves both `IndexStore` and `SQLiteIndexStore` defaults, explicit base_path wins, empty var is unset, and the two classes cannot disagree. | 53fdfcd "fix(storage): honour CODE_INDEX_PATH in the store and lock defaults" | 6 | LOAD-BEARING | Cites #428 shape and the v1.108.280 CWD rule; `falls_back_to_home` resolves `Path.home()` without writing. |
| tests/test_complexity.py | `_count_params` (generics, C `void`), `_max_nesting_depth`, and `compute_complexity` cyclomatic outputs. | 9a7b421 v1.16.0; b4db152 "release: v1.21.19 — Phase 5 uncertainty disclosure (T15-T18)" | 1 | LOAD-BEARING | Predates the two-channel nesting fix (that lives in test_nesting_depth_channels.py). |
| tests/test_confidence.py | `compute_confidence` yields 0 on empty, high on a dominant top-1, lower on close scores/stale index, boosted by identity match; components returned. | a3ac917 "release: v1.75.0 — retrieval confidence score + token-accounting baseline" | 1 | LOAD-BEARING | 13 tests. |
| tests/test_confidence_score_scale.py | The strength curve's ceiling is derived from the live fusion weight vector per channel (BM25/fused/cosine/PageRank) and the pre-fix scale gap is pinned in size. | dc57d20 "release: v1.108.265 - retrieval confidence grades ranking quality, not units" | 1 | LOAD-BEARING | Ported from jdoc #106 as a contract; source-text scan near line 214; magnitude pin "so the fix cannot quietly regress". |
| tests/test_config.py | JSONC stripping, defaults, env mapping, project overrides, `config --check` rows (#300/#304/#335), template key documentation, guide filtering (#495). | 4c0567a "fix(guide): filter jcodemunch_guide by disabled_tools and profile (#495)"; e4fc151 "Document the 13 template-less config keys" | 6 | LOAD-BEARING | 110 tests; isolates HOME so the real `~/.claude` is not read; named regressions from @mmashwani (#335) and @slazarov (#300). |
| tests/test_config_check_hooks.py | `config --check`'s hook report is derived from the installer's set, reports absolute-path installs and `hook-sessionstart`, and still reports absent hooks. | f82dfc9 "release: v1.108.255 - hook output on channels the model never sees"; 9b375cd "fix(hooks): ten confirmed defects in the Claude Code hook steering layer" | 6 | LOAD-BEARING | Runs the CLI as a subprocess against real stdout. |
| tests/test_config_check_matches_resolver.py | `config --check` reports the runtime-resolved indexing limits under both load orders, with project-declared keys still winning. | cb8b3ed "release: v1.108.250 — config --check reported a default while the indexer used your config" (#416 follow-up, @domis86) | 6 | LOAD-BEARING | Parametrized over the SET of limits and both load orders; explicit non-vacuity note in docstring; #411 isolation. |
| tests/test_config_isolation_guard.py | No test fixture calls a bare `load_config()` that would read or write the developer's real `~/.code-index/config.jsonc`. | f03bc6b "fix(tests): stop the suite reading and writing the developer's real config"; docstring cites #437 / #411 / @lilubot's PR #433 | N7 | LOAD-BEARING | Source-scanning ratchet over `tests/`; carries a by-name exemption list; e3663c5 taught it to see the `src.jcodemunch_mcp` twin |
| tests/test_config_lazy_load.py | `config.get()` never returns a hardcoded default for a config that was never loaded. | c4577a7 "release: v1.108.258 - a config read that reads the config"; docstring #426, #425 | 6 | LOAD-BEARING | Spawns fresh `sys.executable -c` subprocesses (the docstring calls those the load-bearing ones) |
| tests/test_config_set.py | `config set`/`unset` write typed values into JSONC with comment-aware scanning, coercion and rollback. | cedc629 "feat: config set / unset CLI with typed JSONC write-back (v1.108.51)" | 6 | LOAD-BEARING | Feature-coverage for a user-facing CLI writer; parametrized type matrix |
| tests/test_config_watcher_keys.py | The five watcher config keys exist in `DEFAULTS` with the expected default values and types. | 845a2df "feat: config-driven watcher params; remove retired tools from docs" | 6 | UNCLEAR | Asserts literal defaults only (`watch_paths == []`, etc.); restates the constant table rather than a behaviour; not covered by test_docs_config_parity.py (that one checks docs vs code, not the values themselves) |
| tests/test_configuration_md_defaults.py | Every `Default` cell in CONFIGURATION.md's tables equals `config.DEFAULTS` for that key, with a bounded escape hatch for oversized defaults. | 0e15604 "docs(config): the documented disabled_tools default was not the shipped one (#515)" | 6 | LOAD-BEARING | Docs-scanning ratchet; sibling of test_docs_config_parity.py (older, skip-list based) — overlapping but this one is table-wide and forbids the empty-dict cell, so neither fully covers the other |
| tests/test_constant_extraction_guard.py | Every language in `constant_patterns` actually extracts a constant from a sample, or is exempt by name. | 3df3ae5 "fix(parser): extract Rust, Go, Java and PHP constants (#428)"; docstring #428 @mussonking, cites #411 config isolation | 10 | LOAD-BEARING | Property-over-registry ratchet; config fixture isolation deliberate |
| tests/test_context_providers.py | ContextProvider base classes, registry, `discover_providers`, `enrich_symbols` and `collect_extra_imports` behave as the framework contract. | c8b9ab5 "Add extensible context provider framework for ecosystem metadata enrichment"; dd0653f (#201) | 1 | STRUCTURAL | Framework plumbing tests for the provider registry; 23 tests |
| tests/test_copilot_hook.py | The Copilot postToolUse adapter parses `toolArgs` as a JSON-encoded string and reindexes only code files. | 51c5e90 "refactor(hooks): split cli/hooks.py into a package"; 2f92477 "close the assertion gaps the steering batch's own audit named" | 6 | LOAD-BEARING | Client-adapter contract (Copilot stdin shape differs from Claude Code) |
| tests/test_count_cap_end_to_end.py | `max_folder_files` from every documented config route bites at every discovery entry point, measured by what got indexed. | 3be11e6 "release: v1.108.247 — a project's file-count cap is no longer resolved and then discarded"; docstring #416 @domis86, #437 | 6 | LOAD-BEARING | End-to-end matrix (route x entry point); sibling of test_size_cap_end_to_end.py |
| tests/test_counter.py | The Counter front door (order/menu/route) keeps `full` unchanged, collapses on `counter`, gates state-changing/exec actions, and routes ambiguous tasks to more than one action. | 3d634e6 "v1.108.213 — the Counter stopped ranking on the word me"; 40a5816 "v1.108.253 — route answered an ambiguous question with one confident action"; docstring cites docs/prd-adaptive-tool-surface.md, jcm#422 | 4 | LOAD-BEARING | 39 tests; includes charter CI gate that no catalog action trips the exec-verb tripwire; autouse env/cache isolation |
| tests/test_counter_surface_stability.py | The published `counter` tool list is byte-pinned (names, order, per-tool sha256, 4,184 total bytes, whitelist, independence from `tool_profile`). | ca39952 "release: v1.108.314"; docstring: cached-prefix cost (174 requests), arXiv:2608.22708 | 4 | LOAD-BEARING | Byte-level pin (`PUBLISHED_TOTAL_BYTES = 4184`, sha256[:16] per tool); a failure is a cache-write cost, not a bug; mentions #437 config shape |
| tests/test_counts_survive_truncation.py | For every tool with a (count, capped list) pair, the count is identical across two `max_results` page sizes over the same repo. | 79e4d0d "fix: a count taken after the page, and a field nobody read (#559-#562)"; docstring #559 @lilubot | 9 | LOAD-BEARING | Property over tools rather than a one-tool regression; fixture must exceed the small page |
| tests/test_credentials.py | `keyring:NAME` env vars resolve through the keyring at startup and fail closed (literal stays) when missing or keyring absent. | aa4cb54 "release: v1.108.22 — keyring creds, ..."; docstring P1.3 | 8 | LOAD-BEARING | Mocks keyring via unittest.mock; no real keyring needed |
| tests/test_cross_repo.py | Package-name extraction per ecosystem, registry building, and `cross_repo=` on find_importers/get_blast_radius/get_dependency_graph/get_cross_repo_map work, including malformed manifests and cycles. | d9a1478 "feat: cross-repository dependency tracking (v1.13.0)" | 1 | LOAD-BEARING | 53 tests; covers `JCODEMUNCH_CROSS_REPO_DEFAULT` env var |
| tests/test_css.py | CSS/SCSS/SASS/Less/Stylus selectors, @keyframes, @media, @supports and variables are extracted as symbols. | 4654a2f "feat: CSS symbol extraction (fixes #193)"; db6b8d0 "add SCSS/SASS/Less/Stylus support (closes #197)" | 10 | LOAD-BEARING | Header comment documents the `src.jcodemunch_mcp` module-twin trap; 43 tests |
| tests/test_dart_imports.py | Dart `import`/`export`/`part` specifiers are extracted and `dart` is registered in `_LANGUAGE_EXTRACTORS`. | e2f4342 "release: v1.21.20 — Phase 6 (Extend & Harden)"; docstring "T19" | 10 | LOAD-BEARING | Single fixture source string |
| tests/test_dbt_provider.py | The dbt context provider detects projects, parses schema.yml/doc blocks and resolves descriptions onto model symbols. | c8b9ab5 provider framework; 9661e0b "test: ten skipped modules that hid 209 tests between them" | 1 | LOAD-BEARING | `skipif(find_spec("yaml") is None)` on three classes only — deliberately NOT module-level importorskip (comment explains the 15-hidden-tests defect); N7-relevant |
| tests/test_dead_code_corpus_adequacy.py | `find_dead_code`'s confidence is capped at 0.6 when the corpus is stale/incomplete, dynamically-enumerated packages are not reported dead, and `check_delete_safe` refuses `safe_to_delete` on an inadequate corpus. | 1282ab5 "fix: find_dead_code published a proof its corpus could not support (#566, #569)" | 3 | LOAD-BEARING | Non-vacuity anchor `test_qualified_path_is_not_the_loaders_own_package` guards the 502-file false-negative; cites #559 shape |
| tests/test_dead_code_v2.py | `get_dead_code_v2` reports the unreferenced function and not the used one on a small indexed repo, with filters. | 9a7b421 "release: v1.16.0 — Refactoring Safety Net" | 1 | LOAD-BEARING | Basic feature coverage; distinct tool from test_find_dead_code.py |
| tests/test_deadcode_eval_harness.py | The pure functions of `benchmarks/deadcode_eval/run_eval.py` (oracle diffing, classification) behave correctly. | c452376 "benchmarks: a re-runnable dead-code false-positive oracle (#408 follow-up)" | N4 | STRUCTURAL | Loads the harness by file path with importlib; oracle collection itself is untested here by design |
| tests/test_decorator_awareness.py | get_file_outline and search_symbols surface decorators and search_symbols can filter by decorator. | 32eb018 "feat: AST call graph, decorator awareness, negative evidence, framework providers (#218)" | 1 | LOAD-BEARING | Integration over an indexed tmp repo |
| tests/test_decorator_census.py | `get_decorator_census` normalises decorator forms, counts, filters by name/scope/kind, caps sites, and is read-only. | ce11160 "get_decorator_census: repo-wide decorator/annotation census (v1.108.112)" | 1 | LOAD-BEARING | Tool feature coverage; Practice 1 test |
| tests/test_decorator_routes.py | The decorator_routes provider detects Flask/FastAPI/Spring/NestJS/ASP.NET and extracts route edges. | 32eb018 (#218) | 1 | LOAD-BEARING | Per-framework detection via requirements/pom/package manifests |
| tests/test_deferred_summarization.py | `parse_immediate` returns symbols with empty summaries and `parse_and_prepare_incremental` defers summarisation. | 4448e3e "feat: split indexing into immediate parse + deferred summarization" | 3 | LOAD-BEARING | Only 3 tests; both functions still exist in `tools/_indexing_pipeline.py` |
| tests/test_delete_index_cli.py | `delete-index` is routed as a known command, emits `--json`, exits 0 on success and nonzero on a missing repo, against an isolated `CODE_INDEX_PATH`. | 26d365e "feat: delete-index CLI subcommand (v1.108.50)"; 3dbab7f "v1.108.54 — list-repos / delete-index honor CODE_INDEX_PATH" | 6 | LOAD-BEARING | Single subprocess test; exit codes are a jMunch Console contract |
| tests/test_delivery_metrics.py | `get_delivery_metrics` buckets commits as durable/reworked/reverted/revert_authored and excludes hub files, on date-controlled real git repos. | e4ea9f6 "release: v1.108.69 - durable-change delivery" | 1 | LOAD-BEARING | Shells out to git with GIT_*_DATE; docstring explains same-second commits would dodge the rework check |
| tests/test_dependency_graph_imports_alias.py | `get_dependency_graph` exposes flat top-level `imports`/`importers` arrays beside `edges`/`neighbors`. | 054d988 "release: v1.83.1 — reference-tool shapes carry line numbers + flat import aliases" | 7 | LOAD-BEARING | Response-shape pin for harvesters |
| tests/test_description_smells.py | No live tool description scores Unclear Purpose or Underspecified Length on the arXiv:2602.14878 scorer. | 68fecb6 "release: v1.108.282 — half the tool descriptions never said what the tool would not do" | 4 | LOAD-BEARING | Single async test; `skipif(not SCORER.is_file())`; loads benchmarks/description_smells/score_descriptions.py by path; deliberately does not gate Unstated Limitation or Opaque Parameters |
| tests/test_digest.py | The `digest` briefing computes deltas from persisted state under `CODE_INDEX_PATH` and reports unloadable indexes truthfully. | 4e9a8a7 "release: v1.86.0 — digest agent stand-up briefing"; 2ff57a8 "fix: report unloadable repo indexes truthfully (#291)" | 9 | LOAD-BEARING | Fake index objects; `source_root="/canonical/repo"` literal |
| tests/test_dispatch_resolution.py | Interface/trait dispatch resolution (keyword tagging, implementor lookup, call-site resolution) works across languages. | 7750d47 "feat: Phase 5 competitive upgrade — interface & trait dispatch resolution (v2.0.0)" | 1 | LOAD-BEARING | Uses MagicMock tree-sitter nodes; `_ABS_PREFIX = os.path.abspath(os.sep + "tmp")` for Windows |
| tests/test_dispatch_schema_parity.py | Every `arguments["k"]`/`arguments.get("k")` read in `_call_tool_impl` is a declared inputSchema property of that tool. | 38a7080 "v1.108.75: Phase 3 quality gates — live core_compact ceiling + schema/dispatch param parity"; 3037636 (#425) | 4 | LOAD-BEARING | Source ratchet via `inspect.getsource` + regex over the dispatcher; cross-cutting key allowlist |
| tests/test_dispatcher_arg_mutation.py | `call_tool` does not `pop` `format` from the caller's own `arguments` dict, so a reused dict keeps its requested encoding. | 1f2aebc "fix(server): stop the dispatcher eating its caller's format argument"; docstring #482 | 1 | LOAD-BEARING | Three async tests; asserts on the argument dict, not the encoding |
| tests/test_django_provider.py | The Django provider detects projects (manage.py + dependency) and extracts URL-pattern routes. | 32eb018 (#218) | 1 | LOAD-BEARING | 7 tests |
| tests/test_docs_config_parity.py | CONFIGURATION.md's Default column matches `config.DEFAULTS` for every key not in an explicit skip set. | 76cd23e "release: v1.70.0 — default detail_level=auto"; 866ad15 "parse only the Default column"; docstring cites the `meta_fields` null-vs-[] drift | 6 | LOAD-BEARING | Docs-scanning ratchet; predates and overlaps test_configuration_md_defaults.py (#515), which is stricter on the skip set — the pair could be consolidated but neither covers the other today |
| tests/test_embed_drift.py | `check_embedding_drift` snapshots vectors, detects shifted centroids, and serves the shared provider advice. | ad4e916 "release: v1.80.0 — embedding drift detector"; 6a43655 (#489) | 3 | LOAD-BEARING | Seeded random vectors; line 83 asserts the shared advice, not a literal (Practice 9 lesson) |
| tests/test_embedding_model_change.py | `embed_repo` detects a stored-model change and rebuilds instead of mixing vector widths; the exclusion count is reported. | 57d19ab "fix(embed): detect a model change instead of mixing vector widths (#500)"; c8dd085 (#488) | 3 | LOAD-BEARING | Patches `_detect_provider` through its shared source |
| tests/test_embedding_provider_advice.py | All three provider-advice sites (embed_repo error, `semantic` param description, search_symbols no-provider error) name the bundled ONNX encoder from one shared message. | 6a43655 "fix(embeddings): one source for provider advice, bundled encoder first (#489)" | 4 | LOAD-BEARING | Source ratchet via `inspect.getsource` asserting the old literal is absent and `NO_PROVIDER_MESSAGE` is used |
| tests/test_endpoint_impact.py | `get_endpoint_impact` resolves verb+path to handlers from decorator (Flask) and string-dispatch (Django) routes and returns their blast radius. | bdebb63 "v1.108.90 — new tool: get_endpoint_impact" | 1 | LOAD-BEARING | Builds real fixture repos via index_folder |
| tests/test_endpoint_infra_impact.py | `include_infra=True` on get_endpoint_impact fuses `collect_project_intel` cross-refs and upstream `exposes[]` into the endpoint result. | 789aa12 "v1.108.91 — endpoint-scoped infrastructure links"; 8417eca "v1.108.93: upstream exposes[]" | 1 | LOAD-BEARING | Cites docs/prd-endpoint-infra-impact.md P1; 19 tests |
| tests/test_explicit_embed_model_wins.py | An explicit `embed_model`/`JCODEMUNCH_EMBED_MODEL` outranks the bundled ONNX default while paid providers stay below it. | c8dd085 "fix(embed): explicit local model outranks the bundled default (#488)" | 6 | LOAD-BEARING | Line 196 scans `config.py` source text for the precedence comment; pairs with test_paid_embeddings_optin.py |
| tests/test_express_provider.py | The Express provider detects Express/Fastify/Hono/Koa from package.json and extracts routes. | 32eb018 (#218) | 1 | LOAD-BEARING | 8 tests |
| tests/test_extra_extensions.py | `extra_extensions` config maps additional file extensions onto languages and is applied once. | 3feb47c "feat(config): complete JSONC config implementation"; a5df17d "Add 11 new languages" | 6 | LOAD-BEARING | Autouse fixture restores `LANGUAGE_EXTENSIONS` and `_GLOBAL_CONFIG` (mutates module globals) |
| tests/test_extraction_candidates.py | `get_extraction_candidates` ranks complex multi-caller functions as candidates on an indexed repo. | 9a7b421 "release: v1.16.0 — Refactoring Safety Net" | 1 | LOAD-BEARING | 8 tests |
| tests/test_fast_path_no_hydration.py | The watcher fast path in `index_folder` reads only metadata (`SelectiveIndexView.promoted is False`) and never hydrates symbols. | c9cdbda "fix: the watcher fast path hydrated the index to read six metadata fields (#557)" | 5 | LOAD-BEARING | Asserts outcome (`promoted is False`) not mechanism; measured 0.172s vs <1ms in docstring; @Ticki84 |
| tests/test_file_io_encoding_guard.py | Every text-mode `open()`/`read_text()`/`write_text()` under `src/` declares an encoding, located positionally not by keyword. | d9a9524 "release: v1.108.264 - text-mode file IO declares its encoding"; 7d81cd1 "scanner locates mode by value, not by position" | 8 | LOAD-BEARING | Source ratchet over `SRC.rglob("*.py")` via AST; third of the cp1252 family (test_subprocess_encoding_guard.py, test_cli_output_encoding.py) |
| tests/test_file_summaries.py | `_heuristic_summary` and `generate_file_summaries` produce file-level summaries from symbol kinds/counts. | early feature; touched by a72b47c (v1.98.0) and 51dca43 (v1.108.96) only incidentally | 1 | LOAD-BEARING | 12 tests; unit-level with hand-built Symbols |
| tests/test_find_dead_code.py | `find_dead_code` v1 reports unreferenced symbols, respects entry points including package.json ones, and matches v2 parity. | 62f6164 "feat: find_dead_code tool (v1.11.13–v1.11.14)"; fe3963a "v1.80.8 — find_dead_code v1 parity" | 1 | LOAD-BEARING | 14 tests over index_folder tmp repos; corpus-adequacy behaviour lives in test_dead_code_corpus_adequacy.py |
| tests/test_find_implementations.py | `find_implementations` discovers implementors via inheritance, interfaces and naming across languages, and reports unloadable indexes truthfully. | 07b8e50 "release: v1.104.0 — find_implementations + check_delete_safe (Serena parity)"; 2ff57a8 (#291) | 1 | LOAD-BEARING | 13 tests |
| tests/test_find_importers.py | `extract_imports` across JS/TS/Python/etc., tsconfig alias resolution (incl. JSONC), and find_importers/find_references resolve real edges including package-relative Python imports. | fdfd1db "v1.94.0 selective re-export tracking"; 28e20a4 "v1.108.254 — Python package-relative imports built no graph edge at all"; refs #170, #252, jcm#423 | 1 | LOAD-BEARING | 74 tests; line 350 notes a prior expectation encoded the defect (Practice 9) |
| tests/test_find_references_line_numbers.py | Each `find_references` match carries the line number of the referencing site. | 054d988 "release: v1.83.1 — reference-tool shapes carry line numbers"; docstring: sverklo benchmark strict parser | 7 | LOAD-BEARING | Response-shape pin; 3 tests |
| tests/test_find_similar_symbols.py | `find_similar_symbols` clusters near-duplicates, picks a canonical by PageRank, honours threshold/min_size/scope/include_tests and falls back to structural mode without embeddings. | 37fb1d8 "release: v1.102.0 — find_similar_symbols (F-15 consolidation detection)" | 1 | LOAD-BEARING | 14 tests |
| tests/test_fqn.py | `symbol_to_fqn` / `fqn_to_symbol` translate PHP PSR-4 namespaces to symbol ids and back. | dd0653f "Stages 4-6 — ... FQN translation (#201)"; 2ff57a8 (#291) | 1 | LOAD-BEARING | 22 tests; PSR4 map fixture |
| tests/test_framework_build_trees_are_skipped.py | The eight dotted framework build trees (`.next`, `.nuxt`, ...) are in `_SKIP_DIRECTORY_NAMES` and derive into both `SKIP_DIRECTORIES` and `SKIP_PATTERNS`, while `out`/`bin`/`obj`/`coverage`/`public` are asserted ABSENT. | f3f424b "fix(discovery): skip the JS/TS framework build trees"; docstring: v1.108.234 defect 4th time, GitNexus fix title | 3 | LOAD-BEARING | Two tests, both directions (presence and absence) |
| tests/test_framework_profiles.py | `detect_framework` identifies Laravel/Django/Next.js etc. from manifests and `profile_to_meta` serialises the profile. | aef7078 "feat: PSR-4 resolution, Laravel provider, framework profiles (#201 Stage 1-2-3)" | 1 | LOAD-BEARING | 15 tests; `profile_to_meta` output is what #561/#562's `_entry_points.py` now reads |
| tests/test_freshness.py | `FreshnessProbe.classify` returns fresh/edited_uncommitted/stale_index and `unknown` (never `fresh`) when no source root, missing file, or no baseline. | e486a8b "v1.77.0 — per-symbol freshness markers"; 7ceca0a "v1.108.209 — per-file freshness stops answering fresh when it could not measure"; b008e19 (B1 git HEAD cache) | 3 | LOAD-BEARING | Line 21/103 comments record the .209 fresh->unknown change |
| tests/test_future_version_no_false_absence.py | A `sqlite_future_version` index makes `search_symbols`/tool layer return a structured error, never an empty result. | 09c82c0 "test: a future-version index must never look like an absent symbol"; docstring #377 @mightydanp, #389 @tgarrick-bit | 7 | LOAD-BEARING | Adversarial repro that did NOT reproduce; pins a convention previously enforced by no test |
| tests/test_fuzzy_search.py | `_trigrams`/`_edit_distance` helpers and `search_symbols` fuzzy fallback return near-miss names within the edit-distance cap. | `7fb0850 feat: fuzzy symbol search (v1.11.11)`; docstring "Feature 6" | 1 | LOAD-BEARING | Feature-introduction test, no issue #; 21 tests, pure unit. |
| tests/test_gcm.py | `GcmConfig` defaults/env fallback/validation and `_is_github_repo` repo-shape detection for the Groq `gcm` CLI. | `1b9f210 feat: add gcm CLI — codebase Q&A powered by Groq (v1.39.0)`; `5dfc6e6 fix: gcm GitHub repo detection on Linux (v1.39.1)` | 6 | LOAD-BEARING | Line 60 uses literal `C:\\Users\\foo` as a not-github input (platform-shaped literal); Groq calls mocked. |
| tests/test_generated_policy_matches_tools_list.py | Every tool named by the CLAUDE.md `init` policy and `jcodemunch_guide` is one `_build_tools_list()` advertises, under session tier override, bundles, and the `languages` gate. | `054755d fix(policy): resolve the active tool set from the builder, not the tier table (#507)`; docstring cites #495/#509 same shape | 4 | LOAD-BEARING | Property test "ask the builder, not the table"; 8 tests. |
| tests/test_generation_contract.py | `describe(index)` is the one read of index generation, and `connect_readonly` reads WAL data when sidecars exist and creates none when absent (a 2x2 matrix reproducing both single-flag defects). | `32058ab release: v1.108.215 - one read contract, and a false absence it was hiding`; docstring "#398 Arc 1", cites v1.108.185 `rebuilding` bug | 3 | LOAD-BEARING | Includes a source-text ratchet (line ~253 reads files) failing on any hand-rolled `?mode=ro` URI elsewhere in the tree. |
| tests/test_get_changed_symbols.py | `get_changed_symbols` maps a git diff to affected symbols and errors cleanly with no git / no source root. | `cdbde73 feat: get_changed_symbols tool — git diff to affected symbols (v1.11.15)` | 3 | LOAD-BEARING | Seeds index directly with a `"a"*40` dummy SHA; 12 tests. |
| tests/test_get_context_bundle.py | `get_context_bundle` returns definition + imports for a symbol and error shapes for missing symbol/repo. | `98d9535 feat: add get_context_bundle tool (definition + imports, symbol-scoped)`; `13072fc release: v1.5.0 — production hardening` | 1 | LOAD-BEARING | Fully MagicMock index; 6 tests; thin. |
| tests/test_get_file_outline_integration.py | Nested class members survive the producer -> MCP encoder -> decoder round-trip as a flat list with parent ids. | `0744be8 fix(get_file_outline): emit flat list with parent ids so nested symbols survive the wire (#278)` | 1 | LOAD-BEARING | Real `index_folder` on a tmp repo; guards #278. |
| tests/test_get_file_risk.py | `_level_for` thresholds and per-axis `_score_*` scorers for `get_file_risk` behave at boundary values. | `25603e1 release: v1.89.0 — VS Code risk-density gutter`; docstring "(v1.89.0)" | 9 | LOAD-BEARING | Parametrized boundary literals (84.9/69.9/54.9); feature-intro. |
| tests/test_get_file_tree.py | File nodes prefer symbol language over extension, overflow guard truncates at `max_files` with a `path_prefix` hint, and unloadable/corrupt indexes are reported actionably. | `56d34b8 feat: add and harden cpp language support`; `e456bac get_file_tree overflow guard — closes #178`; `2ff57a8 fix: report unloadable repo indexes truthfully (#291)` | 1 | LOAD-BEARING | Three eras in one file (#178, #291). |
| tests/test_get_group_contracts.py | `get_group_contracts` computes the cross-repo shared-symbol API surface across a provider and two consumers. | `5890141 release: v1.103.0 — get_group_contracts (F-15 cross-repo API surface)` | 1 | LOAD-BEARING | Indexes three tmp repos per test (16 tests) — likely slow relative to value (N1). |
| tests/test_get_repo_map.py | `get_repo_map` emits PageRank-ranked signatures within a token budget, honours `max_per_file`/scope/kinds, and errors on missing repo/invalid budget. | `5b9e6c0 release: v1.101.0 — get_repo_map (cold-start orientation; RepoMapper parity)` | 2 | LOAD-BEARING | Feature-intro; budget-honoured test is a token-budget property. |
| tests/test_get_untested_symbols.py | `_is_test_file` recognition, untested detection, blast-radius `has_test_reach`, and `untested_count`/`reached_pct` measured BEFORE the `max_results` cut. | `5bb9e26 feat: add get_untested_symbols tool + blast radius test-reach enrichment`; `79e4d0d fix: a count taken after the page... (#559-#562)` | 1 | LOAD-BEARING | #559 section asserts at the user's entry point (`get_repo_health` axis); two page sizes over one repo. |
| tests/test_git_history_coverage.py | A git history shorter than the churn window is reported UNMEASURABLE (tri-state), the composite/grade is withheld rather than the axis dropped, and every offset spelling (`Z`, `-05:00`) parses on 3.10. | `fc5179d fix: nine tools read a git window none of them could tell was truncated`; `636744e fix: git renders a UTC offset as Z, which 3.10 could not parse` | 9 | LOAD-BEARING | Pins the author's own wrong first fix (84.0 B -> 88.8 B); builds real git repos via subprocess; timezone-sensitive unit test. |
| tests/test_git_root_identity.py | Git-root-aware identity (`detect_git_root`, `_parse_owner_repo`), subdir merge for monorepos, and #504 full-root re-walk is not a subdir merge. | `16b496b release: v1.96.0 - subdir merge (#288 phase 2)`; `7fd8768 git identity opt-in (#295)`; `992cd2f fix(index_folder): a full-root re-walk is not a subdir merge (#504)` | 3 | LOAD-BEARING | 25 tests, real git subprocess; overlaps test_identity_mode.py on the default-mode question but covers merge logic that file does not. |
| tests/test_git_sha_verification.py | `verify_against="git_sha"` on `get_symbol_source` compares cached source to `git show HEAD:<file>` line slice, as bytes, with stderr redirected. | `b53b2a5 release: v1.108.224 - verify_against="git_sha" compares like with like`; `9850d6f release: v1.108.227 - git_sha verification answers the question it was asked`; docstring cites #400/#402 | 3 | LOAD-BEARING | `skipif` git not on PATH; wrapper hides the `(status, rev_used)` tuple. |
| tests/test_gleam_imports.py | Gleam imports are extracted (not from strings), specifiers resolve via module path, and `dev/` counts as a source root. | `8fbbc57 feat(parser): Gleam import extraction and module-path resolution`; `dbba4c3 fix(parser): recognize Gleam dev/ directories as source roots` | 10 | LOAD-BEARING | Contributor-era feature; 16 tests. |
| tests/test_go_routers_provider.py | `GoRoutersProvider` detects Gin/Chi/Echo/Fiber from go.mod and extracts route registrations. | `32eb018 feat: AST call graph, decorator awareness, negative evidence, framework providers + perf optimizations (#218)` | 10 | LOAD-BEARING | 8 tests; only history is the mega-commit #218. |
| tests/test_groq_explainer.py | `gcm explain` gathers outline/tree/symbols (mocked tools), degrades on error, and renders output. | `2983cf4 feat: Phase 4 — Voice-to-Codebase + Auto Repo Explainer (v1.40.0)` | 6 | LOAD-BEARING | `importorskip("PIL")` on two tests (N7 contributor); all MCP tool calls patched. |
| tests/test_groq_voice.py | `gcm --voice` audio-dep check, WAV capture bytes, and transcription pipeline with sounddevice/numpy mocked via `sys.modules`. | `2983cf4 feat: Phase 4 — Voice-to-Codebase + Auto Repo Explainer (v1.40.0)` | 6 | UNCLEAR | One test admits "may or may not be None... just check it doesn't crash" — weak assertion; mocks `sys.modules`, fragile under xdist. No issue history since 2026-04-13. |
| tests/test_guide_respects_disabled_tools.py | `jcodemunch_guide` names no tool in `disabled_tools` or outside the profile, anywhere in the snippet including `### Quick start`. | `4c0567a fix(guide): filter jcodemunch_guide by disabled_tools and profile (#495)`; `cb98488 fix(guide): filter Quick start too (#506)` | 4 | LOAD-BEARING | Shipped-default reproduction (`disabled_tools=["test_summarizer"]`); helper deliberately scans whole snippet after #506. |
| tests/test_handshake_watchdog.py | `_signal_handshake` is safe with no event and the stdio watchdog writes one stderr hint when no handler is ever called. | `67d636f release: v1.82.1 — handshake watchdog for stdio transport`; docstring: v1.81.3 Codex/rmcp 5h+ silent hang | 6 | LOAD-BEARING | Autouse fixture resets `srv._handshake_event` module global; 5 tests. |
| tests/test_hardening.py | Grab-bag: per-language fixture extraction, overload disambiguation, content hashing, determinism, incremental reindex, index versioning, timing-safe bearer auth, GitHub URL parsing security, gitignore case normalisation, rate-limit middleware. | `13072fc`-era hardening; latest `89aaa21 fix(rust): give impl methods the type they hang off`, `51dca43 v1.108.96 import-scip`, `a72b47c v1.98.0` | 1, 8 | LOAD-BEARING | 50 tests across 11 classes — a dumping ground; `skipif` for Windows-only case normalisation; reads `tests/fixtures/*`. Candidate to split, not to delete. |
| tests/test_health_radar.py | Six per-axis scorers, `_letter_grade`, `compute_radar`, and `diff_radar` regression/improvement detection. | `cc17ae1 release: v1.87.0 — six-axis health radar + diff helper`; `e816e09 v2.0.0 Phase 7 runtime axis` | 9 | LOAD-BEARING | Threshold literals pinned; does NOT cover `composite: None` withholding (that is in test_git_history_coverage.py). |
| tests/test_health_radar_action.py | `render_comment.py` output text, and the Action YAML's base-checkout step contains no `--depth=1` (which shallowed a full clone and inflated the base grade 82.2 B vs 75.5 C). | `90a3ec4 release: v1.88.0 — health-radar GitHub Action`; `52d0791 fix(ci): health radar measured the base against one commit of history` | 9 | LOAD-BEARING | Text ratchet over `.github/actions/*/action.yml` via `yaml.safe_load`; loads renderer by file path; Practice 6 origin. |
| tests/test_hook_output_channels.py | Model-facing hook nudges go out via `additionalContext` on stdout JSON, never stderr/systemMessage; PreCompact registers transcript root (#421); Read-before-Edit stays advisory (#241). | `51c5e90 refactor(hooks): split cli/hooks.py into a package`; `2f92477 test(hooks): close the assertion gaps`; `1b97759 fix(hooks): external review round` | 6 | LOAD-BEARING | Asserts delivery CHANNEL not content; 24 tests. |
| tests/test_hook_steering_fixes.py | One class per confirmed defect from the "Claude forgets to call jCodeMunch" batch: Bash/Glob hooked, empty journal, symlink-blind root, stale matchers, Read nudge outside repo, surface-blind subagent catalog, and the #559 `symbols` key. | `2f92477 test(hooks): close the assertion gaps the steering batch's own audit named`; `1b97759`; `79e4d0d (#559-#562)` | 6 | LOAD-BEARING | 43 tests; uses `~/notes.txt` literal as the home-path veto input (intentional); cites #334/#406/#559. |
| tests/test_hooks.py | PreToolUse/PostToolUse/PreCompact/SubagentStart/TaskComplete handlers: JSON stdin parsing, advisory vs enforce modes (v1.108.47), settings.json merge, transcript-root learning (#421). | `51c5e90 refactor(hooks): split cli/hooks.py into a package`; `2f92477`; `1b97759` | 6 | LOAD-BEARING | 54 tests; one `skipif(sys.platform != "win32")`; overlaps test_hook_output_channels.py on PreCompact #421 but covers the broad handler surface. |
| tests/test_hotspots.py | `get_hotspots` ranks by complexity x churn, carries an `assessment` field, and does not crash on a non-git source root. | `072a49b release: v1.17.0 — Code Metrics & Health`; `3d500eb v1.19.0 assessment field`; `477abc4 v1.63.1 — fix get_hotspots UnboundLocalError on non-git source_root` | 1 | LOAD-BEARING | Non-git regression (v1.63.1) is the named past failure. |
| tests/test_html_file_class.py | `.html`/`.htm` index as zero-symbol source files so `flow_edges._resolve_template` resolves an indexed template instead of returning None. | `aee836b feat(languages): html as a text-searchable file class (zero symbols)`; docstring "jcm#452, HTML half" | 10 | LOAD-BEARING | Docstring names the one non-vacuous test (`test_resolve_template_resolves_indexed_html`). |
| tests/test_identity_mode.py | Default identity mode is `local` and spawns no git subprocess for a hosted clone; `git`/`config` modes select as documented. | `7fd8768 fix: restore local-first index identity default, make git identity opt-in (#295)` | 3 | LOAD-BEARING | Patches `config.get` with a lambda; 11 tests; complements test_git_root_identity.py. |
| tests/test_identity_normalized_tier.py | `identity_type` is `exact` only for a literal-name match; a fixture `state` no longer outranks class `_State` end-to-end through `search_symbols`. | `c297b2d fix(search): identity_type must not report exact for a normalised match (#458)` | 1 | LOAD-BEARING | Docstring notes helper-level tests would all pass against the broken ordering; the e2e test is the guard. |
| tests/test_incremental.py | `index_folder` incremental re-index returns early on no change, re-parses changed files, and does not crash when existing symbols are dicts (#198). | `56d34b8 cpp support`; `ea97859 feat: repo identity, full-file indexing (v1.1.0)`; `a4f2467 fix: full re-index crash dict has no attr summary (#198)` | 3 | LOAD-BEARING | Overlaps test_hardening.py::TestIncrementalReindex partially; #198 case is unique here. |
| tests/test_index_channel_tri_state.py | `verdict.channels.index` never reports `fresh` for `unknown`/`not_tracked`; `build_file_verdict`/`build_symbol_verdict` route through one `index_channel`. | `1f9d6dd release: v1.108.240 - fresh stops meaning "we could not find out"`; docstring cites #377 item 4, django/django reproduction | 9 | LOAD-BEARING | Parametrized over every tri-state reading; non-`fresh` assertions are the load-bearing half. |
| tests/test_index_dependency.py | `index_dependency` snapshots a node_modules/site-packages package and indexes it with version truth, dist-only dodge, identity safety, and honest-failure paths. | `eba3de7 v1.108.94: new tool index_dependency`; docstring cites `docs/prd-dependency-docs.md` | 1 | LOAD-BEARING | Cited PRD lives under gitignored `docs/` — verify it exists; 17 tests, fake layouts, no network. |
| tests/test_index_file.py | `index_file` errors on missing file / no matching index and updates a single file's symbols in place. | `16c7578 Mt file+optimize (#126)` | 3 | LOAD-BEARING | 8 tests; head-advance semantics live in the sibling below. |
| tests/test_index_file_head_advance.py | `index_file` advances stored `git_head` only when the corpus is shown to match it; #330's no-change advance in `index_folder` must not regress. | `499f52b fix(index_file): advance git_head only for a corpus shown to match it (#493)` | 3 | LOAD-BEARING | Real git via subprocess; 10 tests. |
| tests/test_init.py | `init` client detection, `_patch_mcp_config` backup/merge, CLAUDE.md/Cursor/Windsurf installs, hooks install, audit, and the client registry incl. Codex/opencode. | `e176e7a v1.21.26 Cursor/Windsurf rules + --demo`; `8c6f85b v1.108.35 fix CI-only break`; `c0a82da feat(init): register Codex CLI and opencode` | 6 | LOAD-BEARING | 44 tests; the largest init surface; per-client schema shapes are in test_init_client_schemas.py. |
| tests/test_init_client_schemas.py | Each host gets its own config shape (Codex TOML with resolved binary and never `uvx`, opencode `mcp` array command, VS Code `servers`, Gemini/Cline detection). | `c0a82da feat(init): register Codex CLI and opencode, each on its own schema`; `8c7a6d8 feat(init): register Gemini CLI, Cline and VS Code/Copilot` | 6 | LOAD-BEARING | Docstring: a wrong schema produces no error (silent no-tools); cites CLIENTS.md multi-hour hang. |
| tests/test_init_hooks_paths.py | Hook commands are written with forward slashes on Windows (bash eats backslashes) and re-running `init` never appends duplicate hooks (subcommand-keyed dedupe). | `105e5a1 fix: Windows hook path bash-escape eating + duplicate hook accumulation (v1.108.7)`; `9b375cd fix(hooks): ten confirmed defects` | 6 | LOAD-BEARING | Patches `_hook_invocation` to return literal `C:\Python314\...` and `C:\Program Files\...` paths (intentional inputs). |
| tests/test_init_minimal.py | `init --minimal` registers the MCP server and touches no other channel (no CLAUDE.md, rules, AGENTS.md, hooks, index, audit). | `aa4cb54 release: v1.108.22 — ... init --minimal ...`; `e3663c5 test: retire the src.jcodemunch_mcp package twin` | 6 | LOAD-BEARING | 3 tests, spies on every helper. |
| tests/test_init_policy_matches_surface.py | The policy `init` writes names only tools the advertised surface carries — after `load_config`, honouring `tool_surface` and profile — and `--minimal --hooks` installs hooks (#397). | `d7e3b17 release: v1.108.206 - init wrote a policy for a decision it had not made yet`; `054755d (#507)`; `9cf763e refactor: break three import cycles` | 4 | LOAD-BEARING | Header warns patches must target `cli.policy`, not `init` re-exports (monkeypatch trap). |
| tests/test_install_copilot_hooks.py | `init --copilot-hooks` writes `.github/hooks/hooks.json` with one postToolUse rule keyed by subcommand, idempotently. | `aede3b2 release: v1.81.0 — VS Code extension, Copilot hooks`; `1650270 test(hooks): assert the Copilot rule by subcommand, not by bare-name substring` | 6 | LOAD-BEARING | Asserts outcome not string shape (Windows `.EXE` absolute path). |
| tests/test_install_pack.py | `install-pack` lists/installs Starter Packs from the pinned host `jcodemunch.com` (never the retired `j.gravelle.us`), masks license keys, and reports body/manifest failures. | `9fcca86 v1.108.204`; `c769f25 v1.108.205 packs say whose code they contain`; `50c92bd v1.108.252`; header comment: legacy host served a 3-month-stale pack with every signal green | 6 | LOAD-BEARING | Host name pinned as a literal; `httpx` patched (N5-safe); 21 tests. |
| tests/test_install_pack_member_confinement.py | No archive member can write outside the install dir, asserted as confinement (not as refusal of a specific spelling); could-not-resolve is never a pass. | `f0e4e50 fix(install-pack): confine archive members by resolution, not by pattern (#447)`; docstring credits @elfrost / PR #443 | 8 | LOAD-BEARING | `skipif` non-win32 for drive-absolute names; `httpx` patched; Standing lesson: the test must own its target path. |
| tests/test_install_uninstall.py | `install`/`uninstall`/`install-status` round-trip: policy blocks stripped, user content preserved, hooks stripped, `claude mcp list` counted as configured. | `f2d4536 v1.108.33 Claude Code CLI on Windows`; `cf4dcb8 v1.108.88 Antigravity`; `7189258 v1.108.89 revert Antigravity` | 6 | LOAD-BEARING | Line 276 regression: bare `["claude"]` raised; 25 tests. |
| tests/test_integrity_check_cost.py | `verify_package_integrity()` settles the ordinary install without calling `packages_distributions()` (3.35 s on 894 names), while the map stays reachable when the official dist is absent. | `0e1e9db perf(cli): stop enumerating every distribution to clear the integrity check` | 5 | LOAD-BEARING | Asserts COST as a property (call count on the expensive map), not call order; 5 tests. |
| tests/test_investigator_deletion_safety.py | `investigate_deletion_safety` yields SATISFIED/REFUTED/UNESTABLISHED obligations for the three 2026-03-18 dead-code A/B gaps; Gap 2 (dead export in a live file) is the load-bearing case. | `55645f2 release: v1.108.214 — deletion safety as proof obligations, not a score`; docstring cites `benchmarks/ab-test-dead-code-2026-03-18.md` | 1 | LOAD-BEARING | Line 191 regression: pre-v1.8.1 extractor missed a call form, four live views read dead; 19 tests. |
| tests/test_issue_456_perf_correlation.py | Perf-table rows carry `session_uid`/correlation keys, written through the real dispatcher when `perf_telemetry_enabled`. | `ce08887`/`cc1ecbb`/`829f61b (#456)` refactor + drive through the real dispatcher | 9 | LOAD-BEARING | Imports `httpx` at line 299 (in-process transport, not network); docstring warns about the developer's real `~/.code-index`. |
| tests/test_js_class_field_phantom_methods.py | A JS/TS class field initializer's object-literal shorthand is not a class method, and a phantom no longer shifts a real method's `~N` disambiguated id. | `978e614 release: v1.108.246 - a class field initializer no longer donates members to its class` | 1 | LOAD-BEARING | Bucket B (object literal inside a function) deliberately kept; 9 tests. |
| tests/test_json.py | `.json` maps to `json` language (openapi/swagger compound extensions win), and top-level keys extract as symbols. | `8f886f2 feat: add JSON indexing and top-level key extraction (closes #197 follow-up)`; `9f6c8d3 test: run the suite in parallel under pytest-xdist` | 10 | LOAD-BEARING | Header warns: import via `jcodemunch_mcp` never `src.jcodemunch_mcp` (config twin reads real `~/.code-index`). |
| tests/test_key_files_split.py | Every Key Files entry lives in exactly one of CLAUDE.md / KEY-FILES.md, keyed by full path not basename, with the 15 marker-less `RATIONALE_ENTRIES` staying in CLAUDE.md. | `7e4ac9c release: v1.108.310`; docstring: split 2026-08-29, 44.4% of a 140k budget (Practice 5) | N6 | LOAD-BEARING | Text ratchet over two committed markdown files; its first run caught its own basename-collision defect. |
| tests/test_kind_enum_is_derived.py | The wire `kind` enum in `server.py` is derived from `KIND_ORDER` (AST scan finds no literal list), equals `VALID_KINDS`, and has stable order (cached prefix). | `ef259ce fix: search_symbols(kind="field") was refused by both gates (#571)`; credits @devtomnl | 4 | LOAD-BEARING | Source-text ratchet via `ast.parse(server.py)`; 7 tests. |
| tests/test_languages.py | `parse_file` extracts the expected symbols/kinds/docstrings for ~30 languages from inline snippets (JS, Python, TOML dotted keys, Astro frontmatter, ...). | Many; latest `a6d84ee feat: Add TOML symbol extraction support`, `f0eda7b fix(toml): keep every segment of a dotted key`, `d1aa0e5 Astro` | 10 | LOAD-BEARING | 71 tests; overlaps test_hardening.py::TestPerLanguageExtraction (fixture-file based) but inline snippets differ — keep both. |
| tests/test_laravel_provider.py | `LaravelContextProvider` detects a Laravel skeleton, parses routes/models/migrations, Blade `@include`/`@includeFirst` imports, and Inertia page specifiers. | `aef7078 feat: PSR-4 resolution, Laravel provider (#201)`; `dd0653f Stages 4-6 Inertia bridge (#201)`; `421a8b5 fix: @includeFirst — closes #203` | 10 | LOAD-BEARING | 61 tests; #203 regression. |
| tests/test_license_identifier_agreement.py | `pyproject.toml`, `plugin.json` and LICENSE all name one `LicenseRef-` identifier keyed to LICENSE's major version, with the terms pinned by sha256 of the normalised (LF) text. | `40fab9d fix(license): key the identifier to the MAJOR version only`; `a9857b2 fix(license): digest the normalised terms, not the raw bytes`; docstring #517 (@marcelruhf) | 7 | LOAD-BEARING | Byte-level pin (sha256 hexdigest literal) over LICENSE; a substantive edit must bump the identifier. |
| tests/test_list_repos.py | `repos_report` rows carry the cockpit fields (`source_root`, `freshness`, `watcher_state`, `lock_holder`) and freshness is tri-state after #565. | `302b138 v1.108.36 list-repos --json`; `464318c v1.108.37 source_root`; `d0223c4 fix: get_watch_status reported watcher bookkeeping as index freshness (#565)` | 9 | LOAD-BEARING | Single test; shape check only. |
| tests/test_local_encoder.py | Bundled ONNX local encoder: WordPiece tokenizer, provider priority 0, `embed_texts` routing, `download-model` CLI, and normalised `encode_batch` output with ONNX mocked. | `3e07d8a feat: Phase 3 competitive upgrade — bundled ONNX local encoder` | 1 | LOAD-BEARING | `importorskip("numpy")` on one test; no network (download mocked); 25 tests. |
| tests/test_local_integration.py | End-to-end SQLite WAL backend: -wal/-shm appear after first write, incremental save writes a fraction of a full rewrite, touch-only mtime is re-persisted so the next cycle hits, a second connection sees committed writes, checkpoint_and_close truncates the WAL. | `2ff57a8 fix: report unloadable repo indexes truthfully (#291)`; `cacafd7 fix: prevent data loss on invalidate_cache for unmigrated JSON indexes`; docstring lists five WAL scenarios | 3 | LOAD-BEARING | Multi-process consistency via two sqlite connections in one process; asserts WAL byte sizes (delta write "fraction" of full) so it's sensitive to storage layout. |
| tests/test_lockfile_version_sync.py | The `version` under `name = "jcodemunch-mcp"` in `uv.lock` equals `pyproject.toml`'s version. | `fbd6fde test: gate the uv.lock version bump instead of documenting it`; docstring: "It has happened twice (v1.108.200, v1.108.203)" -- `uv sync --locked` fails every CI job at install | 7 | LOAD-BEARING | Ratchet over repo files (`read_text` on uv.lock + pyproject); one of the five version-pin sites. |
| tests/test_lsp_bridge.py | LSP JSON-RPC encode/decode, LSPServer lifecycle with a mocked subprocess, graceful degradation when no server, `lsp_resolved` call-graph edges, config helpers. | `889032b feat: Phase 4 competitive upgrade -- LSP bridge for compiler-grade call graph resolution`; docstring | 1 | LOAD-BEARING | Mock subprocess only, no real language server spawned; feature-introduction tests rather than a named regression. |
| tests/test_luau_language.py | Luau (.luau) parsing extracts services, exported types, methods and functions from a Roblox-style fixture. | `51760c2 feat: add Luau (Roblox) language support (#91)` | 10 | LOAD-BEARING | Single fixture (`RaidService`); feature-introduction test for #91. |
| tests/test_mcp_instructions.py | The MCP `initialize` `instructions` string names only tools the server actually dispatches on each surface, stays under `_MCP_INSTRUCTIONS_MAX_CHARS`, and every `server.run()` in server.py passes `_initialization_options()` rather than a bare `create_initialization_options()`. | `e2de718 feat(mcp): the one string that survives tool deferral`; docstring: "Until v1.108.292 we sent none" | 4 | LOAD-BEARING | Ratchet: parses dispatcher source via `inspect.getsource`/AST; 1,000-char budget pin; also pins `Server(..., version=__version__)`. |
| tests/test_meta_disclosure.py | All six analytical tools emit `_meta.methodology` and `_meta.confidence_level` in {low,medium,high}. | `b4db152 release: v1.21.19 -- Phase 5 uncertainty disclosure (T15-T18)`; docstring "T15" | 9 | LOAD-BEARING | Uses `inspect.getsource` on one tool; git-dependent tools asserted for field presence only. |
| tests/test_migration.py | A legacy JSON index migrates to SQLite with all symbols/imports/metadata preserved and slug sanitization correct. | `cfd6793 feat: implement JSON->SQLite migration`; `7df0d72 fix: correct SQLite slug sanitization and preserve hashes on incremental updates` | 7 | LOAD-BEARING | Hand-written legacy JSON fixture at `index_version: 4`; guards the one-way migration path old installs still take. |
| tests/test_missing_content_cache_is_reported.py | `get_symbol_source` on an index whose content directory is absent reports `source_status` rather than `source: ""` under an `ok` verdict. | `9fcca86 release: v1.108.204 - report a body the index cannot produce`; docstring: measured on the `nodejs` pack 2026-07-30, 0 of 50 bodies, all reported confidently | 9 | LOAD-BEARING | Builds a real index then removes the content dir beside the .db (starter-pack shape); freshness half lives in `test_v1_108_209.py`. |
| tests/test_mtime_optimization.py | Full index persists `file_mtimes`; incremental re-index skips unchanged-mtime files and re-hashes touched ones. | `16c7578 Mt file+optimize (#126)`; `7df0d72 ... preserve hashes on incremental updates` | 3 | LOAD-BEARING | Overlaps scenario 3 of test_local_integration.py (mtime stickiness) but covers the skip path itself; keep. |
| tests/test_negative_evidence.py | An empty `search_symbols` result carries `negative_evidence` (verdict, scanned counts, best_match_score) and it is absent on hits. | `32eb018 feat: ... negative evidence ... (#218)`; `759537f fix: prevent test index folders from leaking into ~/.code-index/ (#222)`; later `_meta.verdict` releases .116/.117 | 3 | LOAD-BEARING | Absence-claim surface; #222 fixed this file leaking indexes into the real store. |
| tests/test_nesting_depth_channels.py | `max_nesting` is `max(bracket, indent)`: Python control flow raises depth, brace/minified code keeps the bracket answer, and `_BLOCK_OPENER_RE` source holds no stray control characters. | `d069b4e fix(complexity): max_nesting could not see Python control flow (PARSER_GENERATION 5 -> 6)`; docstring: brackets said 3, AST says 6 on `index_folder` | 1 | LOAD-BEARING | Ratchet: `read_text` on complexity.py scanning for a literal 0x08 that once replaced the backslash-b escape and compiled. |
| tests/test_new_languages.py | Extension mapping and symbol extraction for the v1.42.0 batch (Pascal, MATLAB, Ada, COBOL, Common Lisp, Solidity, Zig, PowerShell, Apex, OCaml) plus PL/SQL extensions. | `a5df17d Add 11 new languages: Pascal, MATLAB, ...`; docstring "v1.42.0" | 10 | LOAD-BEARING | Docstring says "10 new" while commit says 11 (PL/SQL is an extension mapping only) -- cosmetic. |
| tests/test_new_languages_v143.py | Extension mapping and symbol extraction for F#, Clojure, Emacs Lisp, Nim, Tcl, D. | `ba817a4 Add 6 new languages: F#, Clojure, Emacs Lisp, Nim, Tcl, D (TIOBE 51-100)` | 10 | LOAD-BEARING | Per-language fixture strings; release-numbered filename. |
| tests/test_next_entry_points.py | Framework-declared entry points from `context_metadata` reach `find_dead_code`, `get_dead_code_v2` and the coupling instability ratio on a Next.js layout on disk; a manifest is never dead and a refusal is not a zero. | `79e4d0d fix: a count taken after the page, and a field nobody read (#559-#562)`; docstring #561/#562 @lilubot | 1 | LOAD-BEARING | Deliberately asserts against a disk layout, not the profile constant (Practice 9). |
| tests/test_nextjs_provider.py | Next.js provider route-path conversion, API method detection, `src/` layout detection (create-next-app --src-dir), root `pages/` vs `src/app` precedence, and `layer_definitions` mirroring `src/`. | `bde6e19 fix(nextjs): detect src/app App Router layouts`; `06bc7bb fix(nextjs): root pages/ blocks src/app`; `93e6046 fix(nextjs): root app/ or pages/ ignores src/middleware too`; refs #432, #433 | 1 | LOAD-BEARING | Three consecutive fixes in the same provider within three days; each has its own test class. |
| tests/test_nuxt_provider.py | Nuxt provider route/API-route path conversion and project detection on the Nuxt 3 root layout. | `dd0653f feat: Stages 4-6 -- Inertia bridge, Vue/Nuxt/Next.js providers ... (#201)` | 1 | LOAD-BEARING | test_nuxt_srcdir.py's docstring notes this file "cannot fail" on the Nuxt 4 class; still covers the Nuxt 3 shape. |
| tests/test_nuxt_srcdir.py | Nuxt 4 `app/` srcDir is probed for pages/API routes (#434), and a sweep asserts no framework profile carries a TS-only glob without a JS twin, with `next` exempted by name and reason (#435). | `fa17065 fix(nuxt): a stock Nuxt 4 project is not an empty one`; `17ff8c4 test(profiles): exempt next by name with a ratchet`; docstring #434/#435 | 1 | LOAD-BEARING | Ratchet over every profile's globs; named-exemption list with a reason (#428 lesson). |
| tests/test_observatory.py | Observatory pipeline rendering/state half: repo slug parsing, history state, skipped-section rendering, and index.json embedding `generator_version` + `index_version` (v1.96.1). | `66bb6c9 release: v1.90.0 -- OSS code-health observatory pipeline`; `b241a9e release: v1.96.1 - self-describing observatory index.json` | 9 | LOAD-BEARING | Clone half intentionally untested (needs network); reads rendered files via `read_text`. |
| tests/test_observatory_clone_depth.py | `clone_or_update` no longer passes `--depth=1`, so the observatory clones enough history to score `churn_surface`. | `7e9ed3a fix(observatory): score against the churn window, not one commit`; docstring: depth=1 scored 81.3 (B) vs full 75.6 (C) on jcm | 9 | LOAD-BEARING | Ratchet via `inspect.getsource` on the clone function; sibling of Practice 6's health-radar Action guard. |
| tests/test_offload.py | `retrieval/offload.classify` marks work offloadable only when every disqualifier is clear; each disqualifier is shown to fire on a spoiled shape; `shape_from_response` maps real tool responses. | `d461674 release: v1.108.241 - offloadable-work annotation, off by default`; docstring: "The load-bearing tests here are the negative ones" | 9 | LOAD-BEARING | 700+ lines; one `read_text` scan near the end. |
| tests/test_offload_contract.py | `offload.CONTRACT_DIGEST == "3d4cf47e24fac358"` and every observable part of the annotation contract matches the suite-wide pinned literal. | `d461674 release: v1.108.241 ...`; docstring: GENERATED from `C:\MCPs\_private\offload_contract_test_template.py`, identical in jcm/jdoc/jdata; v1.108.240 shipped three drifted renderings | 7 | LOAD-BEARING | Byte-level digest pin; generated file (do not hand-edit); machine-specific `C:\MCPs\_private` path appears in the docstring only. |
| tests/test_optional_dep_skips_are_visible.py | No test module under `tests/` uses module-scope `pytest.importorskip`, so a missing optional dependency skips tests individually instead of collapsing a file to `1 skipped`. | `9661e0b test: ten skipped modules that hid 209 tests between them`; docstring: 209 tests behind ten guards, three partway down their file | N7 | LOAD-BEARING | Ratchet: `rglob("test_*.py")` over the tests tree scanning source text. |
| tests/test_org_http.py | `POST /org/report` accepts seat reports only with the two-key turn (enabled + token), rejects unauthenticated and oversized bodies, and aggregates via `org_rollup`. | `f85c860 release: v1.108.39 -- org rollup HTTP transport`; `e6a35fc v1.108.73: Phase 2 harden -- HTTP ingest fails closed without a token` | 8 | LOAD-BEARING | `skipif` via `find_spec("starlette")` (per-test skip, not module abort); Starlette TestClient, no sockets. |
| tests/test_org_license.py | The org-rollup license gate: key masking, Studio/Platform tiers unlock, Builder does not, sticky-offline cache, and `key_valid` separate from entitlement (#364). | `b3f78b7 release: v1.108.42 -- org-rollup license gate`; `2b7bea7 v1.108.43 -- requires a multi-seat tier`; `7222ec2 v1.108.122 -- license CLI stops calling a valid license "unlicensed" (#364)` | 6 | LOAD-BEARING | `_check_server` monkeypatched, no network; autouse fixture isolates CODE_INDEX_PATH and env key. |
| tests/test_org_rollup.py | `record_seat_report`/`org_rollup` aggregate seats, sort by tokens, and upsert same-seat-same-day rows. | `513d96b release: v1.108.38 -- org rollup (team SKU): per-seat savings aggregation` | 9 | LOAD-BEARING | Small store-level unit tests; tmp_path only. |
| tests/test_pack_api_transport.py | `_pack_api_headers` sends the shape the pack API expects (license header, user agent), the keyless download path works for free packs, and failure messages do not relay advice the user already followed. | `8c6515a release: v1.108.248 -- starter packs install again, and stop misreporting why they did not`; docstring #417/#418 @MotoMato85 | 6 | LOAD-BEARING | `httpx` mocked at transport level; docstring says both underlying defects are outside this repo (CDN + index.php), so only request shape and wording are pinned. |
| tests/test_pack_index_survives_orphan_sweep.py | `cleanup_orphan_indexes()` does not delete an installed starter-pack index whose `meta.source_root` is the builder's absent clone path, and `install-pack` rewrites that root. | `2ae9e5a release: v1.108.251 -- every server start silently deleted your installed starter packs`; docstring #419 @MotoMato85 | 3 | LOAD-BEARING | Builds a pack .db with a foreign `C:\`-style source_root as the fixture; docstring warns the maintainer's box was structurally immune. |
| tests/test_pack_marker_not_a_repo_index.py | `IndexStore.list_repos` ignores `.pack-<id>.json` markers instead of reporting them as corrupt repo indexes with a delete-this-file hint. | `8c6515a release: v1.108.248 ...`; docstring #417 finding 2: `pathlib.glob` matches dotfiles | 6 | LOAD-BEARING | Guards actively harmful advice (deleting the marker); tmp markers only. |
| tests/test_pagerank.py | `compute_pagerank` scores sum to 1, respect edge direction/convergence, and `search_symbols` ranking integrates centrality. | `c0ad048 feat: PageRank / centrality ranking (v1.11.12)`; docstring "Feature 2 from upgrades-PRD.md" | 1 | LOAD-BEARING | Cites a PRD document; verify it still exists before relying on the citation (cf. the phantom PRD noted for get_endpoint_impact). |
| tests/test_paid_embeddings_optin.py | `_detect_provider` never selects a paid cloud embedding provider from an API key alone -- the explicit `*_EMBED_MODEL` is the opt-in and the local ONNX encoder wins at priority 0. | `2971bb2 test: pin that a bare cloud key cannot select a paid embedding provider`; docstring: suite-parity check for a jdocmunch defect, deliberately NOT a port of its gate | 8 | LOAD-BEARING | Pins a property jcm already had so a "simplification" cannot remove it; env-only, no network. |
| tests/test_parity_map.py | `get_parity_map` classifies ported/renamed/diverged/added/orphaned symbols across two scopes, computes `parity_pct`, orders the port plan topologically with SCC grouping, errors on same scope/unindexed/empty, and is read-only. | `07bbb0c get_parity_map: correspondence-aware migration parity map (v1.108.111)`; docstring | 1 | LOAD-BEARING | Fixture is a legacy/ vs v2/ tree with commented expected outcomes per symbol. |
| tests/test_parse_cache.py | Content-addressed parse cache is a passthrough when disabled, returns identical symbols on hit, and FIFO-evicts past `DEFAULT_MAX_ROWS`. | `c57b070 release: v1.108.40 -- content-addressed parse cache for shared-host indexing`; `444925e v1.108.41 -- bound the shared parse cache (FIFO eviction)` | 3 | LOAD-BEARING | Env-gated by `JCODEMUNCH_PARSE_CACHE`; compares `dataclasses.asdict` of symbols. |
| tests/test_parse_warnings.py | `index_folder` reports `missing_extractors` for a language with symbols but no import extractor (Elixir), and `get_dead_code_v2` emits `framework_warning` when no entry points are found. | `b4db152 release: v1.21.19 -- Phase 5 uncertainty disclosure (T15-T18)`; docstring T17/T18 | 9 | LOAD-BEARING | Elixir is the representative gap; if an Elixir import extractor is added this fixture language must change. |
| tests/test_parser.py | Core `parse_file` extraction for Python plus Lua, JS/TS const constants, and Astro frontmatter/components. | `ac91053 feat: add Lua language support (v1.0.1)`; `c984804 feat: extract JS/TS const declarations as constants`; `d1aa0e5 feat(parser): enhance Astro support ... regression tests` | 1 | LOAD-BEARING | Phase 1 file that accreted language cases; one `read_text` on a fixture. |
| tests/test_parser_generation_upgrade.py | An index stamped with an older `PARSER_GENERATION` takes one forced full re-parse on `incremental=True`, reported via `performed_incremental: false` + `rebuild_reason`, and stores correct names after. | `1110312 release: v1.108.244 - byte offsets sliced a decoded str in 16 extractors (#414)`; docstring #413/#414 | 3 | LOAD-BEARING | Fixture is a PowerShell file with five U+00E4 chars to shift byte offsets -- the bug report's shape. |
| tests/test_path_entry_point_invariants.py | Every entry point taking a filesystem path resolves a nested independent repo to itself (not the enclosing parent) and loads the project overlay so `repo=` is not inert. | `a2bd093 fix(index_file): establish repo identity and load the project overlay (#509, #508)`; docstring #491/#492/#508/#509 | 6 | LOAD-BEARING | Ratchet over multiple entry points ("fixing the reported call site leaves the mechanism"). |
| tests/test_path_map.py | `JCODEMUNCH_PATH_MAP` parsing (pairs, last-`=` split, whitespace) and `remap()` returning the path unchanged on no match, including Windows and macOS spellings. | `d04c111 feat: reverse-remap folder_path before repo hash in watcher`; `735137f fix(path_map): address PR review issues 2-6`; `972e007 fix: remap() returns path unchanged on no-match` | 6 | LOAD-BEARING | Literal `C:\` and `/Users/` inputs are test data, not machine dependencies. |
| tests/test_perf_db_path_resolution.py | `_perf_db_path` returns a resolved path so aliases and relative spellings of one store share one cached connection and `record_latency` writes to the right database after a chdir. | `4dc1cc5 fix(telemetry): key the perf db connection cache on the resolved path`; docstring cites v1.108.188 writer fix | 9 | LOAD-BEARING | Changes CWD inside a test -- order-sensitive if run with a shared CWD. |
| tests/test_perf_telemetry.py | Latency ring records per-tool p50/p95/max/error_rate, the opt-in SQLite sink persists rows, and `analyze_perf` reads both. | `d99f900 release: v1.74.0 -- telemetry foundation` | 9 | LOAD-BEARING | Autouse fixture swaps in a fresh `_State`. |
| tests/test_perf_trim_is_per_database.py | `_persist_perf_locked` keeps a per-database rows-since-trim counter so one store's writes never trigger another's trim (#476). | `9cf7c4f fix(telemetry): trim each perf database on its own row count`; docstring #476 | 9 | LOAD-BEARING | Asserts on the counter map, deliberately not on 1000-row trims. |
| tests/test_pid_reuse_identity.py | `register()`/`acquire()` record process creation time and readers treat PID-alive-but-create-time-mismatched as dead; rows without `create_time` keep liveness-only behaviour. | `1c7fa62 fix(storage): bind process creation time against PID reuse (#450)`; docstring jcm#450 (two-week-old rows resolving to a Chrome renderer) | 3 | LOAD-BEARING | `pytest.skip` on platforms other than win32/linux (macOS skips 4 tests); Linux create_time is boot-relative. |
| tests/test_plan_refactoring.py | `plan_refactoring` rename/move/extract planning: symbol resolution, word replacement, collision checks, import rewrites, non-code file scans, across every supported language including templating overlays and zero-symbol html. | `a063262 ... plan_refactoring full language coverage ... test parametrization (330->78) (#244)`; `f405bee ... templating-language files (#336)`; jcm#452 html comments | 1 | LOAD-BEARING | 82 test functions, ~1900 lines; one `~/`-style literal at line 1102 is test data. |
| tests/test_plan_turn.py | `plan_turn` returns high/medium confidence with recommended symbols for an existing name and low confidence with `gap_analysis` for a nonexistent one. | `3ad1c16 fix: expose short error summaries and stabilize plan_turn`; `3878e04 fix: restore exact search identity ...` | 1 | LOAD-BEARING | Uses `create_mini_index` helper. |
| tests/test_plugin_manifest_sync.py | `.claude-plugin/plugin.json` version and MCP package name match pyproject.toml, and no root `.mcp.json` exists. | `4c76d3a feat: add Claude Code plugin manifest and jmunch marketplace`; docstring: "same failure server.json had for five months" | 7 | LOAD-BEARING | Version-pin ratchet reading repo files; one of the five pin sites. |
| tests/test_post_tool_use_hook.py | `_enforcement_hooks()` emits PostToolUse (Edit/Write) and PreToolUse (Read) rules invoking `jcodemunch-mcp` by absolute path, and the hook handler routes session-aware suggestions. | `2fb56d8 feat: session-aware routing -- 10 features`; `127930a v1.80.5 -- fix hooks failing on macOS/Linux due to /bin/sh PATH`; `8d141ab opt-in strict enforcement (v1.108.48)` | 6 | LOAD-BEARING | spawns nothing; hook rules only |
| tests/test_process_code_freshness.py | `install_layout` is the one authority for the src-layout rule (no other module re-derives it), `newest_source_mtime`/`started_at` answer whether a running process serves current code, and the registry `version` field is not used for that. | `d9851b5 release: v1.108.313 -- an install created before a default can never learn there is a choice`; docstring: version is frozen in `.dist-info` | 6 | LOAD-BEARING | Ratchet: `read_text` scan of src for a reintroduced src-rule copy, run against the reintroduced defect. |
| tests/test_process_locks.py | Generic `(scope, target)` lock API: acquire/release/inspect/held, stale-PID takeover, metadata fields, and a pinned interleaving for the contended case. | `a186255 release: v1.106.0 -- multi-process shared-index coordination`; `5328179 test(locks): pin the interleaving, not the clock`; docstring | 3 | LOAD-BEARING | Threads with explicit events per the 08-17 #490 lesson; watcher-specific half in test_watcher_lock.py. |
| tests/test_progress.py | `ProgressReporter` is a no-op without notify, emits 0% on start, monotonic updates, flood control, and `make_progress_notify` bridges to the MCP session. | `d1929a3 release: v1.34.0 -- MCP progress notifications`; `08e0129 v1.108.92 -- progress-notification flood control + response drain` | 5 | LOAD-BEARING | HeartbeatReporter (#383) is tested elsewhere (test_v1_108_189 family). |
| tests/test_project_intel.py | `get_project_intel` parsers for Dockerfile, compose, k8s, CI configs (GH Actions, GitLab, CircleCI), Makefile, package/pyproject scripts, env templates, and fuzzy cross-referencing. | `ef9f08e Add get_project_intel tool`; `a063262 ... (#244)` | 1 | LOAD-BEARING | Per-parser unit tests on inline fixtures. |
| tests/test_prompts.py | `list_prompts` returns exactly the five prompts {workflow, explore, assess, triage, trace} with non-empty descriptions and `get_prompt` renders each. | `3487aa1 feat: add 4 guided workflow prompts (explore, assess, triage, trace)` | 4 | LOAD-BEARING | Hard-coded count of 5 -- adding a prompt requires editing this test. |
| tests/test_property_based.py | Hypothesis invariants: symbol IDs unique per index, re-indexing identical files is idempotent, no file imports itself. | `e2f4342 release: v1.21.20 -- Phase 6 (Extend & Harden)`; docstring T22 | 1 | LOAD-BEARING | `max_examples=40`, `deadline=None`, manages its own temp dirs; heavier than average (N1 relevant). |
| tests/test_provenance.py | Every registry confidence value equals the live constant, `measured` entries equal `benchmarks/provenance/measured.json`, measured.json matches the artifacts it cites, and live responses validate against `schemas/`. | `981a74f release: v1.108.138 -- confidence provenance: every number states its basis` | 9 | LOAD-BEARING | Cross-artifact sync ratchet; Practice 4 notes it caught a fourth-artifact drift inside the local-ONNX failure set. |
| tests/test_provider_metadata_and_perf.py | Context-provider metadata merging, SQL stem cache keyed by frozenset, and the perf optimisations from PR #96 review. | `832f7a4 Address PR #96 review feedback + optimizations`; `7d86872 fix(tests): align TestSqlStemCache with frozenset-keyed dict cache`; `9661e0b` moved its importorskip | 5 | LOAD-BEARING | `importorskip` at line 126 is mid-file but now per-test (after 9661e0b); docstring is a PR-review reference, not an invariant statement. |
| tests/test_psr4.py | `build_psr4_map` reads composer.json autoload, `resolve_php_namespace` maps namespaces to files, and `resolve_specifier` resolves PHP `use` imports. | `aef7078 feat: PSR-4 resolution, Laravel provider, framework profiles (#201 Stage 1-2-3)` | 1 | LOAD-BEARING | Racket collection resolution (test_racket_collections.py) is modelled on this. |
| tests/test_pulse.py | `write_pulse` writes `_pulse.json` only under `JCODEMUNCH_EVENT_LOG=1` with tool/tokens/timestamps, and is a no-op otherwise. | `623924a fix: improve summarizer diagnostics + add per-call pulse signal (#224, #225)` | 9 | LOAD-BEARING | Env-gated side-effect; tmp_path base. |
| tests/test_python_relative_imports.py | `from ..pkg.mod import x` builds an import-graph edge through a multi-level package fixture, and a callers answer discloses unresolved specifiers (#423). | `28e20a4 release: v1.108.254 -- Python package-relative imports built no graph edge at all`; docstring: 71 of 818 resolved (8.7%), @faxik | 1 | LOAD-BEARING | Deep fixture by design ("a flat fixture cannot fail this class"); also pins the #284 dotted-basename TS rule. |
| tests/test_python_sibling_imports.py | `from . import sibling` and `from ..pkg import mod` both emit a per-name specifier so the sibling edge exists; a repo-level scan counts synthesised edges against the import statements actually written. | `923c3d6 fix: four import-graph and call-outcome defects (#550, #551, #552)`; `da20587 fix: #550 was fixed for one spelling of the defect (#566)` | 1 | LOAD-BEARING | Ratchet rewritten at #566 (its original passed against the defect); `read_text` scan of src with a floor of 87 measured edges. |
| tests/test_racket_collections.py | `build_racket_collection_map` reads `info.rkt` `collection` declarations and `augment_racket_collection_edges` adds edges that `resolve_specifier` can resolve. | `21df0a1 fix(racket): resolve collection paths through info.rkt`; docstring: splitflap 0/70, congame 147 unresolved, #548 symptom | 10 | LOAD-BEARING | Goes through `resolve_specifier` deliberately. |
| tests/test_racket_config_reparse.py | Changing `racket_definition_forms`/`racket_langs` stamps a config digest and forces exactly one full re-parse through `index_folder` with its own `rebuild_reason`; an unstamped Racket index re-parses once. | `692f619 fix(racket): a config change re-parses unchanged files, once`; `48c14c1 chore(racket): an unstamped Racket index re-parses once`; docstring cites #508 shape | 3 | LOAD-BEARING | Every test goes through `index_folder`, not the parser. |
| tests/test_racket_fidelity.py | Racket extractor scored against a FROZEN expander oracle (`tests/fixtures/racket_oracle.json`): `extra` and `wrong_span` must be 0, `missing` pinned to [] since gen 5. | `8010e11 feat(racket): read .rkt with the Racket reader`; docstring: "the buckets that must never be non-zero are checked everywhere, CI included" | 1 | LOAD-BEARING | Frozen-artifact gate; classifier imported from `benchmarks/racket_fidelity/` via importlib so one bucket definition exists. |
| tests/test_racket_fidelity_artifacts.py | Every prose copy (LANGUAGE_SUPPORT.md, benchmark README) of the Racket coverage figure and its rows matches `benchmarks/racket_fidelity/results.json`. | `72ef602 docs: bind the Racket coverage figure to the artifact that produces it`; docstring cites #549 (86.2 -> 86.5 drift) and the 1.108.298 five-of-eight artifact drift | N4 | LOAD-BEARING | Ratchet over prose text with regex; derives `clean_files`/`top_10_missing` from `per_file` rather than a summary field. |
| tests/test_racket_lang_gate.py | The `#lang` line selects a reader mode (sexp / at-exp with a command char) or refuses a document entirely, so `punct`/`scribble` files emit 0 symbols and `conscript`/`pollen`/`rosette` parse correctly. | `b21c0cc fix(racket): #lang rosette is S-expressions`; docstring: measured 39% found + ~100 fabricated on 207 conscript files before the gate | 1 | LOAD-BEARING | Mostly ABSENCE tests (what must NOT be emitted). |
| tests/test_racket_language.py | Racket extraction end-to-end: define/struct/module+/provide forms, import edges, and a large set of absence tests that non-definitions are not emitted. | `c3a350a refactor(racket): require edges come from the reader`; docstring cites #411 (config isolation, practice #8) and a #548 regression note at L1067 | 1, 10 | LOAD-BEARING | 109 tests; autouse fixture forces all languages enabled so `~/.code-index/config.jsonc` cannot leak in. |
| tests/test_racket_reader.py | The Python Racket reader's node surface, spans, at-exp and error resync, plus every `read-syntax` byte span from the frozen `racket_reader_oracle.json`. | `3aa1c65 fix(tests): pin the Racket fixtures to LF; the frozen reader oracle is byte positions`; `834d4db feat(racket): a Racket reader in Python` | 1 | LOAD-BEARING | Byte-position pin; fixtures must stay LF (line-ending sensitive). |
| tests/test_rails_provider.py | `RailsProvider.detect` needs both Gemfile and routes.rb, and route/model parsing yields the expected context entries. | `32eb018 feat: AST call graph, decorator awareness, negative evidence, framework providers (#218)` | 1 | LOAD-BEARING | Plain provider unit tests; no issue number beyond #218. |
| tests/test_ranking_ledger.py | The v1.78.0 `ranking_events` ledger writes rows only when enabled, with the expected feature columns, via `extract_ledger_features`. | `4c3b337 release: v1.78.0 — ranking ledger (data-collection only)` | 9 | LOAD-BEARING | Patches `token_tracker._state` to tmp_path; monkeypatches `config.get`. |
| tests/test_rate_limit.py | `_make_rate_limit_middleware` returns None at limit 0 and the sliding-window bucket enforces the per-IP limit under Starlette. | `94f9ed2 release: v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)` | 8 | LOAD-BEARING | Seven `pytest.importorskip("starlette")` sites; contributes to the skip count when starlette is absent (N7). |
| tests/test_razor.py | `.cshtml`/`.razor` mixed-language extraction: `@functions`/`@code` become C# symbols, `@page`/`@inject` constants, HTML ids, and discovery picks the extensions up. | `c440df9 feat: add Razor .cshtml mixed-language parsing`; `26604d5 Adding .razor (blazor) support` | 10 | LOAD-BEARING | Three checked-in fixtures including a public dotnet/aspnetcore layout. |
| tests/test_readonly_annotations.py | Every tool advertises `readOnlyHint`, and the non-read-only set covers `counter.STATE_CHANGING_ACTIONS` plus index_dependency/order/route so no mutating tool can masquerade as read-only in plan mode. | `a5f1f50 feat: advertise MCP readOnlyHint annotations`; `02d9b31 v1.108.104 — close the index_dependency gap surfaced by PR #361`; docstring: "regression guard for that fix" | 4, 6 | LOAD-BEARING | Write-set derived from the authority, not a literal. |
| tests/test_receipt.py | `receipt` CLI aggregation, dollar math, price table (Sonnet at its actual rate), model-choice derivation, explain/csv/json renders, lifetime meter. | `b3f4544 fix: the receipt priced Sonnet at a rate that was never charged`; L347 cites the v1.108.131 `fable` regression | 9 | LOAD-BEARING | Pins `_MODEL_PRICES_USD_PER_MTOK` literals; re-read the vendor source when touching (CLAUDE.md 1.108.314). |
| tests/test_redact.py | Response-level secret redaction: bearer token header-anchored (F1), depth cap collapses scalars (F2), `_meta` is scanned (F6), entropy helper. | `b920913 release: v1.59.1 — audit remediation (F1–F6)`; `338235d hardening(redact): require Authorization anchor` | 8 | LOAD-BEARING | Audit-finding numbers F1/F2/F6 in docstring. |
| tests/test_refresh_campaign.py | `refresh` slices an indexed repo without escalating to a full re-parse, resumes from a persisted cursor, stamps `parser_generation` only on full coverage, and `--json` actually emits JSON. | `9e2c638 v1.108.259 - a re-index you can schedule`; `0a1f03e v1.108.263 - refresh --json was dead on arrival`; docstring #395 (@dkiaulakis), #414 shape at L80 | 3 | LOAD-BEARING | `TestSliceDoesNotEscalateToFullReindex` is the named "whole feature is a lie without it" guard. |
| tests/test_refresh_corpus_unreadable.py | A refresh campaign whose discovery returns an empty corpus refuses (`corpus_unreadable`) instead of stamping the generation after re-parsing zero files. | `6f2b03c fix(refresh): a campaign that saw nothing must not certify everything`; docstring: three benchmark corpora stamped `2` after 0 files, damage unrepairable | 3 | LOAD-BEARING | Non-vacuity class `TestTheGuardIsWhatBlocks` reinstates the old check and asserts the false certificate returns. |
| tests/test_register_edit.py | `register_edit` clears the BM25 cache and search result cache so the next search sees the edit. | `2fb56d8 feat: session-aware routing — 10 features` ("Feature 4") | 3 | LOAD-BEARING | Reaches into `search_symbols` private cache globals. |
| tests/test_reindex_state.py | `reindex_state` start/done/failed transitions, strict freshness mode waits, `wait_for_fresh_result` timeouts. | `6f3e05a fix: backpressure review — 10 bugs, spec compliance`; `976545d feat: configurable strict_timeout_ms` | 3 | LOAD-BEARING | Uses threading + wall-clock sleeps; autouse reset of module globals. |
| tests/test_related_symbols.py | `_tokenize_name` splits snake/camel case and `get_related_symbols` ranks name-overlap + co-location neighbours. | `94f9ed2 release: v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)` | 1 | LOAD-BEARING | Builds a tmp index via `index_folder`. |
| tests/test_render_diagram.py | `render_diagram` detects the source tool shape, prunes to `max_nodes`, disambiguates basenames, sanitises Mermaid labels, and edges point the right way. | `bc42e81 feat: add render_diagram`; `90f1c7e fix(render_diagram): correct edge resolution direction + honor max_nodes` | 1 | LOAD-BEARING | Parametrized in #244 (330 -> 78 tests). |
| tests/test_render_diagram_integration.py | With a real `mmd-viewer` binary, `open_in_viewer=True` writes an `.mmd` starting with `flowchart TD` and spawns the viewer. | `c584873 Add open_in_viewer to render_diagram`; `100d98e test: drop hardcoded MMD_VIEWER_PATH fallback` | 6 | STRUCTURAL | Always skipped unless `MMD_VIEWER_PATH` env var points at a local build; never runs in CI (N7 contributor); machine-specific by design. |
| tests/test_render_diagram_viewer.py | `mermaid_viewer` resolves the viewer path (config, `$PATH` via `shutil.which`, or None), is config-gated, spawns and cleans up. | `c584873 Add open_in_viewer`; `4896089 fix(mermaid_viewer): bare-name config falls back to shutil.which (F6)` | 6 | LOAD-BEARING | Patches `os.access` so fake `.exe` paths look executable on both OSes. |
| tests/test_repeat_root_walk_additional.py | Three #504 cases #513 left open: a control proving the no-change path is not always taken, plus two incremental re-walk cases over a git repo. | `337083a test(index_folder): three #504 cases beyond #513's coverage`; docstring credits @lsg1103275794 | 3 | LOAD-BEARING | Shells out to `git` via subprocess; the control test guards vacuity. |
| tests/test_replay_metrics.py | recall@k / MRR / nDCG / aggregate math, and `run_replay._check_gate` fails on a 2% regression against the committed golden `self_v1_75_0-golden.json`. | `69a00c6 v1.76.0 — replayable benchmark + retrieval-quality harness`; `f0e5942 ci: wire the replay retrieval-quality gate into CI (audit WS-7)` | 1, N4 | LOAD-BEARING | `sys.path.insert` into `benchmarks/replay`; reads `results/self_v1_75_0-v1.75.0.json` and asserts all metrics 1.0 (frozen self-replay from v1.75.0). |
| tests/test_repo_health.py | `get_repo_health` returns the radar and grade; `_count_unstable_modules` excludes tests/benchmarks/scripts and framework entry points from BOTH sides; `_is_production_path` filename conventions. | `79e4d0d fix: a count taken after the page, and a field nobody read (#559-#562)`; L100 v1.73.1 NameError regression; L123 v1.91.0, L152 v1.92.0 | 9 | LOAD-BEARING | #561 sign-error guard (84.0 B -> 88.8 B). |
| tests/test_resolve_repo.py | `resolve_repo` computes the same id as the watcher, resolves worktrees (#277), finds the common dir without a subprocess (#303), and `_read_origin_url` is hardened. | `bcd406e v1.108.14 — resolve_repo perf at scale (#303)`; `54ca08b v1.108.16 — provisional-id bypass + _read_origin_url hardening (#303 follow-up)`; `7fd8768 (#295)` | 3, 5 | LOAD-BEARING | `test_fast_path_does_not_spawn_subprocess` guards a reintroduced git spawn; uses `git init` in tmp. |
| tests/test_resolve_repo_nested_repo_boundary.py | Containment fast path must not resolve a nested independent git repo to its parent's index; submodules stay indexed into the parent; the guard is a stat, never a subprocess. | `230bad6 fix(resolve_repo): stop containment resolving across a nested repo boundary (#492)`; docstring cites the #303 hang and #372 | 3 | LOAD-BEARING | Shells out to git to build nested repos. |
| tests/test_response_cap.py | A single tool response over `response_max_bytes` is refused with a structured error at the `call_tool` wrapper; moving `max_file_size` does not move the response limit; the wrapper has one exit. | `3037636 v1.108.257 - a response limit that is a response limit`; `923c3d6 fix: four import-graph and call-outcome defects (#550, #551, #552)`; docstring #425 | 4, 8 | LOAD-BEARING | L148 counts `Return` nodes in the wrapper AST (#551), a source-scanning ratchet. |
| tests/test_result_cache.py | The shared session LRU (`result_cache_get/put/invalidate/stats`): hits/misses counted, per-repo invalidation, eviction at `_RESULT_CACHE_MAXSIZE`, and returned values compare `==` not `is`. | `afbf204 v1.18.0 — session LRU result cache`; `7e9f490 fix: the result cache handed out the object it was holding (#572)` | 3 | LOAD-BEARING | Seven `is` assertions changed to `==` in #572 (Practice 9: the old test encoded the defect). |
| tests/test_result_cache_isolation.py | `cache_get` returns an isolated container clone so the dispatcher's `_meta` edits (meta_fields [], suppress_meta, partial subset, budget writes) cannot reach the stored entry; reproduces the `KeyError: '_meta'` on the second `find_references`/`get_blast_radius` call. | `7e9f490 fix: the result cache handed out the object it was holding (#572)`; docstring cites #570, #377 item 3, #404 (@rknighton) | 3 | LOAD-BEARING | Includes the two quiet cases (suppress_meta then a metadata caller) a two-call repro cannot see; measures `_isolate` vs deepcopy. |
| tests/test_retrieval_counterfactual.py | `explain_route` names the FIRST gate that excluded an action, and the reconstruction agrees with `classify_intent`+`search_catalog` on every benchmark query in `route_recall/results.json`. | `f690af8 v1.108.217 - every route miss now names the gate that stopped it`; `2a1cc2a v1.108.220 - a held-out corpus` | 4, 9 | LOAD-BEARING | Reads the committed route-recall artifact; `TestFidelity` is the named load-bearing class. |
| tests/test_retrieval_inflation.py | `_detect_inflation` reports calls-per-need with `basis: calls`, excludes NULL `session_uid` rows (pre-#456), refuses under `INFLATION_MIN_NEEDS`, and reports concentration/head_share beside the mean. | `87ee49d feat(regret): charge ourselves for the calls before the one that worked`; `a79fd0c v1.108.309 — a mean hides the tail`; docstring arXiv:2608.13571, #456 | 9 | LOAD-BEARING | Builds a sqlite `ranking_events` table in-memory. |
| tests/test_retrieval_tools.py | `get_file_content`, `get_file_outline`, `get_repo_outline`, `search_text` return the expected shapes over a seeded index. | `574ca15 chore: bump version to 1.11.8 (trusted_folders + check_freshness)`; `4b863f1 fix: watcher config layering, hash-cache reindex bug, and flaky test isolation` | 1 | LOAD-BEARING | Early-era tool tests; no issue number. |
| tests/test_reuse_audit.py | `investigate_reuse_before_write` obligations and `_verdict`: the same repo yields a different verdict when the synonym (embedding) channel is off (`lexical_only` vs `write_justified`); dead-only matches yield `adapt_candidate`. | `f750718 feat(investigator): investigate_reuse_before_write, withheld under the moratorium`; docstring names the load-bearing test | 1, 9 | LOAD-BEARING | Investigator module is not an MCP tool (moratorium). |
| tests/test_route_binary_pilot_is_frozen.py | `benchmarks/route_binary_pilot/predicate.py` matches its registered sha256 (line-ending normalised), and every number in RESULT.md/ROADMAP.md equals `results.json`. | `7470ec7 test(pilot): gate the spent corpus and pin the prose to the artifact`; docstring: a refuted one-run experiment (H3) must not be re-fitted | N4, 7 | LOAD-BEARING | Byte-level sha256 pin; any predicate edit fails by design (LICENSE-digest pattern). |
| tests/test_route_recall_artifacts_are_fresh.py | Re-runs `run_route_recall.py` and `run_emitted_task.py` and requires their summaries to equal the committed `results.json` artifacts. | `ee070ba bench(route): a baseline gets as many guesses as the system it is the floor for`; `4a55e9f perf: trim get_ranked_context's description; regate the route-recall artifacts`; docstring: 69.5 vs 71.2 drift on 2026-08-21 | N4, 4 | LOAD-BEARING | Runs the harness as a subprocess (~1s each); skips on sdist checkouts lacking the scripts. |
| tests/test_route_utils.py | `_route_utils.has_dependency` reads requirements/package.json/go.mod, `read_package_json`, `make_route_file_context`, `ENTRY_POINT_DECORATOR_RE`. | `32eb018 feat: AST call graph ... framework providers (#218)` | 1 | LOAD-BEARING | Plain helper unit tests. |
| tests/test_runtime_phase0.py | Runtime schema migration creates the `runtime_*` tables idempotently, `redact_trace_record` strips PII, `resolve_to_symbol_id` maps (file,line,function). | `f62c291 v1.97.0 — runtime trace ingestion (Phases 0-2)`; later `51dca43 v1.108.96 import-scip` | 8, 3 | LOAD-BEARING | Table-name list literal `_RUNTIME_TABLES`. |
| tests/test_runtime_phase1.py | OTel JSON/JSON-Lines/.gz ingest populates `runtime_calls`/`runtime_edges`/`runtime_unmapped` end-to-end. | `f62c291 v1.97.0 — runtime trace ingestion (Phases 0-2)` | 1 | LOAD-BEARING | Builds OTLP span dicts in-test. |
| tests/test_runtime_phase2.py | With runtime_calls populated results gain `_runtime_confidence` + `_meta.runtime_freshness`; with it empty the response shape is unchanged from Phase 1 (zero-cost contract). | `f62c291 v1.97.0 — runtime trace ingestion (Phases 0-2)` | 9, 7 | LOAD-BEARING | Absence half guards the unchanged-shape contract. |
| tests/test_runtime_phase3.py | `get_runtime_coverage`, `find_hot_paths`, `find_unused_paths` over ingested traces; `find_unused_paths` refuses when `runtime_calls` is empty. | `584ab8a v1.97.1 — Phase 3 runtime analytics tools` | 1, 9 | LOAD-BEARING | Refusal-not-zero guard. |
| tests/test_runtime_phase4.py | pg_stat_statements CSV + SQL JSONL parsers, table/column reference extraction, `runtime_columns` upsert, dbt model rescue in `find_unused_paths`, v14->v15 migration idempotent. | `dd1d4e9 phase 4: SQL query log ingest (pg_stat_statements + JSONL)` | 1, 10 | LOAD-BEARING | 26 tests; migration idempotency check. |
| tests/test_runtime_phase5.py | Python/JVM/Node traceback parsers, severity inference, `runtime_stack_events` populated, `get_symbol_provenance` adds `stack_frequency`, v15->v16 migration idempotent. | `a72b47c v1.98.0 — Phases 4 + 5 (SQL log + stack-frame ingest)` | 1 | LOAD-BEARING | `socket hang up` at L153 is a Node stack fixture string, not a network call. |
| tests/test_runtime_phase6.py | HTTP live-ingest routes: 503 when disabled, 400/404/413 paths, 200 envelope for all three, repo from header or query, gzip decode, `get_redaction_log`. | `cb5cedf v1.99.0 — Phase 6 (HTTP live-ingest endpoint)`; `e6a35fc v1.108.73: fails closed without a token`; `9661e0b test: ten skipped modules that hid 209 tests between them` | 8, 6 | LOAD-BEARING | `pytestmark = skipif` via `find_spec` (deliberately NOT importorskip, L33); in-process Starlette TestClient, no sockets. |
| tests/test_runtime_phase7.py | `compute_radar` adds a 7th `runtime_coverage` axis only when passed, PR risk rebalances to six weights summing to 1.0 with traces, `runtime_dark_code_introduced` flips, observatory `append_run` flags runtime evidence. | `e816e09 v2.0.0 — Phase 7 (runtime-aware PR risk; milestone capstone)` | 9, 7 | LOAD-BEARING | Bit-for-bit composite comparability guard. |
| tests/test_rust_fidelity.py | Rust extraction vs the FROZEN `syn` oracle: `extra`, `wrong_span`, `undercount`, `qual_mismatch` must be 0; `missing` reported by kind with only `module`+`macro` known. | `c13b623 feat(benchmarks): rust_fidelity`; `5202792 fix(rust): three definition classes that yielded no symbol (PARSER_GENERATION 4 -> 5)`; `89aaa21 fix(rust): give impl methods the type they hang off`; docstring #553 | 1, 10 | LOAD-BEARING | Fixture roster read off disk (not a literal); qualified-name counting because a set cannot count. |
| tests/test_rust_fidelity_artifacts.py | `benchmarks/rust_fidelity/README.md` rows match `results.json`, and `corpus.json` pins a 40-hex commit SHA. | `c13b623 feat(benchmarks): rust_fidelity`; docstring cites 1.108.298 (5 of 8 artifacts stale) and 1.108.299 | N4 | LOAD-BEARING | Regex over README prose; `_SHA_RE` pin. |
| tests/test_savings_baseline.py | `raw_bytes` counts each source byte once: `symbol_span_bytes` merges nested spans, `distinct_file_bytes` charges a file once, and a tree-wide source scan forbids `sum(byte_length ...)`/per-symbol `file_sizes[...]` patterns. | `542e12c v1.108.291 — counting each byte of source once`; docstring: 2.85x / 12.3x / 32.2x over-counts measured | 2, 9 | LOAD-BEARING | Ratchet over `src/` text with regexes (L134, L152); CLAUDE.md records the first regex walked past the real defect. |
| tests/test_savings_by_tool.py | `_savings.json` gains a per-tool `by_tool` breakdown that stays LOCAL; the telemetry payload remains `{delta,total,anon_id}`. | `542e12c v1.108.291 — counting each byte of source once`; docstring: the question a 2026-08-22 baseline correction could not size | 9, 8 | LOAD-BEARING | Pins telemetry payload shape (disclosure surface). |
| tests/test_savings_two_carriers.py | The receipt discloses that `savings_tokens` scales with call count and shows a per-call figure; the shared telemetry payload is untouched. | `1649fbf fix(receipt): disclose that savings scales with call count, and show per-call`; docstring arXiv 2608.01347 | 9 | LOAD-BEARING | L132 reads `token_tracker.py` source text to pin the payload (ratchet). |
| tests/test_savings_usd_basis.py | The receipt's dollar figure carries a `basis` label naming what it prices, and `test_the_dollar_arithmetic_is_untouched` keeps the number unscaled. | `b95416b feat(receipt): state what the dollar figure prices`; docstring: Graft blended-rate correction, 98.6% cache reads measured; L62 v1.108.312 | 9 | LOAD-BEARING | Fourth `*_basis` field in the family. |
| tests/test_scala_parser.py | Scala 3 significant-indentation parsing extracts abstract defs, vals, vars, type aliases, traits, objects. | `8abb607 fix(parser): extract Scala 3 abstract defs, vals, vars, and type aliases` | 10 | LOAD-BEARING | Single inline source literal. |
| tests/test_schema_baseline_transcription.py | No bare integer from `benchmarks/schema_baseline.json` is transcribed anywhere in `tests/` or `benchmarks/` (prose included). | `d6ecb4f test: the schema budget guard asserted a file against copies of itself`; docstring: `test_v1_108_183.py` asserted `3996` for seven months | 4, N4 | LOAD-BEARING | Text-scanning ratchet; `skipif` baseline missing. |
| tests/test_schema_budget.py | `tools/list` token counts (tiktoken cl100k) at every profile x compact_schemas stay within 5% of `schema_baseline.json`; `core_compact` live ceiling 4,000; schema/dispatch param parity; Counter saving computed not quoted. | `0b27797 v1.108.67 - demote the language enum under compact_schemas`; `38a7080 v1.108.75: Phase 3 quality gates — live core_compact ceiling`; `bc5a31e fix: compute the Counter's schema saving instead of quoting it` | 4 | LOAD-BEARING | `skipif` tiktoken absent or baseline missing (N7); CLAUDE.md: core_compact at 3,998/4,000. |
| tests/test_schtasks_locale.py | `watch-status` decodes `schtasks` output in the console code page (CP936 bytes verbatim from #468) and decides liveness from the state field, not English `Running`/`Ready`. | `139bd26 fix(watch-status): schtasks output is not UTF-8 and its state is not English (#468, #469)`; docstring credits @lsg1103275794 | 6 | LOAD-BEARING | Runs on every platform (drives decode directly, no shell-out); CP936 byte fixture literal. |
| tests/test_scip_evidence.py | Hand-rolled SCIP protobuf reader parses packed/unpacked ranges and .gz, `import-scip` writes `scip_edges`/`scip_unmapped`, row cap FIFO-evicts. | `51dca43 v1.108.96 — import-scip: compile-time evidence via SCIP index ingest` | 1, 8 | LOAD-BEARING | Fixture SCIP bytes built in-test with a tiny varint writer, no binary blobs. |
| tests/test_sdist_exclusions.py | Builds a REAL sdist into a temp dir with canaries planted under every excluded path and requires them absent; also asserts an allowlist of sdist-root files in both directions. | `978e614 v1.108.246` (first); `33bc3b7 fix(tests): build the sdist with whatever builder the environment has`; docstring: sdist carried `vscode-extension/node_modules/` (128 entries) through every release to 1.108.245; `.claude/` leak v0.2.6 | 8, N5 | LOAD-BEARING | ERRORS at setup when the scratch venv must fetch the build backend with DNS blocked; CLAUDE.md says never wave that through. Allowlist is the shipped-artifact roster. |
| tests/test_search_ast_encoder_contract.py | `search_ast`'s encoder table key names `matches`, and EVERY encoder's declared table key is a key its tool actually emits (source-text scan of `tools/*.py`). | `4fa46a1 fix: search_ast encoded to an empty table for every language and preset (#553)`; credits @RascoApps | 4, 1 | LOAD-BEARING | Ratchet: text scan over tool sources (L135 admits it is weaker than execution). |
| tests/test_search_columns.py | `search_columns` filters dbt/SQLMesh column metadata by name/model and `_collect_all_columns` flattens context_metadata. | `13072fc v1.5.0 — production hardening (TODO.md Phase 1-2)` | 1 | LOAD-BEARING | Fully mocked CodeIndex via MagicMock (no real index). |
| tests/test_search_perf.py | BM25 search correctness over a synthetic index plus in-process latency budgets: cold < 2000 ms, warm < 500 ms. | `94f9ed2 v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)`; `dcc1729 perf: carry forward BM25 token bags across incremental reindex` | 5, 1 | LOAD-BEARING | Wall-clock thresholds (generous); module-scoped synthetic repo. |
| tests/test_search_result_cache.py | `search_symbols`' OWN per-tool result cache (`_result_cache_get/put`, `result_cache_invalidate_repo`, `_RESULT_CACHE_MAX`) hits, evicts, invalidates per repo, is thread-safe, and hits count toward the savings meter. | `2fb56d8 feat: session-aware routing ("Feature 5")`; `b130e04 v1.108.133 — cache hits count toward the savings meter` | 3, 2 | LOAD-BEARING | Distinct from `test_result_cache.py` (shared LRU in token_tracker): two caches, not redundant; #572 notes search_symbols fixed its cache twice (#377 item 3, #404) without reaching the shared one. |
| tests/test_search_symbols_defaults.py | search_symbols default detail_level="auto" resolves to compact for broad queries and full-mode payload is materialised before token_budget packing so the budget is honoured. | `76cd23e release: v1.70.0 — default detail_level="auto" + full-mode budget fix`; docstring §1.1/§1.2 | 2 | LOAD-BEARING | Six alpha-prefixed names with 80-line padded bodies as the sole fixture; indexes a real folder. |
| tests/test_secret_classifier.py | Every row of the #351 oracle table (exact names, key material, credential extensions, secret-store dirs, template/public-key exemptions, override knobs) classifies as the oracle says. | `a85901b release: v1.108.79 — structured credential-file classifier (#351 broad redesign)`; docstring cites #351 oracle by @mmashwani | 8 | LOAD-BEARING | Data-table spec; ORACLE list is the contract. |
| tests/test_security.py | validate_path / symlink escape / is_secret_file / binary detection / skip-directory + file-cap resolvers behave as security.py documents, including the secrets-manager substring false positive. | `dd1af5f fix(security): is_secret_file matches basename + secret-store dirs, not path substring`; #351 comments at L159-238 | 8 | LOAD-BEARING | Two `skipif(win32)` on symlink tests; overlaps test_secret_classifier on the #351 rows but also covers path/binary/caps, so not redundant. |
| tests/test_security_disclosure.py | SECURITY.md's enumerations (write-accepting routes, telemetry payload fields, exempt tools) match the code they describe. | `0d6422b release: v1.108.274 — a disclosure that is true when written is not yet a control`; docstring #449, #448, #435 | 8 | LOAD-BEARING | Ratchet reading SECURITY.md text; `importorskip("starlette")` on the route enumeration. |
| tests/test_security_exclusions_are_project_overridable.py | exclude_skip_directories / exclude_secret_patterns honour a project-level .jcodemunch.jsonc override, not global config only. | `b906e4d fix(security): thread repo= through both exclusion resolvers (#491)`; docstring #491, antecedents #300/#187/#304/#301 | 6 | LOAD-BEARING | Uses tests/fixtures/fake_server.py; guards the #209 shipped path from regressing. |
| tests/test_selective_snapshot.py | SelectiveIndexView answers supported narrow calls without hydrating the full CodeIndex, promotes exactly once on unsupported/broad modes, and never opens with immutable=1. | `838bf45 release: v1.108.216 - a narrow read stays narrow, or it promotes`; docstring #398 Arc 2 | 3 | LOAD-BEARING | TestPromotionBoundary is the false-absence guard; CORPUS_WIDE parametrised. |
| tests/test_semantic_search.py | Env-selected embedding providers, no-provider error path and zero-impact defaults work with embed_texts mocked and local_onnx forced absent. | `2b5b77d test: stop test_semantic_search assuming onnxruntime is absent`; `eab59f5 fix: embed_repo preflight loads IDs only (#231)`; #181 follow-up | 1 | LOAD-BEARING | autouse fixture forces onnx absent because the docstring's "no optional deps" was a machine assumption. |
| tests/test_server.py | list_tools publishes exactly the expected 90-tool default roster, kind enum is derived, and index_repo/index_folder dispatch forwards heartbeat/max_size args as documented. | `ef259ce fix: search_symbols(kind="field") was refused by both gates (#571)`; comments #374, #383, #429 | 4 | LOAD-BEARING | Hard literal `len(tools) == 90` (CLAUDE.md says 91 visible; test_summarizer disabled by default per docstring); mutates _GLOBAL_CONFIG directly. |
| tests/test_server_json_sync.py | server.json's version and package version stay equal to pyproject.toml's. | `6c22f5f chore: sync server.json to pyproject version, add drift guard`; docstring: both registry entries sat frozen five months | 7 | LOAD-BEARING | Reads repo files by regex (no tomllib); pin-site drift guard. |
| tests/test_session_journal.py | SessionJournal records reads/searches/edits and get_context reports counts, last_tool and negative evidence. | `2fb56d8 feat: session-aware routing — 10 features`; `196db36 get_session_snapshot + PreCompact hook (#211)` | 9 | LOAD-BEARING | Plain unit tests of a feature; no incident named. |
| tests/test_session_snapshot.py | get_session_snapshot returns the snapshot/structured shape and respects max_* limits on an empty and populated journal. | `196db36 feat: get_session_snapshot MCP tool + PreCompact hook — solve compaction amnesia (#211)` | 9 | LOAD-BEARING | Resets the journal singleton via _journal_lock. |
| tests/test_session_state.py | SessionState save/load round-trips the journal and search cache. | `2fb56d8 feat: session-aware routing — 10 features for smarter AI-guided code exploration` | 9 | LOAD-BEARING | Feature unit test; hardcoded 2026-04-05 timestamps in cache keys. |
| tests/test_share_savings.py | set_bool_key handles all three template shapes, apply_share_savings writes the flag, and upgrade_config preserves a user-set share_savings across upgrades. | `6a84e68 release: v1.108.21 — share_savings opt-out flag`; docstring "critical PRD invariant" | 6 | LOAD-BEARING | Config-upgrade preservation is the load-bearing half. |
| tests/test_signal_fusion.py | Weighted reciprocal rank fuse() math and channel builders produce non-zero weights (the #324 inert-fusion regression). | `ead6dfc release: v1.108.29 — fix inert WRR signal fusion (all weights resolved to 0.0) (#324)` | 1 | LOAD-BEARING | Regression at L112/L158 for #324 default-weight sentinel. |
| tests/test_size_cap_end_to_end.py | For every documented max_file_size route x every discovery entry point, a symbol inside an oversize file is retrievable after indexing. | `293e758 test: verify the size cap where the user stands, not at the layer we edited`; docstring v1.108.193/.194/.197 three-release history | 6 | LOAD-BEARING | Deliberately altitude-level matrix; pins storage before load_config (#437). |
| tests/test_skill_candidates.py | Skill-candidate advisory fires only on subtree concentration, never on size alone or on a package root, and is silent on a default install. | `cc8d87a release: v1.108.249`; docstring NON-VACUITY NOTE; thresholds tuned 2026-08-06 against five real configs | 9 | LOAD-BEARING | Negative assertions are the point; tuning corpus named in comments. |
| tests/test_skills.py | Claude Agent Skill bundle content has frontmatter/marker and install/uninstall/status round-trip. | `ff6992c release: v1.107.0 — Claude Agent Skill bundle` | 6 | LOAD-BEARING | Feature unit test; writes into tmp skill dirs. |
| tests/test_source_drift.py | install-status reports source_drift tri-state from running-process start time vs source mtime, never False for unknown. | `3d1f95c feat: install-status reports whether the running code matches its tree`; docstring: box ran 1.108.293 against a 1.108.307 tree | 6 | LOAD-BEARING | Tri-state guard; L238 cites #559 mock lesson. |
| tests/test_sql_language.py | SQL DDL (tables, views, functions, indexes, schemas) and dbt Jinja SQL extract as symbols. | `49426fd Adding dbt sql support` | 10 | LOAD-BEARING | Inline SQL fixtures only. |
| tests/test_sqlite_store.py | SQLiteIndexStore creates schema/WAL, saves/loads/incrementally saves indexes, and cross-process mtime invalidation works. | `1950077 fix: cross-process LRU cache invalidation`; `2ff57a8 fix: report unloadable repo indexes truthfully (#291)` | 3 | LOAD-BEARING | Core store contract. |
| tests/test_stdio_guard.py | With the stdio transport, any non-JSON-RPC write to fd 1 (C-level, subprocess, thread) is redirected to stderr. | `044c3c9 release: v1.108.268 - JSON-RPC owns a private stdout`; docstring jdoc#110 parity | 6 | LOAD-BEARING | Spawns real Python subprocesses by design; sets PYTHONPATH=src. |
| tests/test_stop_rule.py | build_stop_rule yields terminal=True only on positive evidence and False on every uncertainty or unknown verdict; ALREADY_CONSULTED binds to real import sites. | `2a097af feat(safety): ship an executable stop rule beside the confidence score`; docstring arXiv 2608.01347 | 9 | LOAD-BEARING | Practice 7; fail-closed on destructive verdicts. |
| tests/test_storage.py | IndexStore save/load/has_index/list and corruption handling behave, including unloadable-index truth (#291). | `f580cd4 release: v1.108.105 — index-write lock on delta paths + load_index corruption guard`; `2ff57a8 (#291)` | 3 | LOAD-BEARING | Overlaps test_sqlite_store on save/load but covers corruption guard + has_index. |
| tests/test_storage_path_resolution.py | A relative base_path resolves against cwd at construction, so two stores named "store" in different cwds are distinct. | `af12cf2 fix(storage): resolve relative cache paths` | 6 | LOAD-BEARING | No docstring; chdir-based; clears _VERIFIED_PATHS and _initialized_dbs. |
| tests/test_streamable_http_integration.py | The real Starlette app built like run_streamable_http_server routes requests and never returns None from an endpoint. | `175224c test: add integration tests for streamable-http transport`; docstring names the NoneType TypeError | 6 | LOAD-BEARING | Uses httpx.ASGITransport in-process (no socket); stubs the MCP transport. |
| tests/test_streamable_http_sessions.py | A request carrying a known mcp-session-id is routed to the existing transport rather than a new server.run() task. | `230ae60 fix: streamable-http session persistence (#204)` | 6 | LOAD-BEARING | Re-implements the routing closure with mocks rather than exercising the app; weaker than the integration file but tests the session map specifically. |
| tests/test_strict_deny_requires_resolvable_target.py | The bash hook's strict deny stays silent on unexpanded shell expansions ($HOME, $B) and drive-letter paths but still denies regex-anchor searches like "foo$". | `0d951fb fix(hooks): a strict deny requires a target the hook can resolve`; docstring #541 | 6 | LOAD-BEARING | Literal `C:/Users/j/x.md` and `/c/Users/j/x.md` params (reporter's paths, inputs only). |
| tests/test_subprocess_encoding_guard.py | Every text-mode subprocess call in src/ passes encoding=; the known-gap allowlist is empty. | `bf1a543 release: v1.108.232 - git output is decoded as UTF-8, not as cp1252` | 8 | LOAD-BEARING | AST ratchet over src/**/*.py; Windows-only cp1252 failure invisible in CI. |
| tests/test_suggest_corrections.py | Regret signals are mined from a seeded ranking_events ledger, the no-telemetry path fabricates nothing, and suggest_corrections writes no user file. | `c472c29 release: v1.108.68 - retrieval-regret loop`; `85f3fb6 test: two guards read ambient machine state instead of declaring it` (#437 shape) | 9 | LOAD-BEARING | Patches token_tracker._state per test. |
| tests/test_suggest_queries.py | suggest_queries errors on unindexed/empty repos and returns suggestions for an indexed one. | `94f9ed2 release: v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)` | 1 | LOAD-BEARING | Empty-index test is conditional on indexer output (`if r_idx["success"]`), can pass vacuously. |
| tests/test_summarize_from_docstrings.py | summarize_from_docstrings default-on, opt-out falls to signature fallback, per-repo override honoured (IPI defense F-04). | `aa4cb54 release: v1.108.22 — docstring opt-out`; docstring P1.5 | 8 | LOAD-BEARING | Config-gate test. |
| tests/test_summarizer.py | Docstring extraction, provider detection (paid auto-detect opt-in), Gemini/OpenAI batch summarizers, circuit breaker, timeout and openai_extra_body (#323). | `a04df81 release: v1.108.128 — never auto-bill a paid cloud provider`; `f008def fix: OpenAI summarizer honors the circuit breaker`; #323 | 6 | LOAD-BEARING | Mocks httpx.Client; sets JCODEMUNCH_ALLOW_PAID_SUMMARIES; 900+ lines. |
| tests/test_surface_cli.py | `jcodemunch-mcp surface` routes correctly, emits the --json receipt shape, and reflects env-selected surface/profile. | `5296846 release: v1.108.154 — surface CLI subcommand` | 4 | LOAD-BEARING | Spawns `python -m jcodemunch_mcp` subprocesses (120s timeout). |
| tests/test_surface_offer.py | build_offer is a message not a migration: never imports config or writes, prices via _build_tools_list, carries its basis, and every exit route is a typed command. | `d9851b5 release: v1.108.313 — an install created before a default can never learn there is a choice` | 4 | LOAD-BEARING | AST ratchets over surface_offer.py and server.py; L333 cites #508 do-nothing-parameter lesson. |
| tests/test_svelte.py | .svelte files register, script blocks re-parse via the Vue-style path, and symbols carry language="svelte". | `7068c7a feat: add Svelte (.svelte) language support`; docstring blueprint test_astro.py | 10 | LOAD-BEARING | Fixtures under tests/fixtures/svelte. |
| tests/test_swift_parser.py | Swift struct/class/enum/protocol/extension/typealias and SwiftUI constructs extract as symbols. | `842d4e6 Improves Swift parsing and adds Xcode ignore rules` | 10 | LOAD-BEARING | Inline source fixture. |
| tests/test_symbol_complexity.py | get_symbol_complexity reports cyclomatic/nesting/params for a nested-branch fixture and persists across save. | `072a49b release: v1.17.0 — Code Metrics & Health + complexity persistence bug fix` | 1 | LOAD-BEARING | Persistence half guards the named v1.17.0 bug. |
| tests/test_symbol_diff.py | get_symbol_diff between two indexed repos reports added/removed/changed/unchanged symbols. | `94f9ed2 release: v1.21.18 — Phase 4 test infrastructure (T12/T13/T14)` | 1 | LOAD-BEARING | Two repos in one store. |
| tests/test_taskcomplete_real_contract.py | The taskcomplete hook renders untested-symbol findings from the REAL get_untested_symbols response key with no producer mocked. | `79e4d0d fix: a count taken after the page, and a field nobody read (#559-#562)` | 9 | LOAD-BEARING | Own file by design: leaks from test_hook_steering_fixes' MagicMock IndexStore broke it in-place. |
| tests/test_tectonic_map.py | get_tectonic_map clusters a two-cluster import graph via structural/behavioral fusion and label propagation. | `4a955df feat: add get_tectonic_map — logical module topology via 3-signal fusion` | 1 | LOAD-BEARING | Feature unit tests. |
| tests/test_telemetry_db_skip.py | list_repos skips telemetry.db and org_savings.db instead of initialising them as index schemas and emitting repo="". | `115a0b7 release: v1.81.3 — telemetry.db no longer poisons bare-name resolver`; docstring #276 | 3 | LOAD-BEARING | Builds a real telemetry.db schema fixture. |
| tests/test_templates.py | name.<ext>.j2/.twig files are recognised, engine constructs masked offset-preserving, body parsed as the underlying language. | `f405bee feat: index templating-language files over a supported underlying language (#336) (#337)` | 10 | LOAD-BEARING | Fixtures under tests/fixtures/templates. |
| tests/test_tier_resolver.py | normalize_model_id strips provider prefixes/bracket/date suffixes and resolve_model_to_tier picks the longest normalized substring key (#249). | `3cca720 fix(tier_resolver): normalize substring-match keys (closes #249)`; `f18c13e picks longest key, not first` | 4 | LOAD-BEARING | Model-id literals like claude-opus-4-7[1m]. |
| tests/test_tier_runtime.py | Session tier overrides set/get/reset, tools/list_changed emission, and set_tool_tier error path carries isError. | `42c3633 fix(server): weakref-based session tier tracking (F2 + F3)`; `b999670 v1.108.74: tool errors carry isError` | 4 | LOAD-BEARING | Touches server module globals. |
| tests/test_tier_switch_cost.py | breakeven_requests/classify refuse the full->standard narrowing (174/864 requests to repay) while never refusing a widening or a paying narrowing. | `de1d063 release: v1.108.311 — the intuition inverts once the block is cached`; `c9fda11 v1.108.312 — a count with no time basis is read as per-request` | 4 | LOAD-BEARING | Every refusal has a sibling allow assertion. |
| tests/test_token_budget_context.py | get_context_bundle honours token_budget/budget_strategy/include_budget_report and get_ranked_context assembles query-ranked context. | `843adb8 feat: enhanced BM25 tokenizer, diversity-aware budget packing`; `e6fb764 fix: skip diversity packing tests when native backend unavailable` | 2 | LOAD-BEARING | Some tests may skip when native backend absent (per e6fb764). |
| tests/test_toml_end_line.py | A TOML table's end_line stops before the next table header rather than claiming it. | `a61a894 release: v1.108.236 - a TOML table no longer claims the next table's header line`; docstring fastapi a64dfbbd | 1 | LOAD-BEARING | Fixture trimmed from real fastapi pyproject. |
| tests/test_tool_registration.py | Every tool is present in builder, dispatch, _CANONICAL_TOOL_NAMES, tier bundles and _SNIPPET_TOOL_CATEGORIES, reporting all gaps at once. | `db4b5a4 test: consolidated tool-registration consistency guard` | 4 | LOAD-BEARING | Consolidates prior whack-a-mole checks. |
| tests/test_tool_surface_clamp.py | An unrecognised JCODEMUNCH_TOOL_SURFACE value is reported as "full" with the rejected value named, and the telemetry payload carries no surface field (#424). | `6337d39 release: v1.108.260 - a receipt that confirms your typo`; docstring #424 follow-up | 9 | LOAD-BEARING | Fifth diagnostic-vs-runtime instance per docstring. |
| tests/test_tools.py | parse_github_url, discover_source_files, should_skip_file and Windows UNC/drive path safety (#438/#439/#453) behave without touching the real filesystem. | `1b2f467 test: stop TestWindowsUNCPathSafety letting the network decide its verdict (#453)`; `503ae0b test(index_folder): narrow the Path.resolve mocks (#479)` | 8 | LOAD-BEARING | 1400+ lines; two `skipif(os.name != "nt")` classes; custom guard raises if a path test touches the real FS. |
| tests/test_transcript_roots.py | receipt's transcript discovery unions ~/.claude/projects with CLAUDE_CONFIG_DIR profiles, de-duplicates sessions, and mtime-prefilters. | `50c92bd release: v1.108.252 — receipt counted 12 of 348 calls, because it only ever looked in one profile`; docstring jcm#421 | 9 | LOAD-BEARING | Synthetic JSONL sessions in tmp. |
| tests/test_truncated_flag.py | search_symbols sets truncated=True only when token_budget packing dropped results. | `f729c14 release: v1.21.2 — fix truncated flag and call graph source label` | 2 | LOAD-BEARING | 20 near-identical function names as fixture. |
| tests/test_ts_module_extensions.py | .mts/.cts map to TypeScript and a `./foo.mjs` specifier resolves to foo.mts so importers stay visible. | `fd49ad0 fix: .mts and .cts indexed as nothing`; docstring #550 shape | 10 | LOAD-BEARING | Property-stated tests; pairs table _MODULE_PAIRS. |
| tests/test_ts_type_only_imports.py | All four `import type` spellings reach the import graph like value imports; an ambient declare type with no import is deliberately not a reference. | `79e4d0d fix: a count taken after the page, and a field nobody read (#559-#562)`; docstring #560 (@lilubot) | 1 | LOAD-BEARING | Written as an answer, not a fix; Ambient negative is the guard. |
| tests/test_tsconfig_walk_cost.py | tsconfig discovery derives its skip set from security._SKIP_DIRECTORY_NAMES (never re-hardcoded) and index_folder evicts the alias cache only when a tsconfig was touched. | `1bdebe7 fix: tsconfig discovery walked Rust's build tree on every watcher event (#557)` | 5 | LOAD-BEARING | Literal `C:\repo\tsconfig.json` as a touched-path input; test_the_authority_is_the_source is a source ratchet. |
| tests/test_turn_budget.py | TurnBudget resets per turn, and `auto_compacted` appears nowhere in the tree outside CHANGELOG/turn_budget.py/this file. | `cc8d87a release: v1.108.249 — a flag that claimed a compaction it never performed`; `2fb56d8 feat: session-aware routing` | 9 | LOAD-BEARING | time.sleep(0.15); text ratchet over the repo for a removed flag name. |
| tests/test_undeclared_table_guard.py | schema_driven.encode raises when rows arrive under a key no TableSpec declares, including the actual pre-fix search_ast schema. | `b49802c fix: fail closed when a producer emits a table under an undeclared key (#555)`; docstring #553/#354, @RascoApps | 1 | LOAD-BEARING | test_the_553_shape_now_raises rebuilds the real defective schema. |
| tests/test_unloadable_index_rebuild_disclosure.py | When load_index returns None, index_folder reports the substituted full rebuild and its cause as fields, including the empty-meta case that had no message. | `f22afd4 release: v1.108.243 - an unrequested rebuild is a field, not a sentence (#413)`; @LuigiNicaPRO | 9 | LOAD-BEARING | Corrupts real index dbs in tmp to reach each path. |
| tests/test_v1_108_0.py | index_folder(paths=[...]) indexes only the listed files with security warnings for bad entries, and list_workspaces/get_project_intel(scope_path) enumerate monorepo members. | `2411871 release: v1.108.0 — explicit-paths indexing + workspace-aware project intel`; `0764ebf v1.108.97 — audit P1 fixes (secret-file bypass)` | 3 | LOAD-BEARING | Named after a version rather than a behaviour; secret-file bypass on explicit paths added in .97. |
| tests/test_v1_108_101.py | Hybrid scorer normalises lexical and identity on separate scales so an exact-name match no longer divides every non-exact BM25 score toward zero. | `a37b27f release: v1.108.101 — audit W5: hybrid semantic ranking no longer collapses lexical range on an exact match`; docstring names the max_bm25 denominator defect | 1 | LOAD-BEARING | 2 tests; hand-built Symbol fixtures, no index |
| tests/test_v1_108_102.py | The tuner no longer proposes, persists or reports the write-only `identity_boost` weight while `semantic_weight` learning and `identity_hit` telemetry survive. | `1e177b0 release: v1.108.102 — audit W6: remove dead identity_boost learned weight`; docstring: "write-only dead state" | 9 | LOAD-BEARING | Removal pin (asserts absence of a key in tuning.jsonc); patches config.get for perf_telemetry_enabled |
| tests/test_v1_108_103.py | Nested/closure functions parse as `function` not `method` (V7) and a syntax error in one method no longer erases the enclosing class or declasses its siblings (V8). | `8a38bab release: v1.108.103 — audit WS-6: parser correctness (V7 + V8)` | 1 | LOAD-BEARING | 8 tests over inline Python snippets; parse_file direct |
| tests/test_v1_108_104.py | `index_dependency` is in `counter.STATE_CHANGING_ACTIONS` so `order()` refuses it without `allow_state_change=true`. | `02d9b31 release: v1.108.104 — close the index_dependency gap surfaced by PR #361`; docstring cites PR #361 readOnlyHint annotations | 4 | LOAD-BEARING | Reads live `server._catalog_names()` |
| tests/test_v1_108_105.py | incremental_save / save_branch_delta / SCIP ingest take the `indexwrite` lock keyed like save_index, and load_index returns None on a corrupt .db instead of raising sqlite3.DatabaseError. | `f580cd4 release: v1.108.105 — audit V9: index-write lock on delta paths + load_index corruption guard` | 3 | LOAD-BEARING | Lock tests are per-path; test_v1_108_108.py holds the parametrized four-path version, but this file alone covers V9b corruption guard |
| tests/test_v1_108_106.py | incremental_save persists `package_names` into meta and None preserves the existing value; branch deltas deliberately do not. | `0d5cb63 release: v1.108.106 — audit W7: incremental_save refreshes package_names` | 3 | LOAD-BEARING | Asserts the documented non-plumbing of branch deltas |
| tests/test_v1_108_107.py | The watcher fast path reuses the cached discovered-provider set so a watched edit keeps git-blame/ecosystem enrichment instead of dropping it until full reindex. | `d944959 release: v1.108.107 — audit W1: watcher fast path re-enriches changed symbols`; later touched by db63d2c (v1.108.182 provider budget) | 3 | LOAD-BEARING | Spawns `git` subprocess to build a repo fixture; comment line 84 names "the W1 regression" |
| tests/test_v1_108_108.py | `_write_cached_text` writes via temp file + os.replace, and one parametrized test proves all four store-writing paths (save_index, incremental_save, save_branch_delta, SCIP ingest) hold the indexwrite lock. | `f46d76c release: v1.108.108 — audit WS-5 tail: atomic content writes + 4-path persistence test` | 3 | LOAD-BEARING | Four-path parametrize is the property-shaped version of test_v1_108_105.py's per-path checks; reads dest files back with read_text |
| tests/test_v1_108_110.py | `check_embedding_drift` carries readOnlyHint=False but stays out of STATE_CHANGING_ACTIONS so `order()` runs its drift report without opt-in. | `e8d5a30 release: v1.108.110 — check_embedding_drift readOnlyHint=False (suite parity)`; docstring cites jdoc v1.93.0 / jdata v1.17.0 parity | 4 | LOAD-BEARING | Asserts a deliberate annotation/gate asymmetry; reads live `_build_tools_list()` |
| tests/test_v1_108_114.py | `route()` classifies edit/execute intents to read-only prep tools (check_rename_safe etc.) and session/delta intents to get_changed_symbols/get_session_context, without regressing existing routes. | `7f2bb1c release: v1.108.114 - route() mutate + stateful intent buckets`; docstring: "the leakage finding" | 4 | LOAD-BEARING | 9 tests; catalog is a hand-typed NAMES superset, not the live catalog |
| tests/test_v1_108_115.py | Only a genuinely first-ever install defaults `tool_surface` to `counter`; an existing config lacking the key, an existing install with no config, and an explicit choice all resolve to `full`/the choice, and upgrade_config injects only a commented line. | `469763f release: v1.108.115 - new installs default to the counter tool surface`; docstring: "existing installs are never silently collapsed on a package update" | 6 | LOAD-BEARING | 14 tests; mutates `C._GLOBAL_CONFIG` directly and reads config.jsonc text; the freeze it pins is why surface_offer.py exists |
| tests/test_v1_108_118.py | SCIP `scip_edges` reference rows are unioned into get_blast_radius's affected-file set and get_call_hierarchy's caller edges via the attach helpers, honest-None when the table is absent. | `bea91bb v1.108.118 — SCIP compile-time evidence P2: graph consumers` | 1 | LOAD-BEARING | 11 tests against a hand-built SQLite db with `_FakeSqlite` store; no protobuf |
| tests/test_v1_108_119.py | find_implementations gains a `scip_implementation` channel (confidence 1.0) fed by `scip_edges.kind='implementation'`, surfacing implementations the AST/duck channels miss. | `35b6044 v1.108.119 — SCIP compile-time evidence P2 increment 2: find_implementations channel` | 1 | LOAD-BEARING | One integration test builds a real index and injects an edge via SQL |
| tests/test_v1_108_120.py | A compiler-verified SCIP reference flips check_delete_safe from safe_to_delete to scip_referenced and check_edit_safe from safe_to_edit to signature_impact. | `42ffec2 v1.108.120 — SCIP compile-time evidence P2 increment 3: safety preflights (marquee)` | 1 | LOAD-BEARING | Drives shared reader `scip_reference_files`; two real-index integration tests |
| tests/test_v1_108_121.py | The `license` CLI reads a `license_key` persisted in config.jsonc (surfaced as `key_masked` under `--json`) even though its handler returns before `load_config()`. | `adf5382 release: v1.108.121 — license CLI reads config-file license_key (#364)`; docstring "Regression test for #364" | 6 | LOAD-BEARING | Spawns `python -m jcodemunch_mcp license --json` under isolated CODE_INDEX_PATH; fake key so mode is `grace` — the validator's outbound call is expected to fail; check under N5 whether it actually attempts network |
| tests/test_v1_108_122.py | `check_gate().key_status` classifies valid/absent/rejected/unverified_offline honestly, `format_license_status()` never tells a valid licensee they are unlicensed, and premium-pack downloads honour env/config `license_key`. | `9981c45 release: v1.108.123 — licensing hardening sweep (#364 confidence pass)`; docstring: "two field reports from a paying client... no strike 3" | 6 | LOAD-BEARING | 13 tests; autouse isolation under tmp CODE_INDEX_PATH |
| tests/test_v1_108_125.py | `.mjs`/`.cjs` map to `javascript` in LANGUAGE_EXTENSIONS, resolve via get_language_for_path, and yield symbols. | `bd9f89a release: v1.108.125 — native .mjs/.cjs indexing as JavaScript (#365)`; docstring credits @oderwat | 10 | LOAD-BEARING | 3 tests; extension-map pin |
| tests/test_v1_108_126.py | Hitting `max_folder_files` produces a `truncated` cap report with discovered/indexed/skipped counts that is persisted and attached to query responses. | `cbcff9e release: v1.108.126 — loud, persisted max_folder_files truncation (#366)` | 3 | LOAD-BEARING | Asserts exact dict shape from `_file_cap_report` |
| tests/test_v1_108_127.py | A full (non-incremental) `index_repo` re-index preserves summaries via dict access and no longer crashes with `'dict' object has no attribute 'summary'`. | `03cd93f release: v1.108.127 — fix index_repo full re-index crash (#367)` | 3 | LOAD-BEARING | 2 async tests (`@pytest.mark.asyncio`, so the `def test_` grep count is 0); GitHub fetch monkeypatched; pre-seeds an index via IndexStore |
| tests/test_v1_108_128.py | Auto-detect never selects a PAID cloud summarizer from a bare env key without an explicit provider name or `allow_paid_summaries`; free/local endpoints still auto-enable. | `a04df81 release: v1.108.128 — never auto-bill a paid cloud provider from a bare env key` | 6 | LOAD-BEARING | Autouse clears seven API-key env vars; the 08-18 #488 "principle over a set" lesson is the same axis |
| tests/test_v1_108_129.py | entropy_prune keeps keystone lines (control flow etc.) and get_ranked_context(compress=True) is byte-identical by default, fits more under budget, and never drops a keystone. | `c11a0a0 release: v1.108.129 — keystone-protected structural compression for get_ranked_context` | 2 | LOAD-BEARING | 13 tests; uses a chars/4 token proxy rather than the real estimator |
| tests/test_v1_108_134.py | receipt's `--since`/`--until` parse calendar bounds (until exclusive), `--by-day` yields a per-day series in one scan, and `--rates` dumps the price table. | `c9bbae6 release: v1.108.136 — receipt calendar windows, --rates, per-day savings meter` | 9 | LOAD-BEARING | 20 tests; imports `_MODEL_PRICES_USD_PER_MTOK` — adjacent to the .314 sonnet-rate correction; check for derived-dollar literals that a rate change would move |
| tests/test_v1_108_136.py | The savings-meter flush credits each batch into a per-local-day `daily` map in `_savings.json`. | `c9bbae6 release: v1.108.136 — receipt calendar windows, --rates, per-day savings meter`; docstring: transcript scans were "four orders of magnitude under the meter" | 9 | LOAD-BEARING | Reads `_savings.json` off tmp_path; `_share_savings` patched to a no-op (the network path neutralised) |
| tests/test_v1_108_137.py | Source-shaped query tokens seed exact-name matches ahead of ranked results with `match_channel="exact_name"`, prose queries stay byte-identical, every tool carries openWorldHint, and all SQLite stores set `journal_size_limit`. | `64a9ca9 release: v1.108.137 — source-shaped exact seeding in ranked context; openWorldHint annotations; WAL size bound` | 1 | LOAD-BEARING | 19 tests spanning three unrelated changes (retrieval, annotations, WAL) — split candidate; builds a real index per test |
| tests/test_v1_108_139.py | `measured_provenance()` mirrors the MEASURED registry as a fresh copy and rides receipt JSON/text, get_session_stats and jcodemunch_guide, off the hot retrieval path. | `63fd598 release: v1.108.139 — measured provenance rides the reporting surfaces` | 9 | LOAD-BEARING | Reads `benchmarks/provenance/measured.json` via the module; sibling of test_provenance.py |
| tests/test_v1_108_146.py | `session_token_budget` yields an advisory {limit, spent, state ok/approaching/over} block and session stats carry a `yield` block from served/fetched/edited signals. | `4addf0e release: v1.108.146 — token yield + advisory session budgets` | 9 | LOAD-BEARING | 15 tests; swaps `token_tracker._state` with a fresh `_State` |
| tests/test_v1_108_147.py | Concurrent cold `load_index` calls hydrate once (single-flight lock) and the per-index BM25 build is double-checked under `_bm25_lock` so eight threads do not each rebuild. | `97f6ab2 release: v1.108.147 — single-flight cold index loads and BM25 builds (#370)` | 5 | LOAD-BEARING | 8-thread concurrency test; does not obviously pin the interleaving (08-17 #490 lesson) — non-vacuity worth checking |
| tests/test_v1_108_148.py | plan_turn opens a token estimate that the next plan_turn closes against served tokens; after 3 samples `estimate_calibration` appears in stats and on the budget block. | `3f3c902 release: v1.108.148 — estimate-vs-actual consumption receipts (plan_turn prices its route; session calibrates the estimates)` | 9 | LOAD-BEARING | 14 tests; same fresh_state fixture pattern as test_v1_108_146.py |
| tests/test_v1_108_149.py | Every context-serving sort carries a total-order tiebreak (symbol id / path) so tied scores order identically regardless of channel or storage order. | `41d7c7c release: v1.108.149 — deterministic emission (total-order sort tiebreaks) + cache-stability baseline harness` | N4 | LOAD-BEARING | 4 tests; forward/backward channel order must agree — a determinism pin |
| tests/test_v1_108_150.py | `_session_key()` falls back to the streamable-http auth principal only when a principal was captured AND the session has no `session_id` (the stateless-MCP shape). | `e04dd83 release: v1.108.150 — stateless-MCP forward cover: auth-principal session keying + SSE deprecation notice` | 6 | LOAD-BEARING | 10 tests with fake request-context objects; forward cover for the 2026-07-28 MCP spec change |
| tests/test_v1_108_151.py | A linked git worktree under the indexed root is pruned by discovery, refused by the watcher fast path, and never claims or matches into the parent repo's identity slot. | `22a71f1 release: v1.108.151 — nested-worktree exclusion + per-worktree identity (#372)` | 3 | LOAD-BEARING | Builds fake `.git` file worktrees; subprocess git for real-worktree cases |
| tests/test_v1_108_152.py | The `munch://runtime/identity` resource carries instance id, process start, transport, `launch_id` from JCODEMUNCH_LAUNCH_ID (fallback MUNCH_LAUNCH_ID, omitted when unset). | `77147d8 release: v1.108.152 - munch.runtime.identity/v1 resource (#371)` | 9 | LOAD-BEARING | 14 tests; autouse save/restore of module-global identity state |
| tests/test_v1_108_153.py | `_tool_surface_stats()` reports visible vs catalog schema tokens (bytes/4) with `avoided == catalog - visible`, attached to get_session_stats and never breaking it on helper failure. | `5a316af release: v1.108.153 — tool_surface schema receipt in get_session_stats` | 4 | LOAD-BEARING | Invariant-shaped (no literal counts), so it survives catalog growth |
| tests/test_v1_108_156.py | `order()` maps near-miss arg names (`path`->`file_path`) onto the action's schema properties and moves a list on a singular prop to its plural instead of raising an internal ValueError. | `76c920d release: v1.108.156 — Counter order() maps near-miss arg names onto the schema`; `05b0b69` (.157 plural move); docstring: "one wasted error-turn per session in the 2026-07-22 codebench arm run" | 4 | LOAD-BEARING | 12 tests over live `_order_action_properties` |
| tests/test_v1_108_158.py | Hop tools (search/outline/source) are counted per session and a once-only escalation hint toward get_ranked_context/assemble_task_context fires after N hops with no bundle call. | `7dec620 release: v1.108.158 — turn-economy steering toward the one-call context openers`; docstring cites 2026-07-22 harness runs | 2 | LOAD-BEARING | Mutates `server._steer_state`; advisory-only contract |
| tests/test_v1_108_159.py | `_looks_like_path` accepts `.`, `..`, `~`, Windows and POSIX absolute forms and rejects bare names / owner/name ids; steering surfaces carry compress=True guidance. | `62c2ac7 release: v1.108.159 — response-size steering ... + path-shaped repo acceptance in the shared resolver` | 6 | LOAD-BEARING | Literal `C:\\MCPs\\gin` and `/c/MCPs/gin` inputs — path-SHAPE probes, not touched on disk |
| tests/test_v1_108_160.py | resolve_repo and index_file attach a `working_tree_mismatch` warning naming cheap remedies when the queried checkout differs from the indexed source_root. | `e50d08f release: v1.108.160 — multi-checkout 'different working tree' responses on resolve_repo + index_file`; docstring: "$5.64 vs $2.90 for the tainted rep" | 3 | LOAD-BEARING | 8 tests; tmp_path checkouts |
| tests/test_v1_108_161.py | `_tokenize` splits on Unicode word boundaries and expands CJK runs into character bigrams (Korean -> exact bigram list) while accented Latin survives. | `b16090a release: v1.108.161 — BM25 tokenizer: Unicode word splitting + CJK character bigrams`; docstring: "jdoc #91 class", parity with jdoc v1.114.1 / jdata v1.23.1 | 10 | LOAD-BEARING | Asserts an exact bigram sequence for a Korean phrase; `# -*- coding: utf-8 -*-` header |
| tests/test_v1_108_162.py | finalize_handoff builds a deterministic Markdown handoff, attests evidence_refs against served ids, persists session-scoped, and `munch://handoff/<id>` serves the immutable body. | `b12a9ab release: v1.108.162 — canonical handoff contract: finalize_handoff + munch://handoff/<id> (#374)` | 9 | LOAD-BEARING | 22 tests; docstring names test_v1_108_74.py / test_v1_108_152.py / test_counter.py as its models; also asserts the #371 resource is unaffected |
| tests/test_v1_108_163.py | The license key travels as POST form body (validate.php) or `X-JCM-License` header (packs), never in a URL, and no legacy GET/query retry is attempted. | `2d961d4 release: v1.108.198 - the transitional fallbacks outlived their transition` (rewrote the file); origin audit WS-8 V12 per docstring | 8 | LOAD-BEARING | Installs a fake `httpx` into sys.modules and records every URL — a key in a URL is the defect; later edits by .248/.252 |
| tests/test_v1_108_164.py | A path-shaped `repo` arg (multi-segment) returns a routed, actionable tool error instead of leaking `ValueError: Path separator in name` from `_safe_repo_component`. | `52bd24f release: v1.108.164 - path-shaped repo arg returns a routed error (#376)`; docstring: split out of #375, Ubuntu install | 6 | LOAD-BEARING | SimpleNamespace fake store; monkeypatches `_compute_repo_id` |
| tests/test_v1_108_165.py | Sections may carry per-claim `evidence_refs` attested separately, and an input with no claims produces the byte-identical v1 body, id and receipt. | `45b7c1a release: v1.108.165 — claim-scoped evidence in the handoff contract (#377 phase 1)` | 9 | LOAD-BEARING | Backward-compat byte-identity pin; line 52 "indistinguishable from pre-#377 output" |
| tests/test_v1_108_166.py | Only an `absent` verdict can be cited as absence evidence; low_confidence/degraded, a stale index, a truncated index, and unknown coverage each refuse, with unknown disclosed as unknown. | `256ee56 release: v1.108.166 — absence evidence: cite a zero-result scan as proof (#377 phase 3)` | 3 | LOAD-BEARING | 23 tests; each refusal rule pinned separately per docstring |
| tests/test_v1_108_167.py | The delivery ledger records same-symbol-different-query redelivery, reports `redelivery_rate`, and annotates `_meta.already_delivered` without changing any body. | `0cb72bd release: v1.108.167 — cue-anchored delivery ledger (measure what we hand over twice)`; cites docs/prd-cue-anchored-delivery.md | 2 | LOAD-BEARING | 27 tests; the cited PRD lives under gitignored `docs/` — verify it exists (cf. the get_endpoint_impact phantom-PRD note in CLAUDE.md) |
| tests/test_v1_108_168.py | An index being rewritten under a scan (filesystem signal, not in-process `reindex_state`) downgrades the verdict so no `absent` ref is minted over a half-written index. | `3f116c3 release: v1.108.168 — a rebuild underneath a scan cannot prove absence (5th refusal rule)` | 3 | LOAD-BEARING | Reads `schemas/retrieval-verdict.schema.json` off disk (line 187) |
| tests/test_v1_108_169.py | Every compact encoder preserves `_meta.verdict`, and `search_text` receives `index_changed=` so the rebuilding refusal can fire there too. | `f28b3cb release: v1.108.169 — the retrieval verdict survives compaction` | 3 | LOAD-BEARING | Ratchet: `inspect.getsource` scan at line 176 for the search_text wiring; pins all 15 schemas' `_meta` allowlists |
| tests/test_v1_108_171.py | Context-provider regexes scan in linear time: 4x the input may not cost ~16x the time, and the Express mount pattern carries no leading `(?:\w+)`. | `1ff7c60 release: v1.108.171 — provider discovery no longer scans in quadratic time`; docstring "jcm#375", py-spy, 8k/32k/128k timings | 5 | LOAD-BEARING | Empirical timing test (`perf_counter` scaling ratio), deliberately not a pattern-text assertion; input capped at 32k so a regression hangs rather than fails; timing-sensitive under load (N1) |
| tests/test_v1_108_172.py | The idle index-cache TTL is opt-in (0/unset/garbage = off), the process presence registry records live servers, and `_is_pid_alive` no longer reports dead Windows children as alive. | `ec47a62 release: v1.108.172 — opt-in idle index-cache TTL + process presence registry`; docstring: 25+ instances, ~17 GB, #370/#375 | 5 | LOAD-BEARING | 17 tests; spawns a subprocess for the PID-liveness case; Windows handle semantics |
| tests/test_v1_108_173.py | An identifier-shaped query with no exact match is labelled (`exact_match_report`, `is_identifier_query`) rather than served as confident hits on substring tokens. | `a249bd6 release: v1.108.173 — exact-match honesty in search_symbols`; docstring: jcm#375 suggestion B, `runPromiseFulfillmentCheck` | 1 | LOAD-BEARING | Labels, does not suppress — asserts the guesses still return |
| tests/test_v1_108_174.py | `list_repos` on an empty store returns `empty: True` plus a top-level nudge (not in `_meta`, which siblings strip). | `19277b1 release: v1.108.174 — an empty index says so`; docstring: jcm#375 suggestion C | 9 | LOAD-BEARING | 4 tests; suite-parity key names |
| tests/test_v1_108_175.py | Unrecognised argument keys (`regex` for `is_regex`) are disclosed on every state and downgrade an `absent` verdict to `degraded` so an ignored flag cannot back an absence claim. | `2e83a68 release: v1.108.175 — an ignored argument cannot prove absence`; docstring: found live auditing #375 | 3 | LOAD-BEARING | 19 tests over `apply_argument_contract`/`unrecognized_keys` |
| tests/test_v1_108_176.py | Index coverage (files dropped by the second pass, counted rather than silently `continue`d) is reported separately from SHA freshness, and incomplete coverage refuses absence. | `e2eac18 release: v1.108.176 — a partial index stops reporting itself fresh`; docstring: #375 sub-problem C, "7,659 of 9,634 code files" | 3 | LOAD-BEARING | 20 tests; reads persisted coverage JSON; line 108 notes pre-existing indexes lack the field |
| tests/test_v1_108_177.py | Budget-packer drops, empty context packs, empty scopes, unreadable files, and `meta_fields` stripping each disclose and downgrade `absent` to `degraded` (#377 items 1,7-12). | `bd87053 release: v1.108.177 — an omitted match is not an absent match`; later `1ba02a0` (.180) | 3 | LOAD-BEARING | 21 tests; item 10 (a display preference deleting gate fields) is the same class as #572 |
| tests/test_v1_108_178.py | `subject_state.capture()/changed()/revalidate_verdict()` detect index generation, .db mtime, git HEAD and working-tree changes so a cached `absent` is downgraded when the subject moved; UNKNOWN is never a change. | `1e2face release: v1.108.178 — a cached negative must prove it still describes the subject`; #377 item 3 | 3 | LOAD-BEARING | 17 tests; the cache-revalidation path analyze_perf's `hits_validated_*` buckets depend on |
| tests/test_v1_108_179.py | A zero-result scan whose before/after subject identity differs (`moved_during_scan`) cannot reach `absent`; a scan that returned rows is not refused. | `5ab2834 release: v1.108.179 — the subject has to hold still for the scan`; #377 item 6 | 3 | LOAD-BEARING | 14 tests; `_Idx` stand-in with plain attributes |
| tests/test_v1_108_180.py | `repo_freshness` returns fresh/stale/unknown/not_tracked; `unknown` refuses absence and `not_tracked` discloses; and (#565) get_watch_status reports index freshness, not watcher bookkeeping. | `1ba02a0 release: v1.108.180 — unknown freshness stops collapsing into fresh`; `d0223c4 fix: get_watch_status reported watcher bookkeeping as index freshness (#565)` | 3 | LOAD-BEARING | 27 tests; #565 block appended at line 282; subprocess git |
| tests/test_v1_108_181.py | `working_tree_state()` yields clean/dirty_in_scope/dirty_outside_scope/unknown/not_applicable and blocks only on in-scope dirt the index has not re-read. | `89f9042 release: v1.108.181 — uncommitted work is represented, per scope`; `c41defe test: a fixture helper named _state was outranking the _State replay anchor` | 3 | LOAD-BEARING | 23 tests; the `_state` helper-name collision is the reason later files prefix helpers `_r18x` |
| tests/test_v1_108_182.py | A provider overrunning `JCODEMUNCH_PROVIDER_BUDGET_SECONDS` is skipped and named, source walks prune dependency trees at the walk, and a pathological file's parse is bounded and named. | `db63d2c release: v1.108.182 — a stall has a name and a ceiling (#375 reopened)`; docstring cites Darius Kiaulakis' 268s vs 240s re-run | 5 | LOAD-BEARING | 23 tests; wall-clock budgets — timing-adjacent under a loaded box |
| tests/test_v1_108_183.py | A served symbol no longer attests an unrelated file-level ref (non-vacuity: the file ref IS attested when narrowing is disabled); evidence envelope identity, snapshot binding, `munch://evidence/<id>`, and the producer-registration gate. | `11be8c1 release: v1.108.192 - #377 P3`; `1f9d6dd` (.240); `d6ecb4f test: the schema budget guard asserted a file against copies of itself`; origin v1.108.183 per docstring | 3 | LOAD-BEARING | 75 tests — largest file in slice; end-to-end through the real dispatcher; reads `schemas/evidence-receipt.schema.json`; helpers prefixed `_r183` |
| tests/test_v1_108_184.py | The semantic exit of search_symbols builds the same `_meta.verdict` as the lexical exit, and a zero result from an embedding ranking can never reach `absent`. | `a11cad3 release: v1.108.184 — a ranking cannot prove absence`; docstring: `best_match_score: 53.774` beside "Do not claim this feature exists" | 3 | LOAD-BEARING | 30 tests; reads retrieval-verdict schema off disk twice; validates against JSON schema |
| tests/test_v1_108_185.py | Both fusion exits emit a verdict, fusion CAN prove absence (every candidate scored on BM25+identity), and `build_structural_channel` distinguishes empty `candidate_ids` from None. | `4e49d46 release: v1.108.185 — fusion gets a verdict, and fusion can prove absence` | 3 | LOAD-BEARING | 35 tests; ratchet reads `src/jcodemunch_mcp/tools/<module>` source text (lines 361/375/531); line 191 pins the `mode=ro` mtime mechanism |
| tests/test_v1_108_186.py | `_get_ranked_context_fusion` records `semantic_used=False`; pre-fix `get_ranked_context_fusion` rows are excluded from WeightTuner via `ledger_trust`, not rewritten and not counted as off. | `2fd7db5 release: v1.108.186 — the ledger stops claiming a channel that never ran`; docstring: mislabelled rows walked semantic_weight 0.5 -> 0.45 | 9 | LOAD-BEARING | 22 tests; `inspect.getsource` ratchet on the fusion exit (lines 191, 402); drift guard for the rule expiring if that exit gains a similarity channel |
| tests/test_v1_108_187.py | Both fusion exits pass `extract_ledger_features` with an `identity` key so `top1_score`/`identity_hit` are real; pre-fix get_ranked_context rows are excluded via `identity_label_is_trustworthy`, search_symbols_fusion history is declared unseparable. | `38b28f9 release: v1.108.187 — both fusion exits record the features they measured` | 9 | LOAD-BEARING | 24 tests; helpers `_r187`; replay fixture queries analyze_perf |
| tests/test_v1_108_188.py | ranking_events and tool_calls rows are written to the store the caller's `storage_path` named, while the lifetime savings total keeps its first-call `_base_path`. | `32ede68 release: v1.108.188 — telemetry rows land in the store the caller named`; docstring: a reproduction wrote into a developer's real telemetry db | 9 | LOAD-BEARING | 14 tests; asserts the deliberate non-change of the savings anchor |
| tests/test_v1_108_189.py | HeartbeatReporter emits WARNING log lines only after `JCODEMUNCH_HEARTBEAT_SECONDS` elapses, holds no notify channel, garbage parses to the default (#383); index_folder under auto-watch indexes once via scoped deferral (#384). | `9e1a11b release: v1.108.189 - the two residual #375 sub-problems, closed`; `66c7371` (.191) | 9 | LOAD-BEARING | 19 tests; manual `_Clock` for determinism; pins DECISIONS (a fast run stays silent) |
| tests/test_v1_108_190.py | `maybe_takeover` skips the initial index for a tree the tool just indexed, and the hash cache hydrates lazily so a failed initial index does not silently drop every `old_hash`. | `f7b78e9 release: v1.108.190 - the takeover half of the double index (#388)`; docstring credits @Bortlesboat PR #388 | 3 | LOAD-BEARING | 2 tests; `inspect.getsource` ratchet at line 187; `_Recorder` captures `_start_watch_task` |
| tests/test_v1_108_191.py | Both arms of `_auto_watch_if_needed` (takeover and fall-through) index a folder exactly once — no concurrent writer on `indexwrite`, no redundant full walk. | `66c7371 release: v1.108.191 — the eager auto-watch path indexes once` | 3 | LOAD-BEARING | 2 tests; `inspect.getsource(watcher.WatcherManager)` ratchet at line 233 |
| tests/test_v1_108_192.py | An absence receipt binds to an immutable snapshot (not the mutable `absent:<sha12>` record) and rendering reuses the validated envelope rather than performing a second lookup that eviction can defeat. | `11be8c1 release: v1.108.192 - #377 P3: one immutable snapshot, start to finish`; docstring credits @mightydanp's adversarial edges | 3 | LOAD-BEARING | 10 tests; `inspect.getsource` on `handoff._render_receipt_detail` and `producers` (lines 222/296) so a mirror table cannot drift |
| tests/test_v1_108_193.py | `JCODEMUNCH_MAX_FILE_SIZE`/`max_file_size` override the cap on all walks and the project file; a `too_large` file is WITHHELD, makes coverage `complete: false`, and refuses absence. | `c1c891b release: v1.108.193 - the size cap can be raised, and a withheld file cannot prove absence`; `75e876f` (.194 one walk of three); `3dd3424` (.197 wrong config file, #390 @amarakramali) | 3 | LOAD-BEARING | 19 tests; credits @dkiaulakis; line 76 grep-shaped guard acknowledged as unable to tell refactor from regression; cross-refs test_tools.py #301 |
| tests/test_v1_108_194.py | The `config` reporter prints every indexing limit the resolver honours (asserted over the SET of limits, not `max_file_size` alone), reproducing what a bare REPL without `load_config()` cannot show. | `a294c3f release: v1.108.196` (file origin); `5a3ee39 test: isolate the config FILE in _run_config, not just the env var (#411)`; `85f3fb6 test: two guards read ambient machine state instead of declaring it` | 6 | LOAD-BEARING | Spawns the CLI; SRC path via `parents[1]`; #411/#437/#566 isolation fixes recorded in comments — Practice 8 witness |
| tests/test_v1_108_199.py | No in-server subprocess inherits the JSON-RPC stdin (#392, source scan over src with by-name exemptions), `config --check` reports effective loaded values (#393), and find_references paginates after enrichment (#394). | `24ec17b release: v1.108.199 — a convention everybody followed and nothing enforced`; `0ab83f8` back-ports jdoc/jdata hardening; `51c5e90` hooks package refactor | 6 | LOAD-BEARING | 15 tests; ratchet walks `src/jcodemunch_mcp` for `subprocess.{run,Popen,...}`; one `skipif(sys.platform != "win32")` (the deadlock repro) — counts toward N7 on ubuntu; credits @rknighton |
| tests/test_v1_108_2.py | `index_folder` skips the git-root probe when `git_root_identity=false`, and GitBlameProvider bounds `git log` by `-n 20000`/`--since=2.years.ago`/10s and can be disabled via `git_blame_enabled`. | `eb810c8 release: v1.108.2 — bound git-blame walk + honour git_root_identity=false`; docstring: #294 by @MariusAdrian88 | 5 | LOAD-BEARING | 8 tests; oldest file in slice (2026-05-12); subprocess git |
| tests/test_v1_108_200.py | SCIP staleness is per-file (a moved HEAD is necessary, not sufficient) and still fails toward stale when provenance is missing. | `3ccf6b8 release: v1.108.200 — a staleness warning that fires on nearly every read protects nothing` | 3 | LOAD-BEARING | 8 tests; hand-built sqlite `meta`+`files` tables via executescript |
| tests/test_v1_108_201.py | The delivery ledger's `redelivery_rate` is persisted through an opt-in sink with the pooling rule the benchmarks/anchor aggregate depends on. | `f933fe2 release: v1.108.201 - persist the delivery ledger so P0's number can be collected`; `f41d956` (.202 routes session_yield to the named store) | 9 | LOAD-BEARING | 11 tests; `sys.path.insert` of `benchmarks/anchor` — a test importing benchmark harness code; `importlib.reload(tt)` per test |
| tests/test_v1_108_206.py | `_callees_from_references` builds its name index once per traversal and returns the same rows as the pre-change algorithm, including the same-file-all vs cross-file-first duplicate-name asymmetry. | docstring: "#396 (@rknighton)"; git `c769f25 release: v1.108.205 - packs say whose code they contain` | 5 | LOAD-BEARING | Oracle is the v1.108.204 function copied verbatim into the test; randomized fixtures with `random`; asserts a work count, not wall time. |
| tests/test_v1_108_207.py | The native embedding backend (`onnxruntime`) is imported on the main thread in `main()` before any event loop, never inside the `to_thread` worker. | docstring: "jdatamunch-mcp#3, same defect here"; git `f9cc4d7 fix: import the native embedding backend on the main thread at startup`, `c2201a5 test: verify the embed warm-up on CI` | 6 | LOAD-BEARING | Ratchet: parses `server.py` AST via `inspect.getsourcefile`; `skipif(not ONNX_INSTALLED)` on one test. |
| tests/test_v1_108_208.py | `content_hash` is stripped from `get_symbol_source` rows in the dispatcher AFTER receipt minting, so receipts keep `hash_source=index_content_hash` while the wire drops ~83 B/row. | docstring; git `75eab1a release: v1.108.208 — content_hash stops riding every get_symbol_source response` | 2 | LOAD-BEARING | `TestTheReceiptIsUnaffected` guards the tool-side-gate regression; helpers prefixed `_r208_` to stay out of golden replay ranking. |
| tests/test_v1_108_209.py | `FreshnessProbe.classify` returns `unknown`, never `fresh`, when no source root / moved root / absent file / stat error / no baseline. | docstring: "#377 item 4, one level down"; git `7ceca0a release: v1.108.209` | 3 | LOAD-BEARING | Pure unit over the probe; summary buckets must sum to entry count. |
| tests/test_v1_108_210.py | `EmbeddingStore.get_many(ids)` fetches only named ids over a read-only connection, chunked at `_GET_MANY_CHUNK`, and does not bump db mtime. | docstring: "#398 Arc 5 (@rknighton)"; git `a03c220 release: v1.108.210` | 3 | LOAD-BEARING | Chunk boundary test at 900 (SQLITE_MAX_VARIABLE_NUMBER); raw `sqlite3` inspection. |
| tests/test_v1_108_211.py | `find_similar_symbols` distinguishes "repo not embedded" from "these candidates not embedded" via tri-state `has_any()` (None is never False). | docstring: "Follow-on to #398 Arc 5"; git `f2ee2d8 release: v1.108.211` | 9 | LOAD-BEARING | Tri-state assertion; note text differs per state. |
| tests/test_v1_108_223.py | `embedding_matrix` decodes the store once per stamp, numpy and pure-Python lanes agree with `_cosine_similarity`, cache invalidates on write and read does not bump mtime. | docstring: "#399 (@vondecron)"; git `415ca33 release: v1.108.223` | 3 | LOAD-BEARING | Forces numpy absent for the fallback lane; stamp covers `-wal`/`-shm` sidecars; v1.108.185 mtime class re-asserted. |
| tests/test_v1_108_224.py | `_verify_against_git_sha` compares raw bytes (no `text=True`) and slices from the declaration's first token, so CRLF clones and indented symbols verify. | docstring: "#400 and #401 (@rknighton)"; git `9850d6f release: v1.108.227`, `e3663c5 test: retire the src.jcodemunch_mcp package twin` | 1 | LOAD-BEARING | `pytestmark skipif` git absent; docstring says `test_git_sha_verification.py`'s fixture cannot exercise either defect; `TestDivergenceStillDetected` is the anti-loosening half. |
| tests/test_v1_108_225.py | Per-file `classify()` no longer short-circuits to `stale_index` on a repo-SHA mismatch, and a revalidated cache hit restamps `_freshness` rather than replaying the fill-time value. | docstring: "#405 and #404 (@rknighton)"; git `d79c225 release: v1.108.225` | 3 | LOAD-BEARING | `pytestmark_git skipif`; establishes that `repo_is_stale: true` beside `fresh` rows is legitimate. |
| tests/test_v1_108_226.py | `check_references` skips only the definition line, not the whole defining file; the TaskCompleted hook reads a key the payload carries; `index_repo` follows GitHub 301 redirects. | docstring: "#406 and #407 (@rknighton)"; git `86928cf release: v1.108.226` | 1 | LOAD-BEARING | Ratchets via `inspect.getsource(hooks.run_taskcomplete)` and a `read_text` scan; httpx redirect test is mocked (no network). |
| tests/test_v1_108_227.py | `git_sha` verification distinguishes a post-index commit (Form A, `rev`) from a dirty-at-index working tree (Form B, `git_sha_uncommitted`) instead of reporting mismatch. | docstring: "#402 (@rknighton)"; git `9850d6f release: v1.108.227` | 3 | LOAD-BEARING | `pytestmark skipif` git absent; also pins `verify_against` in `_COMPACT_STRIP_PARAMS` (v1.108.177 class). |
| tests/test_v1_108_228.py | Every channel builder and the semantic scorer break ties on `(-score, symbol_id)`, so numpy float32 and Python float64 lanes produce the same order. | docstring: "#398 Arc 4 / #403"; git `8bed872 release: v1.108.228` | N4 | LOAD-BEARING | Ratchet over `inspect.getsource` of search_symbols and channel modules; `importorskip("numpy")` on one test; synthetic near-tied 4,000-vector corpus. |
| tests/test_v1_108_229.py | `get_dead_code_v2` keeps module-level callers when `callers_by_name` is non-empty (fixture has BOTH intra-symbol and module-level calls). | docstring: "(#409)"; git `a7ba4c6 release: v1.108.229` | 1 | LOAD-BEARING | Docstring warns either call alone passes on pre-fix code; fixture design is load-bearing. |
| tests/test_v1_108_230.py | `get_dead_code_v2` reports per-signal fire rates and `uninformative` diagnostics; confidence counts only signals that voted. | docstring: "(#408, instrument only)"; git `992af0a release: v1.108.230`, `8824d53 release: v1.108.231` | 9 | LOAD-BEARING | One test docstring reads "SUPERSEDED BY v1.108.231" and was rewritten to the .231 contract; partially overlaps `tests/test_v1_108_231.py` but keeps the diagnostics-shape assertions. |
| tests/test_v1_108_231.py | A dead-code signal with a degenerate fire rate (`_DEGENERACY_CUTOFF`) gets no vote; denominator stays 3 so the ceiling falls. | docstring: "(#408)"; git `8824d53 release: v1.108.231`, `d6ecb4f test: the schema budget guard asserted a file against copies of itself` | 1 | LOAD-BEARING | Cites measured 57.3% mean dead rate across 31 repos as motivation. |
| tests/test_v1_108_234.py | `backup`, `old`, `archive` are skipped as whole path segments by both index paths, recoverable via `exclude_skip_directories`, and counted in `discovery_skip_counts`. | docstring (duplicate-source-tree diagnosis); git `cd8885a release: v1.108.234` | 8 | LOAD-BEARING | Negative tests (a file named `old_thing.py`, `backups/`) are the emphasised half. |
| tests/test_v1_108_269.py | An oversize file withheld on the `os.walk` path is warned about (and counted by `index_repo`), and `max_file_size` is reachable per call over MCP. | docstring: "#429, filed against 1.108.264"; git `b5c5128 release: v1.108.269`, `f03bc6b fix(tests): stop the suite reading and writing the developer's real config` | 3 | LOAD-BEARING | 29 tests; ratchets via `ast.parse(inspect.getsource(discover_source_files))` and `inspect.getsource(server)`; comment about not reading `~/.code-index/config.jsonc` (#411). |
| tests/test_v1_108_270.py | A directory holding a `CACHEDIR.TAG` whose first 43 bytes are the spec signature is pruned; a file merely NAMED that excludes nothing; counted as `cache_dir`, not withheld. | docstring (bford.info/cachedir, jdoc#113); git `5fc7e87 release: v1.108.270` | 8 | LOAD-BEARING | Byte-level signature pin; source-scan via `inspect.getsource`; comment notes v1.108.267 coverage-by-node-type trap. |
| tests/test_v1_108_271.py | `get_dead_code_v2` accepts `entry_point_patterns` at function, MCP schema, and dispatcher layers, so its degradation warning names a parameter that exists. | docstring: "#436"; git `9b7ce4c fix(dead-code): advice you cannot follow is worse than no advice` | 4 | LOAD-BEARING | Ratchet reads `server.py` text and AST via `SRC` path; guards "two implementations of what a pattern means". |
| tests/test_v1_108_272.py | `identity_hit` in the ranking ledger VARIES with the identity channel on both non-fusion `search_symbols` exits, while confidence is unchanged. | docstring: "#440"; git `9cfa6d2 release: v1.108.272` | 9 | LOAD-BEARING | Reads `ranking_events` via raw `sqlite3`; pairs with `ledger_trust.identity_label_is_trustworthy`. |
| tests/test_v1_108_273.py | Framework entry-point patterns match real file paths through `fnmatch` (no brace alternation); `next` JS variants exist and its exemption is retired. | docstring: "#445 ... v1.108.271 fixed #435"; git `ce2c667 release: v1.108.273` | 1 | LOAD-BEARING | Effect-based, explicitly because the #435 spelling sweep passed on a non-fix. |
| tests/test_v1_108_275.py | `unmatched_patterns` reports caller-supplied `entry_point_patterns` that match nothing, naming braces, `**`, and typos as causes, without refusing. | docstring: "#446"; git `186c519 release: v1.108.275` | 9 | LOAD-BEARING | `inspect.getsource(get_dead_code_v2)` scan; brace support deliberately NOT added (1.x contract). |
| tests/test_v1_108_276.py | Ranking-ledger rows record the TRUE result count (#441) and the writer reuses one connection per process instead of reopening per event (#442). | docstring: "#441 ... #442 (@rknighton)"; git `e9d5995 fix(telemetry): record the true result count (#441) and stop reopening the db per event (#442)`, `d1617b8 test: rename the state fixture` | 9 | LOAD-BEARING | `skipif` on one test; ratchet reads `token_tracker.__file__` text; fixture renamed after colliding with `_State` in golden replay (#458). |
| tests/test_v1_108_277.py | `find_dead_code` treats an inbound render edge (`render(..., "page.html")`) as reachability; an unrendered template is still dead. | docstring: "(jcm#461)"; git `3780cf5 fix(dead-code): treat a render edge as a reachability edge (#461)` | 1 | LOAD-BEARING | `test_unrendered_template_is_still_dead` guards against a blanket `.html` exemption. |
| tests/test_v1_108_281.py | Rust/Go/Java/PHP constant extraction handles `pub mod` nesting and N-names-per-declaration, and excludes `notStatic`, function-locals and other non-constants. | docstring: "(#428, second half)"; git `3df3ae5 fix(parser): extract Rust, Go, Java and PHP constants (#428)` | 10 | LOAD-BEARING | Complements `tests/test_constant_extraction_guard.py` (one-sample ratchet); fixture avoids developer config (#411). |
| tests/test_v1_108_55.py | Turn-budget idle reset fires in every reader; `token_budget` packing charges per-row payload not source bytes; `search_text` query caps are validated before pre-dispatch work. | docstring: "jcm#327, jcm#328, jcm#329"; git `c03f524 fix: turn-budget reader reset, payload-based budget packing, instant search_text rejection` | 2 | LOAD-BEARING | Three issues in one file (predates one-issue-one-verdict); 200/500-char cap literals. |
| tests/test_v1_108_56.py | No-change `index_folder` paths advance stored `git_head`; a cache hit without `_meta` synthesises one; `paths=[...]` scopes deletions to the listed subset. | docstring: "jcm#330, jcm#331, jcm#333 (@mmashwani)"; git `4920108`, touched by `6ce53a0 fix: preserve blast-radius cache metadata fallback` (#570) | 3 | LOAD-BEARING | `skipif(not _have_git())`; #331 half overlaps the newer `tests/test_result_cache_isolation.py` (#572) but tests the search_symbols cache, not the shared one. |
| tests/test_v1_108_57.py | The out-of-process `hook-precompact` reads the persisted live journal (`save_live_journal`/`snapshot_from_live`) and says "no live session journal" rather than emitting a zero-state snapshot. | docstring: "jcm#334 (@mmashwani)"; git `44ff924`, `9b375cd fix(hooks): ten confirmed defects in the Claude Code hook steering layer` | 6 | LOAD-BEARING | Spawns a subprocess for the separate-process half; `JCODEMUNCH_LIVE_JOURNAL=0` disable path. |
| tests/test_v1_108_58.py | `resolve_flow_edges` turns string-dispatched handlers into http gateways and attaches rendered templates as per-chain `views` in `get_signal_chains`. | docstring: "v1.108.58"; git `f0cdb6b v1.108.58: framework flow-edge resolver for get_signal_chains` | 1 | LOAD-BEARING | Feature-introduction tests, no issue number; end-to-end through `index_folder`. |
| tests/test_v1_108_59.py | `resolve_decision_context` mines revert/perf/refactor/rename/bugfix commits and `get_blast_radius`/`get_impact_preview` attach them only behind `include_decisions`. | docstring: "F-15 #2"; git `64027a4 v1.108.59: decision-context surfacing in impact analysis` | 1 | LOAD-BEARING | `pytestmark skipif` git absent; builds real git history with pinned user.name/email. |
| tests/test_v1_108_63.py | `check_edit_safe`/`check_delete_safe` consume singular-mode `check_references` evidence; multi-file `find_importers(cross_repo=True)` fails closed. | docstring: "issues #338, #339"; git `7d0e996 fix: composite preflights consume reference evidence; cross-repo batch fails closed` | 1 | LOAD-BEARING | Guards a `safe_to_delete` on a still-referenced symbol. |
| tests/test_v1_108_64.py | `_resolve_log_config` applies CLI flag > env var > config key > default for `log_level`/`log_file`. | docstring; git `02145ea release: v1.108.64 — logging honors the log_file / log_level config keys` | 6 | LOAD-BEARING | Config stubbed via dict-backed `config.get`. |
| tests/test_v1_108_70.py | `get_symbol_source` bounded mode: default is byte-identical full source; line/byte bounds author truncation metadata; batch total-bytes returns partial entries; `context_lines` cannot defeat the bound. | docstring: "(#340) ... issue's acceptance comment"; git `8cfaaf4 v1.108.70: bounded-source mode for get_symbol_source (#340)` | 2 | LOAD-BEARING | Byte-arithmetic unit cases plus integration via raw `sqlite3`. |
| tests/test_v1_108_71.py | The telemetry worker thread is started lazily on first share behind the `share_savings` opt-out, never at import, and the endpoint is env-overridable. | docstring: "PRD F-T01 / F-T02"; git `164bc93 v1.108.71: packaging de-risk - lazy telemetry worker`, `e3663c5` | 8 | LOAD-BEARING | Subprocess import probe enumerates threads by name `jcodemunch-telemetry`; this is the disclosed-background-behaviour compliance surface (PyPI quarantine). |
| tests/test_v1_108_72.py | A relative `path` to `resolve_repo` carries `relative_path_warning` + `_meta.relative_path`; absolute callers get byte-identical output. | docstring; git `8cba807 v1.108.72: resolve_repo flags relative-path inputs over detached transports` | 6 | LOAD-BEARING | Monkeypatches cwd to simulate server CWD. |
| tests/test_v1_108_73.py | HTTP ingest write endpoints return 503 when enabled without `JCODEMUNCH_HTTP_TOKEN`; redaction covers `github_pat_`, `sk-proj-`, `sk-`, `sk-ant-`. | docstring: "PRD WI-2.1 + WI-2.3"; git `e6a35fc v1.108.73`, `9661e0b test: ten skipped modules that hid 209 tests between them` | 8 | LOAD-BEARING | `find_spec` + `pytestmark skipif` (starlette optional) with the ⚠⚠ importorskip-collapses-to-1-skipped comment; N7 relevant. |
| tests/test_v1_108_74.py | `call_tool` failures return `CallToolResult(isError=True)` with the same JSON body; successes stay a plain content list. | docstring: "PRD WI-2.2 / F-P01 ... v1.108.30 in-band contract"; git `b999670`, `7762b73 test fix: force JSON in the success-shape assertion` | 7 | LOAD-BEARING | Async; probes an unknown tool name. |
| tests/test_v1_108_78.py | Python 3.12 `type` aliases emit `kind="type"`; ranked-context compact rows are populated and the encoder fails closed on mismatch; a crashed watch task is fatal in `reindex_state`; `*secret*` modules index while credential files are refused. | docstring: "#351/#352/#353/#354 (@mmashwani)"; git `32366d1`, `d0223c4 fix: get_watch_status reported watcher bookkeeping as index freshness (#565)` | 1 | LOAD-BEARING | Four issues in one file (pre one-issue-one-verdict); #354 fail-closed guard is the ancestor of the #553/#555 encoder work. |
| tests/test_v1_108_80.py | dataclass/attrs/Pydantic annotated class-body assignments surface as `field` child symbols. | docstring: "(#355)"; git `c9e5e00 release: v1.108.80` | 1 | LOAD-BEARING | Pure parser unit tests. |
| tests/test_v1_108_81.py | `get_watch_status` skips the per-repo git identity probe when `has_any_reindex_state()` is False, and reports `unknown` never `fresh` when it cannot measure. | docstring; git `eadd8b3 v1.108.81`, `d0223c4 ... (#565)` | 5 | LOAD-BEARING | Only 2 tests; one rewritten at #565 (comment says it used to assert `any_stale is True`). |
| tests/test_v1_108_83.py | Poll-delay resolution (`JCODEMUNCH_WATCH_POLL_DELAY_MS` > `WATCHFILES_POLL_DELAY_MS` > 1000 floor, garbage -> default) and WSL detection helpers. | docstring: "(issue #356)"; git `37ecc09 Tame watch-all CPU under WSL (#356)` | 5 | LOAD-BEARING | Awatch wiring itself deferred to the watcher suite; patches `builtins` for /proc probe. |
| tests/test_v1_108_84.py | `detect_install_mechanism` recognises pipx/uv/pip venvs and `upgrade` prints the matching command while refreshing hooks in-process. | docstring: "(#357)"; git `2a4d3d9 release: v1.108.84` | 6 | LOAD-BEARING | Literal `/Users/g-besoin/.local/pipx/venvs/...` path as a fixture input (reporter's path, mocked, not a real dependency). |
| tests/test_v1_108_85.py | `watch_extra_install_command()` matches the install mechanism; the migration schema warning fires at most once per (owner, name). | docstring: "(#357) ... @zakblacki"; git `b40142e v1.108.85` | 6 | LOAD-BEARING | Mock-based. |
| tests/test_v1_108_86.py | `watch-all` prints a one-time steady-state guidance line after its initial index. | docstring: "(#357 follow-up) ... @zakblacki"; git `5d2bdfc v1.108.86` | 6 | LOAD-BEARING | 3 tests with a `_FakeManager` stand-in. |
| tests/test_v1_108_92.py | `ProgressReporter` throttles (min 1% step + 100 ms gap, final send exempt) and `drain_reporter` flushes in-flight sends before the response. | docstring: "(#359)"; git `08e0129 release: v1.108.92` | 6 | LOAD-BEARING | Threads + asyncio; guards the Claude Code stdio-drop under a 2,000-notification flood. |
| tests/test_v1_108_95.py | `assemble_task_context`'s runtime stage calls `find_hot_paths` correctly and reads `results`/`runtime_count`; `_read_body` streams with an incremental cap and bounded gzip; Nuxt auto-import edges carry names. | docstring: "externally reported findings (email, verified)"; git `360fd2f release: v1.108.95`, `9661e0b` | 8 | LOAD-BEARING | `find_spec`-gated `skipif` (starlette); gzip-bomb fixture built with `zlib`; N7 relevant. |
| tests/test_v1_108_99.py | The fusion embedding channel is actually wired (not swallowed by bare except) and `_meta.confidence` is graded from row scores rather than a ~0.584 constant. | docstring: "audit WS-4 ... V5 + V6"; git `c5e7814 release: v1.108.99` | 9 | LOAD-BEARING | Only 4 tests; uses `inspect` on module source. |
| tests/test_v1_80_10_dead_code_intra_file_calls.py | `get_dead_code_v2` includes the symbol's own file in the no-callers search (AST and text fallback). | docstring: sverklo/sverklo#25 (@nike-17); git `aefda03 release: v1.80.10` | 1 | LOAD-BEARING | 2 tests; external benchmark provenance. |
| tests/test_v1_80_7_dead_code_js_reexports.py | `package.json` main/module/exports/bin are entry points and barrel scanning follows CJS/ESM re-exports, so Express's `createApplication` is not dead. | docstring: sverklo/sverklo#25; git `e2d4e31 release: v1.80.7`, `8824d53` (fixtures need a LIVE symbol post-.231) | 1 | LOAD-BEARING | Fixtures patched at v1.108.231 to avoid degenerate-signal suppression. |
| tests/test_v1_80_9_lodash_class.py | `package.json` entry files are force-included past the size cap, and a repo with empty `index.imports` falls through to call-graph-only dead-code mode. | docstring: "sverklo bench round-2"; git `7f87cbb release: v1.80.9`, `84a270f release: v1.108.195 — the size-cap exemption compared raw Windows paths` | 1 | LOAD-BEARING | `normcase` normalisation note (v1.108.195): Windows path comparison regression. |
| tests/test_verdict_coverage_calibration.py | The index persists a coverage block that absent/degraded verdicts carry, and the planted-query calibration artifact matches a live re-run of the measurement. | docstring: "(v1.108.145)"; git `047fdfc release: v1.108.145 — coverage contract on absence claims + scorer-pinned calibration` | 3 | LOAD-BEARING | Reads `benchmarks/calibration/planted_queries.json` and re-measures live (artifact drift guard; N4 too); slug-unique planted names. |
| tests/test_version_check.py | First launch writes `last_seen_version` silently; a later launch with a newer version prints a release-tag hint to stderr; `JCODEMUNCH_NO_VERSION_HINT` disables. | docstring: "(v1.84.0)"; git `4f35cac release: v1.84.0 — install badges + auto-recency + version-drift probe` | 6 | LOAD-BEARING | Redirects `CODE_INDEX_PATH`; version literal `1.84.0` is a fixture, not a pin. |
| tests/test_watch_all.py | `discover_local_repos` filters remote and vanished entries, and the service installer generates correct per-platform paths. | git `c9c8e49 feat: watch-all subcommand + login-service installer + get_watch_status MCP tool`, `d0223c4 (#565)` | 6 | LOAD-BEARING | Uses `platform` patching; no docstring provenance. |
| tests/test_watch_claude.py | `handle_hook_event` / manifest read-write / `parse_git_worktrees` / worktree removal behave end to end for the watch-claude v2 flow. | git `32fa07d fix(hooks): WorktreeRemove with worktree_path now actually removes the worktree`, `57b1461 fix(hooks): resolve main repo root before git worktree remove`, `4d80271 test: deflake repos-mode worktree-cleanup test` | 6 | LOAD-BEARING | 35 tests; spawns real `git worktree` subprocesses; has a deflake history. |
| tests/test_watch_once.py | `sync_folders` indexes the folder and returns without blocking. | git `f3be2e4 release: v1.24.3 — watch --once for one-shot index sync (#227)` | 3 | LOAD-BEARING | 4 tests; `asyncio.run`. |
| tests/test_watcher_dynamic.py | `WatcherManager` adds/removes folders dynamically; auto-watch-on-demand is deferred to after dispatch (#384) and the eager path indexes exactly once (v1.108.191). | comments: "#384", "v1.108.189", "v1.108.191"; git `9e1a11b`, `66c7371`, `9661e0b test: ten skipped modules` | 3 | LOAD-BEARING | `find_spec`-gated `pytestmark skipif` (watchfiles) with ⚠⚠ importorskip comment; N7 relevant. |
| tests/test_watcher_hash_delta.py | `_watch_single` applies the hashes `index_folder` stored rather than a full reload or a second file read. | docstring: "#557, @Ticki84 ... #370"; git `edc59ad fix(watcher): apply the hashes index_folder stored, not a full reload` | 3 | LOAD-BEARING | Ratchet: `inspect.getsource(_watch_single)` banning `_file_hash(`/`open(`/`.read_text(`; reads `index_folder` source text. |
| tests/test_watcher_knob_parity.py | Every `index_folder` knob the watcher names reaches `index_folder` at every layer (CLI, `watch_folders`, `WatcherManager`, `_watch_single`, `_initial_index`). | docstring: "(#558, surfaced by @Ticki84 in #557)"; git `6ecc043 feat(watcher): --no-context-providers on watch, watch-all, watch-claude` | 3 | LOAD-BEARING | Property over signatures via `ast`/`inspect`, not a roster; reads `server.py` text. |
| tests/test_watcher_lock.py | Lock-file creation/reuse/stale-PID handling and idle timeout for the watcher; `_is_pid_alive` wrapper removed (#450). | comments: "jcm#450", "v1.106.0 schema field renamed folder -> target"; git `617dfcb fix: close watcher standby audit gaps`, `f94799e fix(storage): keep Linux create_time boot-relative`, `9661e0b` | 3 | LOAD-BEARING | 40 tests; `find_spec` skipif (watchfiles); spawns subprocesses for PID liveness. |
| tests/test_watcher_memory_cache.py | `WatcherChange` shape and the watcher fast path honour `extra_ignore_patterns` and every discovery filter via the shared helper. | docstring/comments: "#300 follow-up (@domis86)", "#306"; git `5dc9cbd v1.108.19`, `0666cd6 v1.108.20`, `c1c891b v1.108.193` | 3 | LOAD-BEARING | Uses `tempfile` directly rather than `tmp_path` in places. |
| tests/test_watcher_serve.py | The `--watcher` flag on `serve` starts the embedded watcher without blocking the stdio handshake (#281), indexes once then adopts (v1.108.191), and handles signals/protected log paths. | comments: "issue #281", "v1.108.191"; git `617dfcb`, `66c7371`, `9661e0b` | 6 | LOAD-BEARING | 49 tests; `find_spec` skipif (watchfiles); literal `C:\Windows\System32\protected_test.log` used as a protected-path fixture (mocked; cf. Standing lesson 08-20 #447). |
| tests/test_weight_tuning.py | `tune_weights` consumes the ranking ledger, applies/rolls back weight proposals, and honours the `max_age_days` recency window. | docstring: "v1.79.0"; git `851977a release: v1.79.0 — online weight tuning`, `e0f5210 release: v1.108.53 — tune_weights recency window` | 9 | LOAD-BEARING | Autouse fixture replaces `token_tracker._state`; raw `sqlite3`. |
| tests/test_whatsnew.py | `parse_changelog` / `render_readme_block` / `update_readme` / `write_whatsnew_json` produce the README block and `whatsnew.json` from a sample CHANGELOG. | docstring: "(v1.84.0)"; git `4f35cac release: v1.84.0` | 7 | LOAD-BEARING | `whatsnew.json` is one of the release pin sites; inline sample changelog. |
| tests/test_winnow_symbols.py | `winnow_symbols` applies multi-axis constraint chains (kind, complexity, file pattern, etc.) over an indexed repo. | git `96e484c Release 1.53.0 — winnow_symbols constraint-chain retrieval` | 1 | LOAD-BEARING | Feature tests, no issue number; end-to-end via `index_folder`. |
| tests/test_yaml_byte_extent.py | A virtual YAML symbol's `byte_offset`/`byte_length` slice is a real, whole, self-consistent source line, not a reconstructed signature length. | docstring: "Measured 2026-08-04 ... 237 of 237 virtual symbols"; git `46e8e98 release: v1.108.237` | 1 | LOAD-BEARING | Deliberately does NOT assert `line` (that is `test_yaml_line_location.py`'s job); byte-level pin. |
| tests/test_yaml_key_coercion.py | YAML mapping keys `on`/`off`/`yes`/`no` keep their source text instead of PyYAML 1.1 boolean coercion; values keep YAML semantics. | docstring; git `3c54843 release: v1.108.239 - a YAML key keeps its own name` | 1 | LOAD-BEARING | Guards silent key LOSS (four keys collapsing to two). |
| tests/test_yaml_language.py | Generic YAML and Ansible playbooks parse to expected symbols and `.yml`/`.yaml` map to the YAML language. | git `527371e Add YAML and Ansible parser support` | 10 | LOAD-BEARING | Original feature test from 2026-03; no issue number. |
| tests/test_yaml_line_location.py | A YAML symbol is located at its own key's line via node marks / anchored search (no substring match, no forward-cursor skip, no fabricated `after + 1`), or refuses. | docstring: "measured 2026-08-04 against health-radar-comment.yml ... 0 of 4773"; git `664d491 release: v1.108.238`, `3c54843 v1.108.239` | 1 | LOAD-BEARING | Includes a reverse-direction guard for the former refusal case. |

## 2. Benchmark methodology rules, scripts, fixtures, tightened numbers, removed tests

The numbered rules R1-R62 below are CONSTRAINTS on the new harness (Phase 3), not suggestions; assertions that enforce them will cite the rule number.

Read-only archaeology of C:\MCPs\jcodemunch-mcp, branch `harness/source-of-truth`, 2026-09-03.
No tracked file modified; pytest not run. Sources: benchmarks/*.md, benchmarks/*/README.md,
module docstrings (parsed with `ast`), `git log`, CHANGELOG.md, ISSUE-HISTORY.md, docs/standard/STANDARD.md.

---

## A. Methodology rules (R1..R62)

Each rule quotes its source. "file:line" is the current tree.

### Corpus selection and pinning
- **R1** Corpus is pinned by upstream COMMIT, not branch. `benchmarks/METHODOLOGY.md:14-15` — "All repositories are public and pinned to a specific upstream **commit**, not to a branch. No filtering or cherry-picking of files was applied beyond jcodemunch's standard skip patterns".
- **R2** Pins live in `tasks.json` and are re-checked every run; a mismatch refuses publication. `METHODOLOGY.md:24-27` — "the harness refuses to overwrite the published artifact when a measured index reports a different `git_head`, or when its corpus completeness is anything other than `True`."
- **R3** `index_repo` is banned for the benchmark because it cannot pin. `METHODOLOGY.md:156-158` — "`index_repo` is deliberately NOT used here: it has no ref parameter and always fetches whatever the default branch points at today, so it cannot reproduce a pinned corpus."
- **R4** A different commit is a different corpus. `REPRODUCING.md:16-18` — "A different commit is a different corpus and will not reproduce a published number."
- **R5** Do not lower the file cap. `REPRODUCING.md:52-56` — "**Do not lower the file cap.** ... A lower cap truncates silently — the harness will refuse to publish the result ... but only because that refusal was added in v1.108.222."
- **R6** Four refusal conditions for `--reference`. `REPRODUCING.md:101-107` — refuses when any repo "has no SHA pinned in `tasks.json`, carries no `git_head` ..., reports a `git_head` that differs from its pin, or has a corpus completeness of anything other than `True` — including `"unknown"`".
- **R7** `--allow-unpinned` stamps the artifact provisional. `REPRODUCING.md:109-110` — "stamps the artifact `"provisional": true` with the objections recorded inside it."
- **R8** A file count is a property of the installation, not the repo. `REPRODUCING.md:93-97` — "size limits, not on the repo alone ... different corpus and the numbers are not comparable."
- **R9** Repo identity must be `owner/repo`. `REPRODUCING.md:48-50` — "If a repo lands as `local/<dir>-<hash>` ... delete the local-keyed index, and re-index."
- **R10** Goldset corpus is snapshotted, never indexed as a subtree of the host. `benchmarks/goldset/measure.py` docstring — "never index a subtree of the host repo — the git-identity resolver would fold it into the host index".
- **R11** Route-binary pilot cases bind to `tasks.json` SHAs. `benchmarks/route_binary_pilot/build_cases.py` docstring — "Each case is bound to a real repository at the SHA published in `benchmarks/tasks.json`".
- **R12** Racket fidelity corpus is pinned by Racket VERSION. `benchmarks/racket_fidelity/README.md:98` — "pinned by the **Racket version** rather than a git SHA".
- **R13** Anchor P0 uses real sessions only. `benchmarks/anchor/measure.py` docstring — "there is no synthetic corpus here on purpose."

### Tokenizer / what counts as a token
- **R14** `tiktoken cl100k_base`. `METHODOLOGY.md:101-102`; `benchmarks/README.md:13` — "Consistent across runs regardless of model."
- **R15** Count the serialized JSON response, not raw source. `METHODOLOGY.md:104-106` — "JSON field names and structure overhead are included (slightly understates savings)."
- **R16** System prompt / tool descriptions excluded from the workflow count. `benchmarks/README.md:85` — "does not count system prompt or tool description tokens, which are identical for both approaches."
- **R17** `_meta.timing_ms` and `_meta.total_tokens_saved` are KEPT in the published count. `METHODOLOGY.md:127-129` — "**We keep counting both**, because an agent really does pay for them and excluding them would move every published ratio in our own favour."
- **R18** Single tokenizer is a disclosed limitation. `METHODOLOGY.md:214-216`.
- **R19** Runtime meter uses bytes/4 and is conservative by design. `TOKEN_SAVINGS.md:138-140` — "Every known error term points down."
- **R20** Savings stored as tokens, never currency. `TOKEN_SAVINGS.md:141`.
- **R21** Schema-cost tokenizer is a proxy; ratios transfer, absolutes don't. `harness/capture_schema_baseline.py` docstring — "cl100k_base is a PROXY ... The absolute counts here are approximate; the ratios between arms are not."

### Baseline / counterfactual construction
- **R22** Two baselines, same run, same corpus, same reader; neither a stored constant. `METHODOLOGY.md:56-57`.
- **R23** Baseline A = read-all, "a ceiling nobody pays". `METHODOLOGY.md:59-62`.
- **R24** Baseline B = grep-top-3: `rg -l`, rank by match count, open top 3 whole. `METHODOLOGY.md:64-67`.
- **R25** Every Baseline-B modelling choice favours the baseline. `METHODOLOGY.md:69-84` — paths-only grep cost ("would ... report **~49x instead of ~28x**. The smaller number is the published one."); ranked by match count; case-insensitive substring on ANY term.
- **R26** Baseline B reads files WHOLE; no line-range estimator offered. `METHODOLOGY.md:86-90`.
- **R27** jcm workflow = `search_symbols(max_results=5)` + `get_symbol_source` on top 3. `METHODOLOGY.md:93-95`.
- **R28** AI summaries OFF during benchmarking. `METHODOLOGY.md:97`; `REPRODUCING.md:45-46`.
- **R29** Lead with Baseline B. `METHODOLOGY.md:304-305` — "measuring against read-all overstates the advantage by roughly 8.5x.** Quote the Baseline B figure."
- **R30** Quote the range, not the aggregate alone. `TOKEN_SAVINGS.md:11-12`; `METHODOLOGY.md:307-309` publishes the floor (`middleware` on gin, 7.3x).
- **R31** Read-all aggregate is dominated by fastapi (72.9%); say so. `METHODOLOGY.md:269-270`, `:311-317`.
- **R32** "99.6% is a floor" applies only within the read-everything class. `METHODOLOGY.md:340-345`.
- **R33** Comparator cells where the rival wins are published. `METHODOLOGY.md:333-338` — "published rather than dropped."
- **R34** RAG baseline shares file identity with the main harness by construction. `RAG_COMPARISON_NOTES.md:115-117`.
- **R35** RAG top-3 double-count left in because it cuts against us. `RAG_COMPARISON_NOTES.md:119-124`.
- **R36** Chunk integrity is a heuristic; `chunk_split` is more reliable. `RAG_COMPARISON_NOTES.md:126-130`.
- **R37** Codex arms: isolated HOME so `AGENTS.md` cannot leak; `--sandbox read-only`. `codex_surface/README.md:148-149`, `:198-202`.
- **R38** Dead-code eval measures precision ONLY. `deadcode_eval/README.md:34-38` — "Reporting a 'recall' from this data would be inventing the other half of a confusion matrix".
- **R39** `--compare` cutoffs evaluated in the same run against the same oracle. `deadcode_eval/README.md:60-62`.
- **R40** Description-smell scores NOT interchangeable with the paper's; report both frames. `description_smells/README.md:28-44`.
- **R41** Baselines get k-matched guesses. `route_binary_pilot/run_pilot.py` docstring — "which is the error `run_emitted_task.py` made and this suite is not repeating."

### Determinism
- **R42** Retrieval is deterministic; no seed to pin. `METHODOLOGY.md:107`; `REPRODUCING.md:71`.
- **R43** ±1-token jitter; `--verify-determinism` compares `stable_tokens`. `METHODOLOGY.md:109-135` — "reproduce a published figure to within ±1 token per query, not exactly."
- **R44** Goldset artifact must equal a live re-run and the corpus content hash. `goldset/measure.py` docstring.
- **R45** Cache-stability requires ONE index generation per sequence. `cache_stability/measure.py` docstring. (STANDARD N4: corpus undecided.)

### Never hand-typed
- **R46** No harness carries its own jCodeMunch numbers; `JCODEMUNCH_*` constants fail CI. `METHODOLOGY.md:187-193`.
- **R47** Unmeasured repo prints "not measured"; no estimator. `METHODOLOGY.md:194-198`.
- **R48** Never hand-type a tier-switch figure. `tier_switch/README.md:6`.
- **R49** SWE-bench protocol table printed by `power.py`. `swebench/power.py` docstring.
- **R50** Route-recall schema-saving % computed at runtime from `schema_baseline.json`. `run_route_recall.py` docstring.
- **R51** Do not author a token baseline by hand. `token_baselines/README.md:37-41`.
- **R52** Codex arm numbers must not be quoted. `codex_surface/README.md:38`.

### Re-sync obligations
- **R53** Five artifacts mirror one run; `--reference` rewrites provenance itself (CLAUDE.md Practice 4; `tests/test_provenance.py`): `results.md`, `METHODOLOGY.md`, README, `provenance/measured.json`, `REPRODUCING.md`.
- **R54** `benchmark.yml` only WARNS on drift (`.github/workflows/benchmark.yml:131`). STANDARD §2 Gap: must FAIL on an upward jcm move. Not implemented.
- **R55** Replay golden rewritten only deliberately via `--write-result`. `benchmarks/README.md:103-106`.

### Pre-registration
- **R56** `route_binary_pilot/predicate.py` "must not be edited after the generator lands" (pinned by `tests/test_route_binary_pilot_is_frozen.py`).
- **R57** `measure_route_discriminators.py` — "THE PREDICATE IS DECLARED BEFORE ANY LABEL IS READ."
- **R58** Anchor P0 decision thresholds pre-registered (<10% / 10-25% / >25%).

### Fidelity gate bars
- **R59** Rust: `extra`, `wrong_span`, `undercount`, `qual_mismatch` must be 0; owner is `self_ty`. `rust_fidelity/README.md:14-17`, `:81-82`.
- **R60** Rust ceiling: `syn` parses, does not expand. `rust_fidelity/README.md:100`.
- **R61** Racket: `extra`/`wrong_span` at 0; oracle errors listed, never dropped ("a shrinking denominator flatters"). `racket_fidelity/README.md:19-22`, `:79`. Reader: `only_racket`, `only_ours`, `our_only_error` at 0. `:117-119`.
- **R62** Rust `--write` refuses to publish with nothing pinned. `rust_fidelity/README.md:144`.

⚠ **Stale rule found:** `RAG_COMPARISON_NOTES.md:132-135` still says "update `JCODEMUNCH_GRAND` in `run_rag_baseline.py`". That constant was removed 2026-07-29 (cc397e3) and `tests/test_benchmark_reference.py` fails if it returns. Contradicts R46.

---

## B. Benchmark script and fixture archaeology

Key: LOAD-BEARING = a committed test or CI job reads it; STRUCTURAL = documented instrument, no gate; REDUNDANT; STALE = premise gone; UNCLEAR.

| path | measures / holds | why it exists (first commit) | STANDARD | class |
|---|---|---|---|---|
| harness/run_benchmark.py | tiktoken counts, jcm vs read-all + grep-top-3; `--reference`, `--verify-determinism` | e67b3d1 2026-03-13 "feat: add tokenizer-true benchmark harness" | 2, N4 | LOAD-BEARING (benchmark.yml; test_benchmark_reference.py; test_provenance.py) |
| harness/run_rag_baseline.py | LangChain RAG 512/1024/2048 vs jcm | 49cdc6a 2026-03-29 v1.12.7 | 2 | STRUCTURAL (constants guarded) |
| harness/run_odysseus_compare.py | Odysseus rag_server vs jcm retrieval tokens | a7c76d0 2026-07-18 v1.108.142 | 2 | STRUCTURAL |
| harness/capture_schema_baseline.py | tools/list tokens per surface x profile x compact | 76cd23e 2026-04-19 v1.70.0 | 4 | LOAD-BEARING (test_schema_budget.py, test_schema_baseline_transcription.py) |
| harness/capture_token_baseline.py | per-release tokens_saved snapshot | a3ac917 2026-04-25 v1.75.0 | 5, 9 | STRUCTURAL (one baseline, v1.108.163) |
| harness/ab_v1_70_0.py | v1.70.0 detail_level A/B | 76cd23e 2026-04-19 | 2 | STALE (one-release A/B) |
| replay/run_replay.py, metrics.py, __init__.py | nDCG/MRR/Recall@k; `--gate 0.02` | 69a00c6 2026-04-25 v1.76.0 | 1(b), 7 | LOAD-BEARING (replay.yml) |
| replay/fixtures/self_v1_75_0.json; results/*-golden.json | query→expected ids; golden baseline | 69a00c6 | 1(b) | LOAD-BEARING; ⚠ self-indexing goes red on test-adding PRs (CHANGELOG 1.108.278, PR #457) |
| goldset/measure.py + corpus/ (16 .py) + gold.json | find_implementations per-channel precision/recall | c2e801a 2026-07-18 v1.108.140 | 1(c) | LOAD-BEARING (test_channel_accuracy.py) |
| route_recall/run_route_recall.py | menu()/route() recall over live catalog | c78392c 2026-08-02 v1.108.212 | 4 | LOAD-BEARING (test_route_recall_artifacts_are_fresh.py, test_catalog_moratorium.py) |
| route_recall/explain_misses.py | first gate that excluded an action | f690af8 2026-08-02 v1.108.217 | 4 | LOAD-BEARING |
| route_recall/run_emitted_task.py | route on agent-emitted task strings (#398/#422) | 14a1eaa 2026-08-07 | 4 | LOAD-BEARING (emitted_task_results.json pinned) |
| route_recall/measure_route_discriminators.py | H1/H2 refuted on coverage | 1ad16c1 2026-08-20 | 4 | STRUCTURAL (negative result) |
| route_binary_pilot/predicate.py, build_cases.py, run_pilot.py | pre-registered H3; 60 cases; refuted under ablation | 00cbe9f / a5dff27 / 31720d7 2026-08-21 | 4 | LOAD-BEARING (test_route_binary_pilot_is_frozen.py) |
| rust_fidelity/run_fidelity.py + oracle/ | extraction vs `syn`; 4 buckets at 0 | c13b623 2026-08-27 | 1(a) | LOAD-BEARING (test_rust_fidelity*.py) |
| racket_fidelity/run_fidelity.py, run_reader_fidelity.py | vs Racket expander / reader | 01bd357 2026-08-25; 834d4db 2026-08-29 | 1(a) | LOAD-BEARING (test_racket_*.py) |
| tier_switch/price_tier_switch.py | break-even requests for a tier switch; weights live | de1d063 2026-08-30 v1.108.311 | 4 | LOAD-BEARING (test_tier_switch_cost.py) |
| codex_surface/run_codex_arms.py | 4-arm Codex net-token effect | 1a72f27 2026-08-10 | 4 | STRUCTURAL; NEGATIVE (R52); 86%-cached finding is the load-bearing prose |
| cache_stability/measure.py | reshuffled-repeat share of get_ranked_context | 41d7c7c 2026-07-20 v1.108.149 | N4 | UNCLEAR — no README; STANDARD N4 gap "decide cache_stability's corpus" |
| description_smells/score_descriptions.py, rubric_upstream.py | arXiv:2602.14878 rubric, two frames | 68fecb6 2026-08-16 v1.108.282 | 4 | LOAD-BEARING (test_description_smells.py) |
| deadcode_eval/run_eval.py | definite false positives vs coverage oracle | c452376 2026-08-03 (#408 follow-up) | 1, 9 | LOAD-BEARING (test_deadcode_eval_harness.py) |
| anchor/measure.py | session_yield redelivery; pre-registered thresholds | f933fe2 2026-07-30 v1.108.201 | 9 | STRUCTURAL (needs telemetry rows) |
| offload/run_offload_criterion.py | offloadable-work label rate, first-gate vs all-gates | d461674 2026-08-04 v1.108.241 | 9 | STRUCTURAL |
| swebench/power.py | power simulation; `contested` is an assumption | 3e9b8a5 2026-08-23 | 2 | STRUCTURAL (protocol only) |
| profile_backpressure.py | watcher backpressure profiling (72 checks) | 720049b 2026-03-23 | 3 | STALE — exercises `wait_for_fresh`/staleness `_meta`, retired 0d90e76 2026-03-28 |
| profile_config_regression.py | cold/warm all-tool timings vs FlatCAM_EVO | 2a6880c 2026-03-24 | 5 | STALE (local-only corpus, no artifact) |
| profile_language_filter.py | languages=["python"] vs none | 2a6880c 2026-03-24 | 5 | STALE (same) |
| tasks.json | 5 queries x 3 repos + SHAs | 26c1ccc 2026-03-13 (SHAs v1.108.222) | 2 | LOAD-BEARING |
| jcm_reference.json | measured jcm side, one run | cc397e3 2026-07-29 | 2 | LOAD-BEARING |
| schema_baseline.json | frozen schema token table | 76cd23e 2026-04-19 | 4 | LOAD-BEARING |
| provenance/ (measured.json, channel_accuracy.json) | basis of every published number | 981a74f 2026-07-18 v1.108.138 | 9 | LOAD-BEARING (test_provenance.py, test_v1_108_139.py) |
| calibration/planted_queries.json | scorer-pinned coverage calibration | 047fdfc 2026-07-19 v1.108.145 | 9 | LOAD-BEARING (test_verdict_coverage_calibration.py) |
| token_baselines/v1.108.163.json | only token baseline; token-only shape | a3ac917 | 5 | STRUCTURAL |
| whitepaper.md, rag_baseline_results.md, odysseus_compare_results.md, results_v1.70.0.md, ab-test-*.md | frozen result prose | 49cdc6a 2026-03-29 et al. | 2 | REDUNDANT with results.md/METHODOLOGY (whitepaper); ab-test-dead-code-2026-03-18.md cited by test_investigator_deletion_safety.py |
| `tests/test_inbound_scan.py` | The pre-model scan reads hidden text (HTML comments, zero-width, full-width, entities) and finds the POLICY rule-1 and 4.3 patterns; this repo's own vocabulary (env vars, Claude Code hooks, github.com URLs) trips nothing. | inbound plumbing PR (2026-09-04); docstring: red arms per phrasing, green arms per false positive found in review | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_prompts.py` | Every headless prompt carries the policy's preamble and never-touch list by generation, a version, a model and a recorded sha; a policy edit re-renders without a bump, a body edit needs one. | inbound plumbing PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_plumbing.py` | Kill switch fails closed on anything but the exact string true; budgets decline before a run; the audit record carries every POLICY 6.1 field and no security excerpt; the sdist excludes .github/. | inbound plumbing PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_workflows.py` | Every inbound-*.yml: no pull_request_target, checkouts of main with no persisted credentials (the one exception: the sweep's checkout of inbound-ledger into a subdirectory with the App token), write scope only on actorless triggers, the kill switch before every write, the model job with no write scope, pinned action, event text never interpolated into run:. | inbound item 2 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_triage_apply.py` | The triage plan cannot exceed POLICY section 2 for its category and confidence: medium/low never draft or comment, security is label + needs-human, duplicate is the one comment, the owner's issues get labels only, a malformed result escalates and never reaches gh. | inbound item 2 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_competitive_tier.py` | The competitive tier's interface, null adapters, one-to-one F1 with line tolerance, the band rule (stability before the band; the non-vacuity pair), the task check, and an end-to-end result file with the null rows and no local path the self corpus is `src/` without its bytecode (CF-39: a plain copytree carried 816 `__pycache__` files into the git repo every tool indexed, 2026-09-05; ignored by name and suffix, asserted over `git ls-files`). | competitive PR 1 (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_checks.py` | The competitive tier's fairness checks (DESIGN s3.3, s4.3, s10): corpus_check's five criteria each failing ALONE over synthetic descriptions with every threshold read from `corpus_policy.json`; the policy file complete and a missing key refusing; corpora.json pinned by full SHA with a domain; every task file's corpus a set member or self@HEAD, with the P1/P2/P4 shapes; the task generators' definition-line verification refusing a moved line and their grep rules; task_check's symmetric split, the check's three refusals, and tools_not_called naming only the silent tool. | competitive PR 3a (2026-09-06); docstring | 1 | LOAD-BEARING | competitive layer; the checkouts are not needed (nothing here fetches) |
| `tests/test_competitive_trend.py` | The competitive tier's trend tracking and summary sections (DESIGN s5.3, s6, s10): a history line carries medians, bands and gaps with the self corpus key normalised (its id carries the commit; without it every self row is a `first run` forever); the four movement classes and the two refusals (`no band recorded`, `first run`); `our improvement`/`our regression` only when jcm's value moved past the band while the competitor's release did not; the release on each of the three runs beside the row; the summary's Movement and tools-not-called sections and the `(variant of jcodemunch)` label. | competitive PR 3b (2026-09-06); docstring | 1 | LOAD-BEARING | competitive layer; synthetic history lines, no checkout, no daemon |
| `tests/test_competitive_findings.py` | The competitive tier's findings-to-issue drafts (DESIGN s7, s8): a `competitive-gap` draft only for a meaningful row where jcm is behind (not ahead, not unstable, not our row, not a CLI's zero schema cost) with every s7.1 field and a hypothesis from the fixed list by rule (`tool_not_called` first); `competitive-watch` only when ahead and `narrowed` twice by trend.py; `standard-proposal` only on two runs under the Target read verbatim from STANDARD.md, self corpus, never a Floor, human-edits-only first line; de-duplication by fingerprint (open = update in place, closed = the gap came back, unreadable tracker = refuse); the draft file shape; the module's only `gh` verb is `issue list`; a pin declared a variant of jcodemunch is our own row and is never a gap, a watch or a proposal (CF-54). | competitive PR 3c (2026-09-06); docstring | 1 | LOAD-BEARING | competitive layer; synthetic result files; the tracker read is monkeypatched |
| `tests/test_competitive_workflows.py` | The competitive loop's scheduled jobs (DESIGN s7.3, s9): the three workflows are actorless and read-only on GITHUB_TOKEN; every write follows a kill-switch read with the App token in its job; the competitor job holds no App token and writes nothing; no write touches the never-touch list and every push goes to inbound-ledger; timeouts match budget.py's rows; the post job reads both switches twice; feed.py's two release rules (title-only capability match quoted as data under the preamble, axis match schedules a re-run, unreadable = unknown), the registry-derived source repo; post.py's eligibility, write-back and both-switches refusal. | competitive PR 3d (2026-09-06); docstring | 1 | LOAD-BEARING | competitive layer; the inbound workflow tests are the template; fixtures, no network |
| `tests/test_competitive_compare_ref.py` | `/competitive-compare`'s table script (workflows DESIGN s2.7; competitive DESIGN s9.1): every row of either result file on one page, jcm rows first with the SIGNED current-minus-ref difference, every other row's movement from `trend.classify` over the two gaps against the current band; `n/a` for a side that did not produce the value, never 0; `no band recorded` rather than an invented band; the self corpus matched across the two commits; no total or mean in any table row (F-13); a file of another schema or an out-dir holding two result files refused; a count printed in full, never in exponent; each side's scorer sha256 and interpreter on the page with a mismatch said before the first number; the `--findings-row` figures equal to the page's counts; the command names the script and carries no table row of its own. | competitive PR 3e (2026-09-06); docstring | 1 | LOAD-BEARING | competitive layer; synthetic result files; a subprocess run of the script |
| `tests/test_competitive_mcp_driver.py` | The competitive MCP stdio driver answers a server that floods stderr before replying (a stub writes 4 MB per call; the pre-change driver read stderr only at exit, the pipe filled inside the tool and every call read as a hang, FINDINGS CF-27), keeps a bounded stderr tail, and records tools/list and every call through `main`. | competitive PR 2d (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_codegraph.py` | The CodeGraph adapter parses the tool's real text shapes (fixture from v1.6.0 in the sandbox: a search hit's `path:line`, a node's `**Location:**`, a caller row, a file's `used by N files` list with its `+K more` marker dropped, not invented), its call plan follows the fairness note with every call charged and one session per T task (explore elides lines sent earlier in a session), the index report is `init`'s wall and `status`'s count, the captured tools/list is the README's example allowlist and the default surface is one tool, and the pinned bundle digest is the one the Dockerfile verifies. | competitive PR 2e (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_graft.py` | The Graft adapter (deterministic path) parses the tool's real text shapes (fixture from 0.16.0 in the sandbox over the PINNED self corpus: a ranked node's `path:Lstart-Lend` span, a trace hit, a find_all symbol header and its module-level shape, the "no indexed callers" answer), a P2 answer cites references only and never the queried symbol's header, a P4 find_all answer cites each file once, the P4 follow-up exists only when the tool's own answer names it, the index report is the build's wall and its own `parsed: N of M` line, the captured tools/list is the six documented tools, the lockfile pins the package by the full sha512 integrity the adapter names, and that integrity IS the pin's digest (review round 1: a tarball sha256 nothing verified was published as the pin); a follow-up session that times out or errors marks the P4 answer with an error, never a quieter answer (review round 1, finding 4). | competitive PR 2f (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_aider.py` | The Aider RepoMap adapter, token axis only: the payload is the map after the tool's own repo-map preface (fixture from 0.86.2 in the sandbox over the PINNED corpus, no bytecode), the announce block ahead of it (version, `Git repo: N files`, the budget line, the sandbox's fetch-failure lines) is kept aside and not charged, the tool's own lines feed the index report and no file count is invented, the adapter answers T tasks only with no citation (a map has no F1), the script copies the corpus to the uid-owned tmpfs and runs the map once cold (index) and once per T task (charged) with the query passed nowhere, prepare() reads the container's files and a run that never reached the preface is an error, the pin's digest is the wheel hash the lockfile makes pip require, and a competitor refuses the `none` sandbox. | competitive PR 2h (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_cocoindex.py` | The CocoIndex Code adapter (the embedding representative) parses the tool's real `search` result JSON (fixture from 0.2.41 in the sandbox over the PINNED corpus, no bytecode): each result's `file_path`/`start_line` is a citation, a project-absolute path is made relative, unparseable text cites nothing; the index report reads `init`+`index`'s wall and the `Files:` line of the tool's own status (never the chunk count, which the first parser took for it); the call plan is one `search` per P1 or T task at the defaults, P2 and P4 refused (no documented tool); prepare() reads the driver's file into index and answers; the captured tools/list is the one documented tool; the pin's digest is the wheel hash the lockfile makes pip require, the Dockerfile installs with --require-hashes and carries the offline switches; a competitor refuses the `none` sandbox. | competitive PR 2i (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_sandbox.py` | Every `docker run` of the competitive sandbox carries the D2 flags as a whole list, mounts only the corpus (ro) and /out, passes no host variable; cymbal's citations parse its real --json shapes; a competitor refuses the `none` sandbox; a failed or timed-out index is a not_runnable row, never partial means; the jcodemunch worker's P2 branch asks `check_references` at its defaults (never the import-graph tool, never a raised cap) and cites every content line from the tool's real reply shape (CF-51), and our fairness note exists and argues it; the `counter` variant is the same adapter with one environment variable for the worker in both sandbox modes, declared `variant_of`, registered, never the default (CF-54). | competitive PR 2a (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_serena.py` | The Serena adapter parses the tool's real shapes (fixture from v1.7.0 in the sandbox: grouped find_symbol results, find_referencing_symbols results, LSP 0-based lines converted to 1-based), its call plan follows the fairness note with every call charged and the follow-ups (source reads, references) named by the first answers, P4 is not answered rather than improvised, and the pinned wheel digest is the one the hashed lockfile installs. | competitive PR 2d (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_code_review_graph.py` | The code-review-graph adapter parses the tool's real shapes (fixture from v2.3.8 in the sandbox: search rows, the build summary, the `ambiguous` reply), its call plan follows the fairness note with every call charged and the disambiguation re-run per exact-name candidate, the captured tools/list is the 30-tool default, and the pinned wheel digest is the one the hashed lockfile installs. | competitive PR 2c (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_competitive_codebase_memory.py` | The codebase-memory-mcp adapter parses the tool's real JSON and tree shapes (fixture from v0.10.8 in the sandbox), its call plan follows the fairness note with citation twins uncharged, the MCP driver serialises tools/list in the name/description/inputSchema shape, and the self set's P4 truth is the union of textual and re-export-resolved importers (CF-19). | competitive PR 2b (2026-09-05); docstring | 1 | LOAD-BEARING | competitive layer (docs/competitive/); new file, no history |
| `tests/test_inbound_intake_plan.py` | Intake never re-queues a held or classified item and never acts on an edit or comment by anyone but the author. | inbound item 2 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_sweep.py` | The sweep posts only a draft a HUMAN approved with the literal `approved: true`, verbatim; an edited body posts but resets the graduation streak; a draft is never posted twice; security never graduates. | inbound item 3 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_depkind.py` | A Dependabot PR's kind (patch-or-minor / major / grammar-or-parser / unknown) is decided from the lock delta and the DIFF with no model; a tree-sitter bump wins over a major; a removed package is major; a diff outside the dependency set, a version-pin change, an sdist-exclude edit or a workflow line that is not a `uses:` pin is unknown (item-4 review: file names admitted whole files). | inbound item 4 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_depeval_apply.py` | `agent:ready-to-merge` needs kind patch-or-minor, gate success, every Floor PASS and reviewer APPROVE; every weaker combination is a weaker label; a missing Floor table is UNKNOWN and the table is read from the gate's job log (IN-14); one sticky comment is edited, never duplicated, and the bench's appended table keeps it; the model's assessment is a draft, never posted. | inbound item 4 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_bench_table.py` | The full-corpus bench table is one row per threshold id from two fresh runs; an absent side is n/a, never 0 (F-13, W-28). | inbound item 4 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_selfcheck.py` | The agent-PR self-check reads the never-touch list from POLICY 4.4 and the template from DESIGN 7; the failing test must precede every src/ change; every clause (a) to (f) fails for its own named reason. | inbound item 5 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_fix_gates.py` | The fix job's two no-model gates: the pre-flight declines a run on a bot label without `INBOUND_AUTOFIX`, a blocking label, a young or silent author, or a merged revert newer than the last human label (UNKNOWN blocks); the publish gate refuses a bundle off `inbound/fix-<n>-`, off the top of main, touching a never-touch path or the version pin, with src before the test commit, or a body missing a heading or the `Closes` line. | inbound item 6 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |
| `tests/test_inbound_digest.py` | Every number in the weekly digest is computed by `digest.py` from the ledger; the model's paragraph is admitted only when every digit-run in it is in the JSON it was given; an absent cost or sweep summary is "not recorded", never 0 or empty; a malformed ledger line holds itself only. | inbound item 7 PR (2026-09-04) | 1 | LOAD-BEARING | inbound layer (docs/inbound/); new file, no history |

### tests/fixtures/
| path | holds | first commit | STANDARD | class |
|---|---|---|---|---|
| fixtures/rust/ (basics, guards, qualification, sample .rs; REGENERATE.md) + rust_oracle.json | Rust fixtures + frozen `syn` oracle so CI gates without a toolchain | 927854a 2026-02-15 (sample.rs); c13b623 2026-08-27 (oracle + 3 fixtures) | 1(a) | LOAD-BEARING (test_rust_fidelity.py reads names off disk) |
| fixtures/racket/ (7 .rkt + REGENERATE.md) + racket_oracle.json + racket_reader_oracle.json | frozen expander + reader oracles | 01bd357 2026-08-25; 834d4db 2026-08-29 | 1(a) | LOAD-BEARING |
| fixtures/templates/ (3) | templating-over-language (#336) | f405bee 2026-06-19 | 10 | LOAD-BEARING (test_templates.py) |
| fixtures/astro/ (10) | Astro frontmatter/components | d1aa0e5 2026-05-25 | 10 | LOAD-BEARING (test_astro.py) |
| fixtures/razor (3), svelte, blade, al (1 each) | per-language | c440df9, 7068c7a, cb57224, 68e73ab | 10 | LOAD-BEARING (test_<lang>.py exists) |
| fixtures/{python,typescript,go,javascript,java} | v0.2.0 hardening | 927854a 2026-02-15 | 10 | LOAD-BEARING (24 test files reference `fixtures`) |
| fixtures/{c,cpp,csharp,dart,elixir,perl,php,ruby,arduino,verilog,vhdl,xml} (1-2 each) | language-support fixtures | 2026-03-01..04-13 | 10 | LOAD-BEARING via generic parser tests (no dedicated test_<lang>.py) |
| goldset/corpus/ (events, payments, pos, reporting) | authored implementation-relation corpus | c2e801a 2026-07-18 | 1(c) | LOAD-BEARING |

### tests/conftest.py fixtures
| fixture | scope | guard |
|---|---|---|
| `_pin_code_index_path` | autouse, session | Points `CODE_INDEX_PATH` at a per-worker temp store so no test writes the real `~/.code-index`; session-scoped so module-scoped write-then-read fixtures (`served_repo`) keep working; motivated by the xdist contention failure of 2026-08-17 |
| `_clear_index_cache` | autouse | Clears the SQLite in-memory LRU before and after each test (WAL may not bump mtime) |
| `_reset_global_config` | autouse | Resets `_GLOBAL_CONFIG`/`_PROJECT_CONFIGS`/`_REPO_PATH_CACHE` to `DEFAULTS` BEFORE and after (#411; v1.108.258 #426 — teardown-only let the first test read the real config.jsonc); calls `_close_perf_dbs()` (v1.108.276, #442) |
| `small_index`, `medium_index`, `hierarchy_index` | function | T12 synthetic repos with inline ground truth |
| `tests/conftest_helpers.py` | — | `create_mini_index`, `create_exact_match_index` (snake/camel exact-match with call refs so the SQLite loader must resolve them), `get_index` |

⚠ From 9f6c8d3's body: "14 files still import through the twin [`src.jcodemunch_mcp`]; test_al.py and test_blade.py are the same defect unfired" — the reset does not reach the twin module's `_GLOBAL_CONFIG`.

---

## C. Numbers made more conservative over time

| date | what | before | after | reason |
|---|---|---|---|---|
| 2026-06-22 | CI coverage floor | none | `--cov-fail-under=74` (ce8bb3f v1.108.76) | "current coverage ~76%, so coverage can no longer erode silently". Unchanged since; STANDARD N2 target: raise on evidence. |
| 2026-04-19 | Schema drift tolerance + hard ceiling | none | `DRIFT_TOLERANCE = 0.05`; `core_compact <= 4000` (76cd23e); live-ceiling test 38a7080 v1.108.75 | v2.0.0 §10; frozen sibling only fails after regeneration. At 3,998/4,000 on 2026-09-02. |
| 2026-08-14 | Removed self-referential schema-budget test | `test_v1_108_183.py::test_the_core_compact_schema_budget_is_unchanged` | removed; `test_schema_baseline_transcription.py` added (1.108.278) | "Both sides were the same frozen artifact, so it pinned the artifact and never the surface". |
| 2026-07-05 | Replay gate wired into CI | none | `--gate 0.02` (f0e5942, WS-7) | 2% relative below golden fails. Unchanged. |
| 2026-08-02 | Catalog moratorium exit bar | none | `CATALOG_CEILING = 91`; `EXIT_ROUTE_AT_1 = 60.0` vs 45.8 (3075ffa v1.108.219) | "named HERE, before the work"; leakage bar 0.08. |
| 2026-08-02 | Moratorium bar moved to held-out CONTROL subset | route@1 >= 60% corrected corpus | control-subset route@1 >= 55%, baseline 40.0% (n=20); aggregate never gated (2a1cc2a v1.108.220) | "the 65.9% aggregate is an upper bound produced by a corpus 24/44 fitted to the fix ... This is not a moved goalpost." ⚠ Numeric bar went DOWN (60→55) while the population got harder. ⚠ STANDARD.md §4 Floor still says "route@1 at or above 60%" — inconsistent with the test. |
| 2026-08-21 → 08-27 | CLAUDE.md budget | HARNESS_LIMIT 150,000; BUDGET 130,000 (87ee49d) | BUDGET 140,000 (da23d93, jjg) | LOOSENED: "HALVES that gap" to the harness limit; the 10k buffer is declared the last. |
| 2026-07-23 | README headline | "95% average" (34-file capped express index) | 99.6% (d8bd888 v1.108.163) | Went UP on uncapped re-measure; "95%+ floor phrasing elsewhere is unchanged (conservative floor, still true)". |
| 2026-08-03 | Headline switched read-all → grep-top-3 | 99.6% / 237.3x | 96.4% / 27.9x; range "86-99%" (d7f8d22 v1.108.233) | "the headline moved against us on purpose ... quoting only the aggregate overstates the low end by omission". FAQ "99.6% is a conservative floor" rewritten as answering the wrong objection (1.108.212). |
| 2026-08-25 | Reference re-measured after 22 days drift | 27.9x / 237.3x / 7.3-84.3x | 27.4x / 233.4x / 7.3-79.8x (b98a907) | "Our side moved AGAINST us": +81 gin symbols (#428 Go constants); express/fastapi +4 files at the same commit. |
| 2026-07-29 | Hardcoded jcm constants removed from comparison harnesses | `JCODEMUNCH_*` from 2026-03-28 | read from `jcm_reference.json`; unmeasured → "not measured" (cc397e3) | flipped a published winner (gin: jcm 1.2x leaner → RAG 1.1x leaner). |
| 2026-08-06 | Skill-candidate thresholds | floor 0.8, share cap 0.6 | floor 0.65, cap 0.25 (1.108.249) | "the first guess was backwards"; measured subtrees 0.12-0.13 vs roots 0.33-0.50. |
| 2026-07-16/17 | Savings meter | cache hits uncredited | credited (1.108.133); per-day rollup (1.108.136) | "already conservative in every other respect ... never overstates". |
| 2026-05-08 | `receipt` multipliers | — | "deliberately conservative against the 30–56× RAG-benchmark numbers" (1.85.0) | design choice |
| 2026-07-18 | Channel priors vs measurement | AST 0.85 / duck 0.65 / decorator 0.45 | measured 0.833 / 0.6 / decorator prior "more conservative than measured" (1.108.140) | priors kept, artifact pinned |
| 2026-06-26 | Watcher poll floor | 300 ms | 1000 ms (1.108.83, #356) | idle CPU under WSL |
| 2026-09-01 | Dead-code confidence on inadequate corpus | 1.0 published | `UNPROVEN_CEILING` 0.6 < 0.8 default, so the default call refuses (1.108.315/#566) | "PROVABLY UNREACHABLE ... computed from the INDEX with nothing in between" |
| 2026-08-15 | Counter schema-saving % | hand-typed "~98%" | computed 95.9% (bc5a31e) | "the budget guardrail only walked tool_profile" |

`git log -S'27.4x'`: cc397e3 07-29, b98a907 08-25, 10ad479 08-25, 0965117 08-26, 83ae9c3/999ea0d/9322855 09-03 (STANDARD phases). README ratio history: 99.5% single-row table (d371e52 2026-02-08) → "95% average, 79.7%-99.8%" (574ca15 2026-03-27) → 99.6% (2026-07-23) → 96.4%/27.9x (2026-08-03) → 27.4x (2026-08-25).

---

## D. Tests added and later removed

Only FOUR test files were ever deleted; one rename.

| file | deleting commit | class | why |
|---|---|---|---|
| tests/test_antigravity_install.py (129 lines) | 7189258 2026-06-30 "v1.108.89 — revert 1.108.88 Antigravity install target" | retired-with-reason | "wrote to the wrong locations (~/.gemini/settings.json + ~/.gemini/antigravity/skills/, taken from a competitor README) and never actually configured Antigravity ... restored byte-for-byte to their 1.108.87 shape." |
| tests/test_backpressure_regression.py | 0d90e76 2026-03-28 "perf: remove staleness _meta injection and retire wait_for_fresh / check_freshness tools" | retired-with-reason | "no client acted on these fields — they were annotated noise on every single response ... 670 lines of tests covering the removed surface area [removed] ... 1256 tests passing, 0 regressions". |
| tests/test_check_freshness.py | 0d90e76 | retired-with-reason | tool deleted |
| tests/test_wait_for_fresh.py | 0d90e76 | retired-with-reason | tool deleted. ⚠ `benchmarks/profile_backpressure.py` (720049b, five days earlier) still exercises the removed surface — STALE. |
| tests/test_init_codex_opencode.py → tests/test_init_client_schemas.py | 8c7a6d8 2026-08-17 (R058) | renamed/merged | successor `test_init_client_schemas.py` |

Removed within a file, named by CHANGELOG: `test_v1_108_183.py::test_the_core_compact_schema_budget_is_unchanged` (1.108.278) — cover: `test_schema_budget.py` live ceiling + `test_schema_baseline_transcription.py`. Seven `is` assertions in `tests/test_result_cache.py` rewritten to `==` (1.108.316, Practice 9) — a rewrite, not a removal.

Regret grep over CHANGELOG.md + ISSUE-HISTORY.md (`deleted test|removed test|should not have|reinstat|restored the test`): NO entry calls any removal a mistake. Hits: 1.108.278 defending the removal above; `test_refresh_corpus_unreadable.py` "reinstates the one-directional check" — that re-installs a DEFECT for non-vacuity, not a restored test. STANDARD §9 Gap records the exposure: "nothing today fails if one of these tests is removed."

---

## E. CI command, runtime, markers

- **CI test command** (`.github/workflows/test.yml:84,106`): `uv sync --locked --group dev --extra watch` then `uv run pytest tests/ -n 4 --dist loadfile --tb=short --cov=src --cov-report=term-missing --cov-fail-under=74`. Second leg (line 141) syncs without `--extra watch`. `pyproject.toml`: `testpaths=["tests"]`, `asyncio_mode="auto"`; dev group `pytest-xdist>=3.6.0`; no pytest-timeout, no rerunfailures.
- **Documented runtime** (9f6c8d3 body, 2026-08-16): "599s serial -> 183s parallel on a 24-core box, same 7,859 collected. CI runs `-n 4 --dist loadfile`; with coverage that command is 258s locally." `--dist loadfile` is load-bearing; `-n 4` pinned because runners are 4-core. CLAUDE.md Practice 10: full local 12:55, CI 9:21 (#558, 2026-08-28). STANDARD N1 floor: CI < 20 min, local `-n auto` < 6 min; no `timeout-minutes` on the test job (benchmark.yml has 45).
- **Replay gate** (`replay.yml`): index self, `run_replay.py --fixture self_v1_75_0.json --baseline-file results/self_v1_75_0-golden.json --gate 0.02`.
- **Benchmark job** (`benchmark.yml`): Mondays 07:00 UTC; clones pinned corpora; `run_benchmark.py --verify-determinism`; compares to `jcm_reference.json`; `::warning::` on drift, never fails.
- **Markers in tests/**: `pytest.mark.skipif` 39; `importorskip` 31; `pytest.skip(` 80 call sites; `pytest.mark.skip(` 0; `pytest.mark.xfail` 0; `parametrize` 352; `asyncio` 174; `filterwarnings` 2; no `slow`/`integration`/`timeout`. `flaky` (5) and `rerun` (7) occur only in prose and test NAMES — no flaky plugin, no rerun decorator. Skip counts: local 13, CI ubuntu 26 / windows 19 (CLAUDE.md); STANDARD N7 floor ubuntu ≤30 / windows ≤25.


## 5. UNCLEAR items for human review (untouched this session)

| path | what it asserts | what was found | question |
|---|---|---|---|
| `tests/test_agent_selector.py` | ComplexityScorer/ModelRouter scoring, clamping, threshold boundaries, and tier-to-model resolution. | 57b7455 "feat: add Agent Selector — complexity-based model routing (v1)" | Feature unit tests for `agent_selector`; no issue or downstream failure named; 39 tests. |
| `tests/test_config_watcher_keys.py` | The five watcher config keys exist in `DEFAULTS` with the expected default values and types. | 845a2df "feat: config-driven watcher params; remove retired tools from docs" | Asserts literal defaults only (`watch_paths == []`, etc.); restates the constant table rather than a behaviour; not covered by test_docs_config_parity.py (that one checks docs vs code, not the values themselves) |
| `tests/test_groq_voice.py` | `gcm --voice` audio-dep check, WAV capture bytes, and transcription pipeline with sounddevice/numpy mocked via `sys.modules`. | `2983cf4 feat: Phase 4 — Voice-to-Codebase + Auto Repo Explainer (v1.40.0)` | One test admits "may or may not be None... just check it doesn't crash" — weak assertion; mocks `sys.modules`, fragile under xdist. No issue history since 2026-04-13. |
| `benchmarks/cache_stability/` | reshuffled-output share of `get_ranked_context` | no README; artifact pinned by nothing; every number moved on re-run 2026-09-03 | is the `hold` verdict still consulted anywhere? |
